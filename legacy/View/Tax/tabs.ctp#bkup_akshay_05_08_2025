<?php

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
?>
<script>
     $(document).ready(function(){
        
        
        
                var usersoptions = {
            url: function (phrase) {
                return livesite+"UserCredentials/getusers?username=" + phrase;
            },
            getValue: "full_name",
            list: {
                onClickEvent: function () {
                     var selectedItem = $("#user").getSelectedItemData();
                    var site_fkey = selectedItem.emp_pkey;
                    $('#emp_pkey').val(selectedItem.emp_pkey);
                     //$('#loaders').show();
                  $('#load').load(livesite+'Tax/setup/'+site_fkey);
                    //reloadDatagrid(site_fkey);

                }
            }
        };

        $('#user').easyAutocomplete(usersoptions);
       $('#tt').tree({
   
});
    })
</script>

<section class="content">
    <div class="row">
<div class="col-md-12">
<div class="box box-header">
       <div class="box-body" id="div-reportcriterias">
                     <div class="col-md-12">
                     <input type="hidden" id="emp_pkey">
                
                     <div class="col-md-3" ><h2>Tax Details</h2> </div> <div class="col-md-6" style="padding-top: 21px ; "> <input type="text" id="user" class="form-control" name="user" placeholder="Search Your Employee Name" /> </div>
                     
            </div>
                </div>
  
    
    
</div>
 <div id="load" class="box box-body" style="margin:10px 0">
       
    </div> 
</div>
        
    </div>
</section>