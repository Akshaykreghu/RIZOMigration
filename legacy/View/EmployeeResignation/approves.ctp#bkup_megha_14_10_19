<?php
/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
?>
<div class="box box-default collapsed-box box-solid">
    <div class="box-header with-border">
        <h3 class="box-title">Full And Final Settlement Summary </h3>

        <div class="box-tools pull-right">
            <button onclick="toggleExpandable(this, 'contain2')" type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-plus"></i>
            </button>
        </div>
        <!-- /.box-tools -->
    </div>
    <!-- /.box-header -->
    <div class="box-body" id="contain2" >
        <h3>Settlement summary</h3>
        <form id="heads" method="POST" action="<?php echo $this->webroot; ?>EmployeeResignation/save_heads" >
        <table class="table" >
            <thead>
                <th>Month</th>
                <th>Name</th>
                <th>Amount</th>
            </thead>
            <tbody>
            <?php $sum = 0; ?>    
            <?php foreach ($arr_emp_settle as $val){ ?>
                <tr>
                    <td><?php echo $val['emp_settle_slip']['month_year']; ?></td>
                    <td><?php echo $val['emp_settle_slip']['salary_head_item_desc']; ?></td>
                    <td><?php echo $val['emp_settle_slip']['salary_rate']; ?></td>
                    <td><div><input type="number" class="net_sal" name="head_vals[]" value="<?php echo $val['emp_settle_slip']['salary_rate']; ?>" > <input type="hidden" name="head_fkey[]" ></div></td>
                    <?php $sum = $sum + $val['emp_settle_slip']['salary_rate']; ?>
                </tr>
            <?php } ?>
                <tr><td colspan="2" >Total</td><td><?php echo '<b>'.$sum.'</b>'; ?></td><td id="total_amt_s"></td></tr>
                
            </tbody>
        </table><br>
        <button class="btn btn-primary pull-right">Generate Full and Final Slip</button>
        </form>
    </div>
</div>
    <script>
        var options = {
            success: function (resp) {
                process();
                $.notify($.parseJSON(resp).msg, {
                    type: 'success',
                    allow_dismiss: false,
                    autoHideDelay: 50000,
                });
            }  // post-submit callback
        };
        $('#heads').on('submit', function (event) {
            $("#contain2").prepend('<div class="loader-div"><li class="fa fa-spinner fa-spin"></li></div>');
           setTimeout(function() { $(".loader-div").hide(); }, 1000)
            event.preventDefault();
            if (confirm(" Do You Want  To Save The Form  ")) {
                $('#heads').ajaxSubmit(options)
            }
        });
        jQuery(document).ready(function ($) {
            var net_sal_sum;
            $(".net_sal").each(function (index) {
                net_sal_sum++;
            });
            $('#total_amt_s').html(net_sal_sum);
            
        });
        
        
    </script>