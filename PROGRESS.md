# RIZO Migration — Execution Progress

**Reference:** See `MIGRATION_PLAN.md` for full technical details.  
**Rule:** Mark each task `[x]` immediately when completed. Never batch.

---

## Phase 0: Pre-Work
**Status: COMPLETE** (table names, stored procs, and controller mapping done in MIGRATION_PLAN.md sections 7 & 8)

- [x] Read complete `mypayrol_trial.sql` — table definitions extracted (MIGRATION_PLAN.md §7)
- [x] Map CakePHP Model `$useTable` to real table names (MIGRATION_PLAN.md §7)
- [x] Document stored procedure parameters (MIGRATION_PLAN.md §8)
- [x] Map all active (non-backup) controllers to features (MIGRATION_PLAN.md §2.2)
- [ ] Create local MySQL test instance with `mypayrol_control_db` + `mypayrol_trial` ← **manual step for user**
- [ ] Verify stored procedures callable from test script ← **manual step after MySQL is up**

**Note:** MySQL setup is a manual environment step. Code work begins at Phase 1.

---

## Phase 1: Infrastructure
**Status: ✅ COMPLETE** — Build passes clean. `npm run dev` will serve the app.

> **Note:** `create-next-app` installed Next.js **16.2.9** (not 14 as planned). Differences handled:
> - `src/middleware.ts` is now `src/proxy.ts` (Next.js 16 renamed the convention)
> - Turbopack is default in Next.js 16; webpack config replaced with `turbopack: {}`
> - `useSearchParams()` requires `<Suspense>` boundary (applied in login page)

### 1.1 Project Initialization
- [x] Initialize Next.js project (`rizo/`) — **v16.2.9** with TypeScript, Tailwind, App Router, src-dir
- [x] Install core dependencies (next-auth, mysql2, tanstack query/table, react-hook-form, zod, clsx, tailwind-merge)
- [x] Install export/chart deps (jspdf, jspdf-autotable, xlsx, recharts, lucide-react)
- [x] Install bcryptjs + type defs

### 1.2 Core Library Files
- [x] `src/lib/db.ts` — dual-pool connection manager (controlPool + per-company pool cache, global singleton for hot-reload)
- [x] `src/lib/auth.ts` — NextAuth CredentialsProvider + SHA1→bcrypt transparent upgrade + planId in JWT
- [x] `src/lib/utils.ts` — cn() + formatDate, formatCurrency, formatYearMonth, currentYearMonth
- [x] `src/lib/company-config.ts` — per-company feature flag helper (getCompanyConfig)
- [x] `src/lib/features.ts` — plan_feature gate helpers (isPlanFeatureEnabled, getPlanFeatures)
- [x] `src/types/next-auth.d.ts` — session + JWT type extensions

### 1.3 Auth & Routing
- [x] `src/proxy.ts` — route protection proxy (all routes except /login and /api/auth)
- [x] `src/app/api/auth/[...nextauth]/route.ts` — NextAuth handler
- [x] `src/app/(auth)/login/page.tsx` — login form (company code + username + password) with Suspense boundary

### 1.4 Base Layout & Shell Components
- [x] `src/app/(dashboard)/layout.tsx` — authenticated layout shell (server-side session check)
- [x] `src/components/layout/Sidebar.tsx` — collapsible sidebar with admin/employee visibility
- [x] `src/components/layout/Header.tsx` — top header with user info + logout
- [x] `src/app/(dashboard)/dashboard/page.tsx` — dashboard stub (stats placeholder)
- [x] `src/components/data-table/DataTable.tsx` — reusable TanStack Table v8 (client+server pagination, sorting)
- [x] `.env.local` — local dev environment variables

### 1.5 Providers & Config
- [x] `src/app/layout.tsx` — root layout with Providers (SessionProvider + QueryClientProvider)
- [x] `src/app/providers.tsx` — client providers wrapper
- [x] `src/app/page.tsx` — root redirect to /dashboard
- [x] `next.config.ts` — Turbopack config

**Milestone:** ✅ Build passes. Connect MySQL and run `npm run dev` to verify login → dashboard flow.

---

## Phase 2: Company Setup + Employee Core
**Status: ✅ COMPLETE (all setup screens done, including deferred cleanup items)**

### 2.1 Dashboard
- [x] `GET /api/dashboard/stats` — employee count, present today, pending leaves
- [x] `GET /api/dashboard/events` — birthdays and work anniversaries (7-day window)
- [x] `GET /api/dashboard/notifications` — pending leave + salary increment notifications
- [x] `/dashboard` page — admin view (stats cards, events, notifications); graceful fallback if DB not connected
- [ ] `/dashboard` page — employee view (personal summary, leave balance, attendance) ← Phase 9 polish

### 2.2 Organizational Setup (CRUD)
- [x] Branches — `GET/POST /api/setup/branches`, `PUT/DELETE /api/setup/branches/[id]`, `/setup/branches` page
- [x] Departments — `GET/POST /api/setup/departments`, `PUT/DELETE /[id]`, `/setup/departments` page
- [x] Designations — `GET/POST /api/setup/designations`, `PUT/DELETE /[id]`, `/setup/designations` page
- [x] Grades — `GET/POST /api/setup/grades`, `PUT/DELETE /[id]`, `/setup/grades` page
- [x] Financial Year — `GET/POST /api/setup/financial-year`, `PUT/DELETE /[id]`, `/setup/financial-year` page (branch-scoped, duplicate branch/status/type/year rejected via DB unique key)
- [x] Holiday Calendar — `holiday-groups` + `holidays` APIs, `/setup/holidays` page (group→holiday drill-down); group delete blocked if employees (`emp_proff.HOLIDAY_GROUP_ID`) assigned
- [x] Attendance Config (DB Config) — `GET/PUT /api/setup/attendance-config`, `/setup/attendance-config` page; validates `attendance_format` (A/B) and `attendance_date` (0-31), the two fields `att_start_end_fn` needs for Phase 3
- [x] Shifts — full parity port of `working_day_time_procedures` + `shift_exceptions`, `GET/POST /api/setup/shifts`, `PUT/DELETE /[id]`, `/setup/shifts` list + `/setup/shifts/new` + `/setup/shifts/[id]` bespoke form (weekday grid, 6 duty windows, grace/OT rules, conditional exceptions sub-table)
- [x] Reusable `SetupCrudPage` component (`src/components/setup/SetupCrudPage.tsx`) — extended with `type` (date/number/select/checkbox), `options`, and `queryParams` for scoped lists (used by Holidays)

### 2.3 Company Profile
- [x] `GET/PUT /api/company` — reads/updates `comp_contact_info`
- [x] `/setup/company` page

### 2.4 Employee Management
- [x] `GET /api/employees` — list (admin: all with pagination/search/status/branch filters; employee: self only)
- [x] `POST /api/employees` — create new employee (emp_details + emp_proff + emp_ctc_upload in transaction, duplicate id_card/lwf_code check)
- [x] `GET/PUT /api/employees/[id]` — view/edit profile (personal, professional, salary & statutory, bank details)
- [x] `PATCH /api/employees/[id]` — activate/deactivate (status 1/2)
- [x] `POST /api/upload` — generic file upload (used for employee photo)
- [x] `GET /api/setup/salary-structures` — lookup for structure assignment
- [ ] `GET/PUT /api/employees/[id]/access` — menu access (user_access table) ← Priority 3 of Employee gap-closure plan
- [x] `/employees` list page — status (Active/Resigned) + branch filters, Activate/Deactivate row action
- [x] `/employees/new` create page — Personal (+Gender/Blood Group/Marital Status/Photo), Professional (+Employment Type/Reporting Manager/Probation), Salary & Statutory (structure/CTC/PAN/PF/ESIC), Bank Details
- [x] `/employees/[id]` detail/edit page — same 4 sections, inline edit toggle
- [x] `DataTable` updated with `onRowClick` prop
- [x] New shared components: `EmployeeSearch`, `FileUploadField`

**Milestone:** ✅ Build passes. Can view/manage employees and org structure, with full personal/professional/salary/bank data entry matching live-site parity for Priority 1 of the Employee gap-closure plan (see gap report comparison against `in.mypayrollmaster.online`). Cost Center/UAN/PT Applicable/Bank Account Type intentionally excluded — no backing columns in the schema, and the decision was made not to alter the database. CSV import still open (needs `DataUploaderController.php` column mapping). Resignation, Menu Allocation, Bulk Policies, Document Upload/Generation, IT Declarations remain per the gap-closure plan's Priorities 2-4.

### 2.5 Employee Join (Priority 3.1 of the Employee gap-closure plan)
- [x] `GET/POST /api/employees/join` — list pending (`emp_join.status=1`) candidates / create new staging record
- [x] `GET/PUT/DELETE /api/employees/join/[id]` — fetch full staging record (join + documents + education + experience + family), update personal fields, discard (hard-delete, relies on `ON DELETE CASCADE` for child rows)
- [x] `POST/DELETE /api/employees/join/[id]/{documents,education,experience,family}[/[rowId]]` — child sub-tab CRUD on staging tables (`emp_documents`, `education`, `work_experience`, `family`, all keyed by `emp_join_fkey`)
- [x] `POST /api/employees/join/[id]/onboard` — transactional conversion: duplicate check (PAN/Aadhaar/ESI/UAN/LWF/account no. against active `emp_details`, plus `emp_id`/`emp_company_id`/username uniqueness) → insert `emp_details` (incl. the legacy `pf`/`company_pf` field swap) + `emp_proff` + optional `emp_ctc_upload` + `user_credentials` (bcrypt-hashed password, `access_allowed='n'`) → mark `emp_join.status=0` + `emp_fkey` → copy staged child rows to permanent tables (`education→qualifcations`, `work_experience→history`, `family→emp_family`, `emp_documents→emp_passport_visa`)
- [x] `GET /api/setup/nationalities` — lookup (`countries_nationality` table) for the join form's Nationality/Country of Origin selects
- [x] `/employees/join` list page — "Employee Joining" (pending candidates, Edit/Continue Onboarding/Discard actions) + "All Employees" tab (reuses the existing `/employees` list page component directly)
- [x] `/employees/join/new` — staging record creation form (Personal/Statutory/Bank sections)
- [x] `/employees/join/[id]` — staging record edit page with Documents/Education/Work Experience/Family sub-tabs (repeatable add/remove rows)
- [x] `/employees/join/[id]/onboard` — onboarding conversion form (Employee ID/Employee Company ID/Login Username+Password, Professional Details, Salary) — matches legacy: no auto-generated username/password, no onboarding email sent
- [x] New shared components: `RepeatableRows` (generic add/remove row list, used 3x for Education/Experience/Family), `DocumentUploadField` (generic file upload, reuses `/api/upload`)
- [x] Nav: added "Employee Join" under the Employees section in `Sidebar.tsx`

Schema-verified against the real `mypayrol_mpm121` dev DB before implementation (not just the schema dump): `emp_join`, `emp_documents`, `education`, `work_experience`, `family`, `qualifcations`, `history`, `emp_family`, `emp_passport_visa`, `countries_nationality` all confirmed to match the dump exactly. Curl-verified end-to-end as admin: created a staging record with all 4 child row types, ran the full onboarding conversion (confirmed the `pf`/`company_pf` swap landed correctly, CTC/professional/login rows all created, staging row correctly marked `status=0` with `emp_fkey` set, all 4 child tables copied to their permanent counterparts), confirmed the duplicate-PAN check blocks onboarding with 409, confirmed discard hard-deletes a staging record via cascade. All test rows cleaned up afterward (including the `emp_ctc_transaction` cascade row, same gotcha as documented in the critical fix below).

Per an explicit scope decision earlier in the plan, Education/Work Experience/Family sub-tabs were included in this pass (full legacy parity) rather than deferred, and onboarding matches legacy exactly for login creation (HR types both username and initial password on the conversion form — no auto-generation, no email).

**emp_id auto-generation (2026-07-10):** ported legacy's `loadEmpProfDetails()` auto-numbering (byte-identical in `EmployeeController.php`/`EmployeeJoinController.php`) as `src/lib/empId.ts`'s `generateNextEmpId()` — next sequential numeric ID (MAX+1, seeded at 1000 for a company's first employee), used by both `POST /api/employees` and `POST /api/employees/join/[id]/onboard` when `emp_id` is left blank. Deliberately implemented as plain-numeric (no company-code prefix) based on real dev data (`emp_id` values like `1000227`), not the letter-prefix reading suggested by a literal read of the legacy source — verified against real data before implementing, per the project's standing schema-drift discipline. Confirmed `emp_company_id` (a separate `emp_proff` field, also used as part of the login username) is NOT auto-generated in legacy — always manually typed by HR — so it correctly remains a required manual field in both forms.

**Gap-closure fixes vs. live site comparison (2026-07-10):** an external gap report comparing this module against `in.mypayrollmaster.online` surfaced several real bugs, since fixed:
- Edit page (`/employees/join/[id]`) was silently missing most fields present on the New form (Gender, Nationality, Country of Origin, Guardian/Relation, Photo, the entire Statutory Details and Bank Details sections) — a real data-loss risk since saving from Edit would have wiped anything not shown. Rebuilt to full parity with the New form.
- Date of Birth wasn't pre-filling on Edit — root cause was the API returning a full ISO datetime string directly into a `type="date"` input; fixed by slicing to `YYYY-MM-DD` when populating form state.
- Nationality/Country of Origin dropdowns were showing the wrong list (demonyms like "Afghan" instead of country names like "AFGHANISTAN", per legacy) and several `countries_nationality.nationality` values are genuinely corrupted in the real DB (mojibake, e.g. `BelarusianÂ¿Â¿orÂ¿Â¿Belarusan` — confirmed via `HEX()` inspection, a real data quality issue, not a display artifact). Fixed by switching both dropdowns to `country_name` (matches legacy and is clean across all 189 rows — no DB fix needed since the corrupted column is no longer used for display).
- "EPS" was a free-text field; legacy has it as a Yes/No eligibility checkbox — fixed to a checkbox (`Y`/`N`) on both New and Edit forms.
- Marital Status was missing Divorced/Widowed (legacy has all four); added, and made the Edit page use the same select control as New (was inconsistently free-text).
- Added a PAN format placeholder (`ABCDE1234D`) matching legacy.
- List page (`/employees/join`): added an avatar/photo column, a name/mobile search box, and real pagination (previously loaded all records unpaginated) — reused the existing `DataTable` component and added `search`/`page`/`pageSize` params to `GET /api/employees/join`.
- Added bulk import: `GET /api/employees/join/template` (downloads an `.xlsx` template with the exact legacy column order/headers) and `POST /api/employees/join/upload` (parses an uploaded `.xlsx`, validates mandatory fields per row, dedupes against active `emp_details` by Aadhaar/PAN, resolves Nationality/Country of Origin by name against `countries_nationality`, converts Yes/No columns, and reports per-row errors without failing the whole batch).
- Deliberately **not** built: the legacy toolbar's "Import" button turned out (per legacy source research) to be an unrelated third-party "MyProfile" OTP-based external API integration, not a file import — out of scope, no equivalent credentials/API exist for this migration. The toolbar's row-selection "OnBoarding" button was also skipped since it opens the same edit/onboarding screen already reachable via this page's per-row "Continue Onboarding" action — redundant UX, not new capability.

Curl-verified all of the above end-to-end as admin: created and edited a staging record confirming Nationality/EPS/Marital Status round-trip correctly, downloaded the template and confirmed it round-trips through the `xlsx` library, uploaded a 2-row file (one valid, one deliberately incomplete) and confirmed 1 inserted / 1 reported error with the correct row number, confirmed a genuine Aadhaar/PAN collision against real seed data is caught by the dedupe check. All test rows cleaned up.

### 2.6 Employee Access (Priority 2.1 of the Employee gap-closure plan)
- [x] `GET /api/employees/access` — list active employees joined to `user_credentials` (web login) + `mob_user_credentials` (mobile access/punch type) via `user_id`
- [x] `PUT /api/employees/access/[id]` — single combined save (matches legacy's one-form-submit design): toggles `user_credentials.access_allowed`/`locked` (implicitly zeroes `incorrect_login_attempt` on unlock), creates a new `user_credentials` row on demand if the employee has no login yet (username + password required), upserts `mob_user_credentials` (creating it on first save if missing, exactly as legacy does) for mobile access (`locked` field, inverted) and `punchtype`; password reset hashes via bcrypt and sets `reset_login_flag='Y'`/`locked=0`
- [x] `POST /api/employees/access/[id]/reset-device` — clears `mob_user_credentials` securitycode/macid/imei and sets `token='Y'` to force re-registration (legacy's `resetmobiles()`)
- [x] `/employees/access` page — grid with web login/mobile access/punch-type status chips, "Manage" modal (access/lock/mobile/punch-type/password reset), "Reset Device" action
- [x] Deliberate deviation from legacy: mobile password is not stored in plaintext on `mob_user_credentials.password` (legacy does this) — out of scope for this pass since no mobile client exists in this migration; mobile login itself is unaffected since it's a separate app hitting the same DB

Schema-verified: confirmed `user_credentials` has no `mobileaccess`/`punchtype` columns in the real dev DB (only `mob_user_credentials.punchtype` is real) — the legacy research flagged these as needing verification and they turned out to be dead/nonexistent columns in this schema, consistent with the project's recurring schema-drift gotcha. Punch type codes confirmed from legacy: `W`=Machine/biometric, `M`=Mobile from anywhere, `O`=Mobile from office only, `S`=Web (gates the employee dashboard's web check-in/out buttons). Curl-verified end-to-end as admin against real employee data: toggled web access/lock, reset a password (confirmed `reset_login_flag` flips to `Y`), set mobile access + punch type (confirmed a new `mob_user_credentials` row is created on demand), ran reset-device, then fully restored the test employee's original state (including deleting the newly-created `mob_user_credentials` row, since none existed originally). Found and fixed one bug during verification: the `locked` field arrived as the string `"0"` from the request body, which is truthy in JS — `body.locked ? 1 : 0` always evaluated true; fixed to `Number(body.locked) === 1 ? 1 : 0`.

### 2.7 Allocate Assets (Priority 2.3 of the Employee gap-closure plan)
- [x] `GET/POST /api/assets`, `PUT/DELETE /api/assets/[id]` — asset catalog CRUD (`asset_management`), soft-delete via `active='0'`
- [x] `GET /api/setup/asset-types` — lookup (`asset_types` table)
- [x] `GET/POST /api/employees/assets` — list allocations (joined to `emp_details` for holder name) / allocate a catalog asset to an employee (denormalizes name/model/brand/serial/warranty onto the `asset_allocate` row, syncs `asset_management.status='Allocated'`, blocks with 409 if the asset is already allocated)
- [x] `PUT /api/employees/assets/[id]` — mark returned: sets `asset_allocate.status='Returned'` + `retreived_date`, syncs `asset_management.status='Returned'` back (two-table sync, matches legacy `release()`)
- [x] `/assets` page — catalog CRUD via the reusable `SetupCrudPage` component
- [x] `/employees/assets` page — allocation list + "Allocate Asset" modal (`EmployeeSearch` + available-assets select + condition + date) + "Mark Returned" row action
- [x] Nav: "Assets" (catalog, already existed as a dead link in `Sidebar.tsx`, now has a real page) + "Allocate Assets" added under Employees

Schema-verified against the real dev DB before implementation — corrected an initial misreading of the schema dump: `asset_management.emp_fkey` looked like it might mean "current holder" but legacy research (`AssetController.php`) confirmed it's vestigial/never populated by the create-asset form — `asset_management` is a genuine shared catalog, and ownership lives entirely in `asset_allocate` (a reallocation log keyed by `emp_fkey` + `asset` = the catalog's `asset_pkey` as a string). Also confirmed `asset_allocate.pemp_fkey` is dead/unused in all legacy code paths — not modeled as anything in the port. `asset_state` codes confirmed: `1`=Good, `2`=Damaged But Working, `3`=Not Working. Curl-verified end-to-end as admin: created a catalog asset, allocated it to an employee (confirmed the catalog's `status` flipped to `Allocated`), confirmed a double-allocation attempt correctly 409s, marked it returned (confirmed both tables synced back to `Returned`), edited and soft-deleted the catalog entry. All test rows cleaned up.

### 2.8 Allocate Policies in Bulk (Priority 3.2 of the Employee gap-closure plan)
- [x] `POST /api/employees/bulk-policies` — single endpoint, `{ type, policy_id, emp_fkeys[] }`. For SHIFT/LEAVE/HOLIDAY/NOTICEPER: inserts an `emp_config` audit row per employee (mirrors legacy `addEmpTo{Shift,Leave,Holiday,Notice}`) and updates the live `emp_proff` column those features actually read from (`day_time_seq`/`LEAVEPOLICY_GROUP_ID`/`HOLIDAY_GROUP_ID`/`notice_days`, the last resolved from `notice_period.notice_days` since `emp_proff.notice_days` stores the day count directly, not the master's PK). For SALARY: calls the real legacy stored function `sal_structure_distribution_fn(company_code, emp_fkey, structure_id, user_id)` once per employee (never rewritten, per project policy on stored procs) and only records the `emp_config` audit row if the function returns success; reports employees where it returned failure (typically means no active CTC assigned yet) without failing the whole batch.
- [x] `GET /api/setup/notice-periods`, `GET /api/setup/leavepolicy-groups` — new lookups (reused existing `/api/setup/shifts`, `/api/setup/holiday-groups`, `/api/setup/salary-structures` for the other three sections)
- [x] `/employees/bulk-policies` page — 5 sections (Shift/Leave/Holiday/Notice Period/Salary Structure), each: policy select + filterable employee checkbox list + bulk assign button, reporting assigned/failed counts
- [x] Nav: "Allocate Policies in Bulk" added under Employees

Scope note: legacy's `EmployeeConfigController.php` actually has more `emp_config` types than originally covered (GRADE, DIVISION, SECTION, HIERARCHY/reporting-manager, LAPPR/leave-approval-hierarchy) — these were added in the follow-up gap-closure pass below (2.10). `MSHIFT`/additional-shift with primary-shift swap logic remains out of scope. Also confirmed `sal_structure_distribution_fn`'s actual signature takes one `emp_fkey` at a time (`RETURNS int`), not a comma-list as initially reported by research — verified directly via `SHOW CREATE FUNCTION` against the real DB before trusting it, consistent with the project's standing schema-drift discipline. Curl-verified end-to-end as admin: bulk-assigned Shift/Holiday/Leave/Notice Period to a real employee and confirmed both the `emp_config` audit row and the corresponding `emp_proff` column updated; called the salary function with an invalid `structure_id` (correctly returned 0/failed) and then a real one (correctly populated a full `emp_salary_structure` breakdown and the `emp_config` audit row). All test assignments cleaned up afterward.

### 2.9 Menu Allocation (Priority 3.3 of the Employee gap-closure plan)
- [x] `GET /api/employees/menu-allocation/[id]` — builds the full `emp_menu` hierarchy (`parent_id`-based tree, `active='Y'` only) plus which `menu_id`s are currently active for this employee via `user_access` (confirmed `user_access.user_fkey` references `emp_details.emp_pkey`, not `user_credentials.user_pkey` — verified against real data since a naive read of the schema doesn't disambiguate the two, given they're often numerically identical)
- [x] `PUT /api/employees/menu-allocation/[id]` — diffs the submitted `menu_ids[]` against existing `user_access` rows: reactivates/updates matches, inserts new rows (`organization_id` hardcoded to `'1'`, the only value observed in real data — multi-tenancy is handled at the DB level in this architecture, so this column looks vestigial), and soft-deactivates (`active='N', status=0`) anything removed
- [x] `/employees/menu-allocation` page — `EmployeeSearch` to pick an employee, then a checkbox tree (`<MenuTree>`, recursive component) built from the real hierarchy, Save button
- [x] New shared component: `MenuTree` (recursive checkbox tree)
- [x] Nav: "Menu Allocation" added under Employees

Curl-verified end-to-end as admin: fetched the real menu tree and an employee's real pre-existing assignment (48 active menu items), saved a small test set and confirmed the diff correctly deactivated everything not in the new set while activating the rest, then restored the employee's original 48-item assignment exactly. (Mid-verification the dev server had stopped responding for an unrelated reason and was restarted — session/login re-established before continuing, no impact on the changes made.)

### Maintenance pass (2026-07-10): fixes from an Employee-menu design comparison report
An external design-comparison report (browser review of legacy vs. Next.js) was mostly stale — it predated the Bulk Policies/Menu Allocation work above — but surfaced two real, confirmed issues:
- **Allocate Assets "Asset" column rendered empty** on rows created before this feature existed. Root cause: `asset_allocate.asset_name`/`model`/`brand` are empty strings (not NULL) on old rows, so a first-pass `COALESCE(a.asset_name, m.name)` fallback silently did nothing (`COALESCE` only substitutes on NULL, and an empty string isn't NULL) — caught this via curl-verification after the initial fix, then corrected to `COALESCE(NULLIF(a.asset_name, ''), m.name)` (`rizo/src/app/api/employees/assets/route.ts` GET), joined to `asset_management` on `a.asset = m.asset_pkey`. Confirmed via curl that every row now shows a real name/model/brand, old and new alike. No data migration needed.
- **User Access and Allocate Assets lacked search/pagination**, unlike `/employees` and `/employees/join`. Brought both up to the same standard: added `search`/`page`/`pageSize` params returning `{ data, total }` to `GET /api/employees/access` and `GET /api/employees/assets`, and replaced the hand-rolled `<table>` in both pages with the shared `<DataTable>` component plus a search box, matching the existing `/employees`/`/employees/join` pattern exactly (including the inlined `LIMIT/OFFSET` mysql2 workaround).

Explicitly not done, per your decisions: Resignation workflow stays out of scope (unchanged from the original plan decision), and no change to the Add Employee / Employee Join relationship (both are intentional, complementary paths — no code change needed). Curl-verified both endpoints' search and pagination against real data (160 real employees on Access, real asset allocations). `npm run build`/`tsc --noEmit` clean.

### 2.10 Bulk Policy Allocation gap-closure (2026-07-10): Division, Section, Grade, Employee Hierarchy, Leave Hierarchy
A screenshot comparison of the live legacy "Bulk Policy Allocation" page against the Next.js "Allocate Policies in Bulk" page showed 5 of legacy's 10 tabs were missing entirely (Division, Section, Grade, Employee Hierarchy, Leave Hierarchy) — a real gap I'd missed when originally scoping 2.8. Researched the real legacy controller before building:
- **Division / Section / Grade**: identical mechanism to each other and to the existing Shift/Leave/Holiday sections — pick a master record (`division`/`section`/`grade` tables) → bulk-assign employees → `emp_config` audit row (`type`='DIVISION'/'SECTION'/'GRADE') + denormalized `emp_proff` column (`emp_vertical`/`emp_sep_priv`/`emp_grade`). Added as 3 more sections to the existing checkbox-list UI and `TYPE_CONFIG` map (`rizo/src/app/api/employees/bulk-policies/route.ts`, `rizo/src/app/(dashboard)/employees/bulk-policies/page.tsx`), plus lookup routes `GET /api/setup/divisions`, `GET /api/setup/sections` (grades lookup already existed).
- **Employee Hierarchy** (reporting-manager tree) and **Leave Hierarchy** (leave-approver pairing): a genuinely different UI — a 3-panel mover (pick a manager/approver → non-allocated pool → currently-assigned list, with drag-free up/down reordering for Hierarchy only). New component `rizo/src/components/employees/HierarchyMover.tsx` and API `rizo/src/app/api/employees/hierarchy/route.ts` (GET available/assigned lists, POST add/remove/reorder against `emp_config` type `HIERARCHY`/`LAPPR`). Leave Hierarchy's "available" list excludes direct 1-level cycles (can't make someone your approver if you're already theirs).
- **Real bug found and fixed during verification**: an existing DB trigger (`emp_config_au`, fires `AFTER UPDATE` on any `emp_config` row) unconditionally NULLs the corresponding `emp_proff` denormalized column for that type — this is legacy's own mechanism for clearing the value on removal, but it fires on *any* update to the row, not just soft-deletes. My original `reorder` action updated `emp_config.hirc_leval` on every drag, which silently wiped `emp_proff.attr1` (the manager assignment) for every reordered employee, including pre-existing real assignments. Caught this by watching a real employee's `attr1` unexpectedly go from `'120'` to `NULL` mid-verification. Fixed by re-applying `emp_proff.attr1 = parentId` immediately after each `hirc_leval` update in the reorder handler, with a comment documenting the trigger's non-obvious behavior for future readers.
- Curl-verified end-to-end as admin against real data: DIVISION/SECTION/GRADE bulk-assign (via temporary test `division`/`section` rows, since those tables had no seed data — confirmed empty tables, not a bug), Employee Hierarchy add/reorder/remove (confirmed a real pre-existing assignment for employee 104 under manager 120 was preserved through reorder after the fix, having caught and reverted the corruption from the pre-fix version), Leave Hierarchy add/remove (confirmed cycle-prevention exclusion). All test data and the temporary `division`/`section` rows removed afterward; the real employee's `emp_proff`/`emp_config` state was restored exactly. `npm run build`/`tsc --noEmit` clean.

### 2.28 Bulk Policy Allocation — Salary Structure tab rebuilt to legacy parity (2026-09-03)
A screenshot comparison flagged that the Salary Structure tab was the one tab still using the generic checkbox `PolicySection` (all-active-employees list, assign-only, no un-assign, no eligibility filter), where legacy is a **3-panel allocate/de-allocate workflow** (structure picker → Non-Allocated Employees → Employees in selected salary structure). Full plan + legacy behaviour dissection in `legacy/Bulk_Policy_Salary_Structure_Migration_Plan.md`.
- **Phase 0 verification** against `mypayrol_mpm121`: confirmed triggers `emp_config_bi` (INSERT → `emp_proff.structure_id = policy_id`) and `emp_config_au` (UPDATE → `structure_id = NULL`) both exist and handle `type='SALARY'` — so the app **must not** write `emp_proff.structure_id` for SALARY; INSERT allocates, `UPDATE ... status=0` de-allocates. Confirmed `salary_structure_limit_prc(IN int, IN varchar(30), OUT varchar(500))` and `sal_structure_distribution_fn(varchar(30),int,int,varchar(30))→int` signatures; `structure_eg_amt`/`structure_active` are `int`; GRTL is not in the special-companies list (so the `ctc_upload_type` 2→1 reset applies).
- **New route** `rizo/src/app/api/employees/bulk-policies/salary/route.ts`:
  - `GET ?structureId=` → `{ candidates, allocated }`. Candidates = active, `emp_proff.structure_id` null/0, **monthly CTC ≥ `structure_eg_amt`** (`DAILY WAGES` → annual figure as-is, else `/12` — legacy special-cases only DAILY WAGES). Allocated = `emp_config` `type='SALARY' AND policy_id=? AND status=1`. Both return Name/Branch/Designation/Gross.
  - `POST {action:'assign'|'remove', structureId, empFkey}` — **one employee per call** (per explicit decision; UI is single-select, no checkboxes). `assign` ports `addEmpToSallary` in a transaction: `ctc_upload_type` 2→1 reset (non-special companies) → `sal_structure_distribution_fn` → recompute each formula head's `structure_det_value` from its `remarks` string via `evaluateArithmetic()` from `salaryFormula.ts` (ESI ceil-if-deduction / round otherwise, Deduction sign flip — **decision: follow legacy**) → `INSERT emp_config` (trigger sets `emp_proff.structure_id`) → `CALL salary_structure_limit_prc` (errors swallowed, matching legacy). `remove` ports `removeEmpFromSallary`: `UPDATE emp_config SET status=0` (trigger nulls `emp_proff.structure_id`) → end-date all open `emp_salary_structure` rows for the employee.
  - **`remarks` recompute divergence, deliberate**: legacy's inline block runs PHP `eval()` on the `remarks` string, which in this tenant is fully numeric for all 241 live rows (verified) so the port matches exactly. For rows where `structure_formula` has drifted back to token syntax (`monthsal`, `<pkey>_<Item>` — e.g. structure 25), legacy's `eval()` would hit an invalid-identifier parse error and silently reuse the previous loop iteration's `$salary_amount` (a latent legacy bug); the port instead catches `FormulaError`, skips that row (keeping the value `sal_structure_distribution_fn` already computed), and returns a `formulaWarnings[]` note. Strictly safer, same intent.
- **Generic route** `rizo/src/app/api/employees/bulk-policies/route.ts`: the `type==='SALARY'` branch now returns a 400 pointing at `/salary`; the other 7 policy types are unchanged.
- **New component** `rizo/src/components/employees/SalaryStructureAllocator.tsx` (modelled on `HierarchyMover`): structure `<select>` labelled `` `${name} - ${eg_amt}` `` from `/api/setup/salary-structures?full=1` (active only), two single-select employee tables with per-panel filters, `window.confirm()` dialogs carrying legacy's exact wording before assign/remove. Wired into `page.tsx` for `activeTab==='SALARY'` in place of `PolicySection`; `SALARY` removed from the `SECTIONS` array.
- **NOT ported**: `checkEmpSalaryStructure` (dead in legacy — the view JS discards its response); multi-employee batch (single-select by decision); the `LNWY`/`GAAR` gross-hide + `GLET`/`ABSG` group-2 branch scope (both unreachable behind this route's `userGroup===1` gate — the API still returns `gross: null` support if a future caller needs it).
- **Verification**: `tsc --noEmit` + `eslint` clean. Both SQL sequences (assign and remove) run end-to-end against live `mypayrol_mpm121` inside a transaction with `ROLLBACK` — confirmed `sal_structure_distribution_fn` returns 1, `emp_salary_structure` rows regenerate, `emp_proff.structure_id` flips via trigger on both INSERT and `status=0` UPDATE, `emp_ctc_transaction.ctc_upload_type`/`emp_derived_anualctc` update, `emp_config` audit row lands, and rollback fully reverts all of it (the `CALL` proc does not force a commit). `evaluateArithmetic()` spot-checked against real tenant `remarks` values. Authenticated HTTP curl pass was **not** run — setting a temp password on the admin row was blocked by the environment's safety classifier; the route is a thin auth wrapper over the verified SQL, identical in shape to the in-production `/api/employees/hierarchy` route.

## Critical fix (2026-07-07): Setup + Employee routes were using wrong table/column names

Discovered while starting Employee module gap-closure work: `branches`, `departments`→`department`, `designations`, `grades`→`grade`, and `/api/employees` were all built against invented/simplified table and column names that don't match the real `mypayrol_mpm121` schema. Every one of these routes was silently 500ing (or, for `/api/setup/branches`, returning nothing) against real data. Root cause: never curl-verified against a populated DB after the original build (unlike the later Phase 2 cleanup features, which were).

Fixed:
- `departments` → `department` (real table name), `grades` → `grade`; PK columns corrected from invented `*_pkey` names to the real `id` (branches/departments/designations use `id`; grades' `grade_pkey` was actually already correct)
- `branches`: added required NOT NULL columns (`city`, `state`, `pincode`, `latitude`/`longitude` defaulted to 0) that the original INSERT omitted
- `/api/employees`: `emp_proff` stores branch/department/designation/grade as **varchar codes** (`emp_branch`, `emp_dept`, `designation`, `emp_grade`), not integer FKs — rewrote all joins/writes to match (`emp_branch = branches.branch_code`, etc.). Also fixed `e.mobile`/`e.personal_email` → real columns `mobile_no`/`email`
- Separately found and fixed a mysql2 `.execute()` quirk (`LIMIT ? OFFSET ?` placeholders throw `ER_WRONG_ARGUMENTS`) by inlining the already-sanitized numeric values into the SQL string

Verified end-to-end via curl (as both admin and employee sessions) against real `GRTL` data: list/search/branch-filter, create, detail view, update, and the 4 setup CRUD screens (create/update/soft-delete) all confirmed working with real returned data (159 real employees, real branch/dept/designation names resolving correctly).

**Lesson carried forward:** always curl-verify new routes against the populated dev DB, not just `npm run build` — TypeScript passing says nothing about whether table/column names are real.

### 2.11 Priority 4 gap-closure (2026-07-11): Document Upload + Employee Income Tax Declarations

Researched the real legacy controllers before building (background Explore agent + live schema verification), which corrected a wrong assumption from the original plan:

- **Document Upload** is NOT a standalone page in legacy — it's a "Documents" tab embedded directly on the employee profile, and it writes to **`emp_passport_visa`** (keyed by `emp_fkey` directly), not `emp_documents` (which is the Employee Join *staging* table, keyed by `emp_join_fkey`). Onboarding conversion already copies staged `emp_documents` rows into `emp_passport_visa`, so this gap was really "let HR keep managing that same table after onboarding." Built as a new section on `rizo/src/app/(dashboard)/employees/[id]/page.tsx` (reusing the existing `RepeatableRows`/`DocumentUploadField` components from Employee Join), backed by `rizo/src/app/api/employees/[id]/documents/route.ts` (GET/POST) and `[docId]/route.ts` (DELETE, soft `status=0`). `reccuring`/`remind` columns confirmed vestigial in legacy (no cron/reminder logic anywhere) — intentionally not exposed in the form.
- **Employee Income Tax Declarations** — new top-level page `rizo/src/app/(dashboard)/employees/tax-declarations/page.tsx` (`EmployeeSearch` picker, matching legacy's standalone-menu-item shape). Backed by `rizo/src/app/api/employees/[id]/tax-declarations/route.ts` (GET resolves the employee's branch's currently-open `fin_year` — picks the most-recently-started `OPEN`+`is_current_finyear='Y'` row, since real data can have more than one flagged current per branch — then returns `tax_heads`/`tax_heads_details` grouped by Income/Deductions merged with any existing `emp_tax_transactions`; POST upserts a declared value), `lock/route.ts` (admin-only, single-row or bulk lock/unlock, matching legacy's per-`(emp_fkey, tax_heads_fkey)` boolean toggle), and `upload/route.ts` (proof-document upload: MIME allow-list + 2MB cap mirroring legacy's `finfo`-based validation, though ours checks the browser-reported MIME type rather than sniffing real file bytes, since adding a magic-byte-sniffing dependency wasn't warranted for this pass; blocks upload once locked; appends to the comma-joined `file_name`/`file_type` columns like legacy does for multi-file-per-head support).
- Computed-tax preview (`tax_salary_distribution_fn`) intentionally **not** wired up in this pass — out of scope per decision, since it's a payroll-side computation rather than something the declaration/lock/upload workflow itself needs.
- **Bug caught during curl-verification**: `UPDATE emp_tax_transactions SET last_value = ...` threw a MySQL syntax error — `last_value` is a reserved word in MySQL 8 (the `LAST_VALUE()` window function). Fixed by backtick-quoting the column name.
- Curl-verified end-to-end against real data (employee 104, branch GRTL08): document add/list/soft-delete; tax declaration GET correctly resolved FY2026 as the open year; declare → lock → blocked-edit-while-locked (403) → unlock → edit-succeeds; proof upload accepted a real PDF and rejected a `.exe`; all test rows/files cleaned up afterward. `npm run build`/`tsc --noEmit` clean.
- **Generate Employee Documents deferred**, per decision — legacy has a real template/merge-field/Dompdf-PDF pipeline (`DocumentManagerController.php`: `doc_template`/`documents` tables, ~60 merge placeholders) worth porting (we'd use the already-installed `jsPDF` instead of Dompdf), but it's larger and less-defined than the other two and needs its own scoping pass.

### 2.12 Employee menu completion (2026-07-13): Import Employee, Promotion Approval, Generate Employee Documents, Remove Employee

Closed the remaining 4 legacy Employee-menu items (all 12 legacy items now have a Next.js equivalent). Researched each via background Explore agents before building, then verified every schema assumption against the real dev DB rather than trusting the research or the static schema dump.

**Import Employee** (`/employees/import`, `rizo/src/app/api/employees/import/route.ts` + `template/route.ts`) — bulk XLSX upload that writes **directly** to `emp_details`/`emp_proff`/`user_credentials` (not routed through the `emp_join` staging table — that's Employee Join's job). Legacy's real column template comes from a vendor helper class (`EmployeeCSVData.php`) not present in this repo checkout, so the template is our own design (kept legacy's 6 confirmed mandatory columns, added the common optional fields our own Add Employee form supports) rather than a byte-identical port. One branch applies to the whole uploaded file, matching legacy's `uploadandsaveempdetails($emp_branch)`. Uses the stronger PAN/Aadhaar duplicate check already used elsewhere in this app (legacy's own check is weaker: name+DOB only). `user_credentials.user_id`/`password` inserted as `NULL` (not `''`) to avoid colliding on `user_id`'s `UNIQUE` constraint across multiple imported rows. Curl-verified: real branch/designation/department resolution, duplicate-check rejection on re-upload, all three tables populated correctly; test rows cleaned up.

**Promotion Approval** (`/employees/promotions`, `rizo/src/app/api/promotions/route.ts` + `[id]/route.ts`) — ports `PromoController`. A single central approval step (not tiered manager→admin, matching legacy): request → approve (cascades designation/department/branch/employment-type as direct `emp_proff` updates, shift/leave as `emp_config`-audited assignments matching the Bulk Policies convention, a salary-structure change via the real `sal_structure_distribution_fn`, a CTC row, and a reporting-manager change matching the Employee Hierarchy mover's convention) or reject. Confirmed "Approvals (Probation/Promotion)" in legacy is really just Promotion — Probation approval doesn't exist anywhere in legacy (no controller, no table, nothing beyond a plain probation-days field used in payroll date math). **Bug caught during verification**: the salary-structure cascade was ordered before the CTC insert, but `sal_structure_distribution_fn` requires an active CTC to already exist (the same prerequisite documented for Bulk Policies' SALARY type) — fixed by inserting the CTC row first. Curl-verified end-to-end on a real employee (cascaded designation/department/shift/leave/salary-structure/CTC/hierarchy changes, reject flow, already-processed 409 guard); all changes restored to the employee's original state afterward.

**Generate Employee Documents** (`/employees/generate-documents`, `rizo/src/lib/documentMerge.ts`, `rizo/src/app/api/document-templates/*`, `rizo/src/app/api/employees/[id]/generate-document/route.ts`, `rizo/src/app/api/documents/*`) — HTML template/merge-field engine only, per decision (skipped legacy's separate image-composite certificate feature, which calls an external legacy microservice, and its birthday-email feature, which is a different feature area entirely). Templates are legacy's real, already-seeded `doc_template` catalog (74 real rows: offer letters, termination letters, experience certificates, company policies). Merge-field resolution (`buildMergeTokens`) pulls real employee/company/branch data using legacy's exact token spelling where confirmed (including its `empolyee_name` typo) and our own consistent naming for the rest (documented in-app via a placeholder reference panel). PDF output uses the browser's native print dialog (`window.print()` on a generated preview window) rather than adding an HTML-to-PDF rendering dependency. Curl-verified: real production template ("Employee Promotion letter") merged correctly against a real employee's name/designation/department/CTC; save persists to the real `documents` table (confirmed alongside real pre-existing production documents for that employee); template create/edit/delete all confirmed; test rows cleaned up.

**Remove Employee** (`/employees/resignations`, `rizo/src/app/api/resignations/**`) — the full 4-stage legacy workflow: (1) request (`resignation_requests` + a `termination` row created together, matching legacy's `Saverequests()`), (2) HR handover checklist (`resignation_accept`), (3) admin approval, which calls the real `final_settle_pay_prc` stored procedure and surfaces its result, (4) final removal (`emp_details.status=2`, clearing *other* employees' hierarchy references to the now-terminated manager, deactivating their `HIERARCHY` `emp_config` rows, marking outstanding payroll/settlement/leave-encashment rows settled) — plus a withdraw/cancel path. **Real blocker found and flagged before building**: `final_settle_pay_prc`'s actual signature (verified via `SHOW CREATE PROCEDURE` against the dev DB, not the schema dump) is `(branch_code, month_year, emp_pkey, presant_days, encash_days, user_id, OUT error_message)` and depends on `attendance_register` data for that employee/branch/month — infrastructure this port hasn't built yet (Attendance phase not started). Decision: call it anyway and surface `perror_message` to HR (e.g. "Attendance not verified") rather than silently no-op, so the workflow is ready for whenever Attendance/Payroll exist. **Two bugs caught during verification**: (1) a raw `new Date().toISOString()` string passed to a MySQL `datetime` column threw `ER_TRUNCATED_WRONG_VALUE` (needed `T`/`Z` stripped); (2) the approve route tried to update a nonexistent `termination.approved_date` column (confused with `resignation_accept`'s own `approved_date` column of the same name) — fixed by removing it. Curl-verified the full lifecycle end-to-end on a throwaway test employee (created and destroyed for this test, since the final step is a real irreversible termination): request → checklist → approve (confirmed real "Attendance not verified" response) → finalize (confirmed `emp_details.status=2` and all cascaded updates) — plus a separate withdraw/cancel test. All test data removed afterward.

All four: `npm run build`/`tsc --noEmit` clean. This closes out the Employee module gap-closure plan — all 12 legacy Employee-menu items now have a Next.js equivalent (Import Employee and the Promotion half of Approvals were previously paywalled/unbuilt in legacy itself; both are now real, working features here).

### 2.13 Remove Employee refinements (2026-07-14): flat list view + full legacy Reason list

A follow-up design-comparison review of Remove Employee against the live legacy site (`in.mypayrollmaster.online`) found the list layout diverged too far from legacy's single searchable table, and confirmed the Reason dropdown was missing 14 of legacy's 19 real options (no path for termination/dismissal/absconding/death/retirement in the UI).

- **List view** (`rizo/src/app/(dashboard)/employees/resignations/page.tsx`): replaced the 5-tab card layout with a single `DataTable` (same component/pattern as `/employees`) — columns match legacy (Full Name, Branch, Reason, Resignation Submitted, Last Applied Date, Last Approved, Remarks) plus a color-coded Status column, with status now a filter dropdown instead of separate tabs, and a name/ID/reason search box. `GET /api/resignations` (`rizo/src/app/api/resignations/route.ts`) extended with `search` (matches employee name/ID/reason) and an optional (rather than default-required) `status` param, plus a `branches` join for the Branch column.
- **Reason list**: replaced the 5-option voluntary-exit-only dropdown with legacy's real 19-option list, confirmed against `legacy/View/EmployeeResignation/form.ctp` (not the earlier gap report's text, which had garbled the 4 "Cessation (Short Service)" sub-options into unreadable fragments — verified against the actual `.ctp` source instead of trusting it).
- **Bug found and fixed**: `resignation_requests.Reason` was `varchar(20)` in the real dev DB — under `STRICT_TRANS_TABLES` (confirmed via `@@sql_mode`), inserting most of the real legacy reason strings (e.g. "Death Away From Service", any of the 4 "Cessation (Short Service)..." options, all >20 chars) would hard-fail with `ER_DATA_TOO_LONG`. Legacy's own `termination.Reason` column (which receives the identical value in the same request) is `varchar(100)`, so `resignation_requests.Reason` was too narrow for its own UI's option list — a genuine schema-drift bug, not a new-field decision. Fixed via `ALTER TABLE resignation_requests MODIFY Reason varchar(100) NOT NULL` to match.
- Curl-verified end-to-end as admin (temporary password override on the real `GRTLADMIN01` control-DB row, restored immediately after obtaining a session): flat-list fetch against 3 real resignation records (branch names resolved, no crash on legacy rows with no matching `termination` row), search and status-filter params both behave correctly, and a real submission using the longest new Reason option ("Cessation (Short Service) - The Employee Ill", 44 chars) round-tripped correctly post-widen. Test resignation/termination rows deleted afterward.
- `npm run build`/`tsc --noEmit` clean.
- **Follow-up bug**: the settlement button ("Approve & Settle") only rendered at the `HR Reviewed` stage, but nothing in the backend actually requires the Checklist step first, and all real data in this tenant sits at `Applied` — so the button was invisible for every real record, reported by the user as "I don't see a button." Fixed: renamed to **"Process Full & Final"** (matching legacy's label) and made it visible at both `Applied` and `HR Reviewed`, matching legacy's un-gated toolbar-style access rather than our own invented stage requirement. Curl-verified calling it directly from `Applied` (skipping Checklist) succeeds and returns the expected "Attendance not verified" message; test rows cleaned up.

### 2.14 Remove Employee deep build (2026-07-14): real Full & Final settlement engine

Prior passes only called `final_settle_pay_prc` and showed its bare return message — HR never saw what the procedure actually computed. Three background research agents fully mapped legacy's real "Process Full & Final" flow (controllers, `.ctp` view files, live-DB procedures/functions/triggers/schemas), scoped down to the GRTL/non-KWMT path (the `KWMT`/`DEMO`/`GLET`-only editable per-month tables and "metro" formal-memo printables are out of scope for this single-tenant deployment). Full plan recorded in the session's plan file before building.

- **`GET /api/resignations/[id]/eligibility`** (new) — ports legacy `setup()`'s hard-blocking gates: approved last-working-date not in the future, not already processed (`emp_settle_slip` ⨝ `emp_details.status=2`), shift + salary structure both assigned. The two attendance-window checks are ported with the same SQL shape legacy uses, but since `emp_detail_timeattandance` is empty until the Attendance phase exists, they resolve to "nothing to check yet" rather than blocking — the honest behavior of running legacy's own logic against currently-empty tables, confirmed deliberately rather than approximated. Per an explicit decision, this is a **hard block** (not just a warning) on all checks including attendance — meaning full settlement processing stays gated until Attendance is built and real attendance data exists for the resignation window.
- **Notice period auto-calc** on the New Resignation form — fetches the selected employee's `emp_proff.notice_days` and auto-suggests `Last Working Day = today + (notice_days − 1) days` (matches legacy's `asper_notice()` JS formula), always editable/overridable. **Bug found**: `POST /api/resignations` was hardcoding `termination.notice_period` to a literal `0` regardless of the employee's actual notice days — fixed to read `emp_proff.notice_days` at submission time.
- **`PUT /api/resignations/[id]/approve`** extended to close the central gap — legacy's real flow: (1) resolves each eligible leave head (`leavepolicy.is_leave_encash='Y'`) via `leave_balance_inthe_year_fn`, replaces any stale `leave_encashment_master` row, and calls `leave_encash_prc` to post the encashment (leave_encash_prc's 5th param is declared `IN` not `OUT` in the live DB — a legacy bug, not ported; we just call it and let a thrown SQL error surface via rollback); (2) calls `final_settle_pay_prc` as before; (3) **reads back** the resulting `emp_settle_slip` rows and returns a real Additions/Deductions/Net Salary breakdown instead of a bare message; (4) computes resignation-period day-count stats (`weekoff_days_count_fn` + a `holidays` count) and a display-only Notice Pay shortfall figure (`(annual_ctc/12)/notice_days` per-day rate × shortfall days), matching legacy's GRTL formula exactly — not persisted as its own settlement line, per an explicit decision; (5) surfaces read-only Loans (`emp_loan`/`emp_loan_info`) and damaged-Assets (`asset_allocate`) reference panels, with no auto-netting (matches legacy precedent — HR manually accounts for these).
- **Frontend rebuild** (`rizo/src/app/(dashboard)/employees/resignations/page.tsx`): the "Process Full & Final" button now calls the eligibility endpoint first, showing blockers in a modal if any and only opening the settlement modal if clear; the settlement-result modal now renders the full breakdown (day-count stat tiles, Additions/Deductions columns, Net Salary, Notice Pay, Loans/Assets reference lists) instead of a plain message.
- **`finalize/route.ts`** fix: its `emp_settle_slip` lock (`approved='Y'`) was missing legacy's `AND status = 'Y'` filter, meaning it would have flipped `approved` on cleared/reset rows too, not just active ones — fixed to match legacy's `removeemps()` exactly.
- **Real bug caught during verification**: mysql2 returns `DATE` columns as JS `Date` objects (no `dateStrings` config on the pool) — `String(dateObj).slice(0,10)` silently produces a garbled locale string, not an ISO date, corrupting every date comparison in both new routes. Caught because the eligibility gate blocked with a nonsensically-shifted date on a manual test. Fixed with a `toISODate()` helper (`instanceof Date ? .toISOString() : String(...)`) in both `eligibility/route.ts` and `approve/route.ts`.
- **Curl-verified end-to-end** on a disposable cloned test employee (emp_pkey 253, cloned from a real active employee's `emp_proff` config then fully deleted afterward, including the FK-cascaded `emp_ctc_transaction`/`emp_salary_structure` rows the CTC-insert trigger creates): eligibility correctly blocks on a future approved-date and correctly passes once backdated; notice period correctly propagates from `emp_proff.notice_days` into the new resignation; `leave_encash_prc` executes cleanly and posts a real `leave_encashment_master` row when the employee's leave policy group has encashable leave configured; `final_settle_pay_prc` continues to report "Attendance not verified" as expected; the settlement breakdown, Notice Pay, and day-count math all computed correctly; Finalize correctly flips `emp_details.status=2` and `Resignation_status='Completed'`. All test rows (across 7 tables) deleted afterward.
- `npm run build`/`tsc --noEmit` clean.
- **Explicitly out of scope for this pass** (per decisions made before building): employee self-service resignation submission (legacy's `ResignationRequestController` letter/agree flow) stays admin-only; Notice Pay stays display-only rather than persisted as an audit-trail line item.

### 2.15 Remove Employee refinement (2026-07-14): two-step preview + missing Encashable Leave Balance

A real production screenshot of legacy's live Remove Employee screen (`company_code = 'GRAT'`, a genuine non-KWMT/DEMO tenant, not a hypothetical) surfaced two concrete gaps against §2.14's build:

1. **Encashable Leave Balance was computed but never shown.** `computeLeaveEncashment` already calculated this internally but the API response and modal only surfaced 3 of legacy's 4 "Notice Period Adjustments" stat tiles.
2. **Legacy is a two-step flow, ours was one-click.** In legacy, clicking "Process Full & Final" in the list loads a preview screen (Resignation Details + Notice Period Adjustments stats + Loans/Assets) **without** calling `final_settle_pay_prc` yet — HR reviews the numbers, then a separate button inside that screen actually commits. Our single button did eligibility + leave-encashment + `final_settle_pay_prc` + breakdown all in one call.

Fixed both:
- **`rizo/src/lib/settlement.ts`** (new, shared) — factored the settlement logic into reusable pieces: `getTerminationContext` (the joined resignation/termination/proff/CTC query), `computeDayCountStats` (`weekoff_days_count_fn` + holidays, parameterized by `presantDays` so it works for both a 0-value preview and an admin-supplied commit), `computeNoticePay`, `previewEncashableLeaveBalance` (read-only sum, no writes — for the preview step), `commitLeaveEncashment` (the real write-and-call-procedure logic — for the commit step, now also returns the total days committed), and `getLoansAndAssets`.
- **`GET /api/resignations/[id]/preview`** (new) — the read-only first step: Resignation Details (submitted date, notice period, last working date, approved last working date), Notice Period Adjustment stats including Encashable Leave Balance, and Loans/Assets reference panels — computed with zero writes, mirroring legacy's `setup()` screen exactly.
- **`PUT /api/resignations/[id]/approve`** — refactored onto the shared lib (no behavior change to the commit itself), now also returns `encashableLeaveBalance` in the final result.
- **Frontend**: clicking "Process Full & Final" now runs eligibility → (if clear) fetches the preview and opens a modal showing Resignation Details, Notice Period Adjustments (now all 4 tiles), and Loans/Assets **before** the Settlement Month/Present Days/Leave Encashment Days inputs and the actual commit button — matching legacy's two-step structure. The post-commit settlement-result modal also gained the 4th (Encashable Leave Balance) tile.
- **Curl-verified end-to-end** on a second disposable cloned test employee (emp_pkey 254, deliberately given a leave-policy group with real encashable leave configured this time, to exercise the nonzero case) — confirmed the preview endpoint returns `encashableLeaveBalance: 12` with zero DB writes, and the subsequent commit step returns the identical `12` after actually persisting the `leave_encashment_master` row — i.e., preview and commit agree, which is the whole point of splitting them. Finalize confirmed as before. All test rows (8 tables) deleted afterward.
- `npm run build`/`tsc --noEmit` clean.

### 2.16 Remove Employee: Edit + View Slip (2026-07-14)

A dedicated deep-dive comparison report (Remove Employee vs. legacy, field-by-field across every form/button/process step) found two real remaining gaps once its stale headline claim was corrected (it had checked the All Employees Deactivate/Activate toggle rather than noticing `/employees/resignations`, which by this point already covered most of legacy's workflow): **Edit** an active resignation record, and **View Slip** (a printable Full & Final settlement document). Legacy's row-state rule — Edit/Delete lock once the employee is actually terminated — was confirmed to hinge on `emp_details.status = 2`, the same signal our own eligibility/settlement code already uses, not on any of the intermediate resignation-workflow stages.

- **`PUT /api/resignations/[id]`** (new) — updates `resignation_requests` (Reason/Reason_Desc/Comments_to_manager/Last_workingday/contact_no/authorised_to) and the matching `termination` row (Reason/Reason_desc/last_working_date/last_approved_working_date/act_last_working_day, plus re-deriving `notice_period` from `emp_proff.notice_days` in case it changed) together. Blocked with 409 once `Resignation_status = 'Completed'` — matches legacy's real gate exactly (tied to termination status, not to HR-Reviewed/Approved, both of which stay editable). The employee itself is not editable in edit mode (locked, matching legacy's own pre-filled/read-only "Select Employee" in its edit modal).
- **Frontend**: the New Resignation modal now doubles as an Edit modal (`editingId` state) — employee shown read-only when editing, submit button reads "Save Changes" instead of "Submit" (deliberately *not* carrying forward legacy's own flagged UX quirk of the button always saying "Submit Termination" even in edit mode), a new pencil-icon Edit action appears on any non-`Completed` row.
- **`GET /api/resignations/[id]/slip`** + **`/employees/resignations/[id]/slip`** (new) — a standalone printable Full & Final Settlement Statement, deliberately built as its own page outside the `(dashboard)` route group (no Sidebar/Header) rather than routed through the existing Generate-Employee-Documents template engine, per an explicit scoping decision — opens in a new tab via a `target="_blank"` link (mirroring legacy's own separate-window behavior), with a `window.print()` button. Reads the already-persisted `emp_settle_slip` rows from the last Process Full & Final run (read-only, no recompute) — available once a resignation reaches `Approved` or `Completed`, matching legacy's "View Slip enabled once already processed" rule.
- Both new endpoints/pages reuse the `rizo/src/lib/settlement.ts` helpers introduced in §2.15 (`getTerminationContext`, `computeDayCountStats`, `computeNoticePay`) rather than duplicating the settlement math a third time.
- **Curl-verified end-to-end** on a fresh disposable test employee: Edit updates both tables correctly; Edit correctly 409s once the record is `Completed`; View Slip returns a correct read-only breakdown once `Approved`, remains accessible after `Completed`; slip page itself renders (HTTP 200). All test rows across 8 tables deleted afterward.
- **Incidental fix**: a stale `.next` dev cache produced a spurious `LayoutRoutes` typed-route TypeScript error after adding the new print route group — resolved by clearing `.next` and restarting the dev server (not a real code issue).
- `npm run build`/`tsc --noEmit` clean.

### 2.17 Remove Employee: fix crash on orphaned real records (2026-07-14)

User-reported crash: clicking "Process Full & Final" on a real resignation record threw `Cannot read properties of undefined (reading 'map')` in `resignations/page.tsx`, crashing the whole page. Root cause: **all 3 real `resignation_requests` rows in this tenant** (Anas, Asna Mol T A, Gawtham — pre-existing data, created before this app's `POST /api/resignations`, which always creates a companion `termination` row) **have no matching `termination` row at all**. The eligibility endpoint returned a bare 404 `{error}` shape for these, which doesn't match the `{ok, blockers}` shape the frontend expects — `setBlockers(undefined)` then crashed on `.map()`.

- **`GET /api/resignations/[id]/eligibility`**: now returns `{ok: false, blockers: [...]}` instead of a 404 when no `termination` row exists, with an actionable message ("Use Edit to re-save it and create one").
- **`PUT /api/resignations/[id]`** (Edit): now upserts — if no `termination` row exists yet, it creates one (using the resignation's original `applied_date` as `submitted_date`, a historically-accurate backfill, not "today") instead of silently no-op'ing an `UPDATE` against zero rows. This makes the eligibility endpoint's advice actually true.
- **Frontend crash-proofing** (defense in depth, in case any endpoint ever returns an unexpected shape again): `checkEligibility`'s mutation now checks `res.ok` explicitly and coerces `blockers` to always be a real array before calling `setBlockers`; the modal's render guard changed from `!== null` to `!= null` (the actual bug — `undefined !== null` is `true` in JS, so a `null`-only check doesn't catch `undefined`).
- **Repaired one real record live**: re-submitted Gawtham's (Resignation_pkey 6) unchanged values through the fixed Edit endpoint, which created the missing `termination` row (confirmed eligibility now passes cleanly). The other 2 orphaned real records (Anas, Asna Mol T A) were deliberately left as-is — the fix is now self-serviceable via Edit in the UI, not something to unilaterally apply to more real employee records than needed for verification.
- `npm run build`/`tsc --noEmit` clean.

### 2.18 Leave Policy setup screen (2026-07-30)

User reported `/setup/leave-policy` 404ing — the Sidebar had always linked there (`Company Setup > Leave Policy`), but no page was ever built; only a bare GET lookup (`/api/setup/leavepolicy-groups`, used by Bulk Policy Allocation) existed. Researched the real legacy screen before building: `legacy/Controller/LeavePolicyController.php` (routed via the `hrm_menu` "Leave Policy" entry — confirmed live, not one of this codebase's usual dead-controller near-duplicates) plus `View/LeavePolicy/{index,groupform,policyform}.ctp`. GRTL's plan is `standard` (not `basic`) and GRTL is not in the controller's `$restricted_companies` list (a ~10-tenant list with a different form variant), so GRTL takes the plain, non-restricted `policyform.ctp` path with the Sanction-By/Exceptions sections visible but Level-of-Approval/Notified-By hidden (those are gated to `gede`/`demo`/`mbct` only in legacy, not GRTL).

Confirmed via live schema (`mypayrol_mpm121`) two linked tables: `leavepolicy_group` (thin — just a name) and `leavepolicy` (~25 columns — one row per leave type within a group: allotted days/year or /month depending on duration type, carry-forward limit, sanction-by employee, encashment + limit, exceptions block with min/max leave/min service/advance-notice-days/allow-all-leaves, sandwich/negative-balance/document-mandatory flags). A separate legacy screen (`DbConfig::leave_heads`) toggles which leave *types* (`salary_head_items.item_type='LEAVE'`, `value` Y/N) exist for the company at all — a real prerequisite gap on its own, since the policy form's Leave Type dropdown only offers types where `value='Y'`. Scoped via `AskUserQuestion` to build all 3 pieces (types toggle + group CRUD + policy CRUD), per the user's choice of "Full build."

Built:
- **Leave Types** (`/setup/leave-types`, `GET /api/setup/leave-types`, `PUT /api/setup/leave-types/[id]`) — simple enable/disable checkbox list over `salary_head_items` where `item_type='LEAVE'`, matching legacy's `save_leave_head()` toggle exactly (`value='Y'/'N'`).
- **Leave Policy Groups** — extended the existing GET-only `/api/setup/leavepolicy-groups` with `POST` (duplicate-name guard, matching legacy's `checkleavepolicyexists`) and new `[id]/route.ts` for `PUT`/`DELETE` (soft `status=0` + clears `emp_proff.LEAVEPOLICY_GROUP_ID`, blocked with 409 if any active employee is still assigned — same pattern as Holiday Groups' delete guard). Rendered via the existing generic `SetupCrudPage` component (same as Holiday Groups).
- **Leave Policies** (per-leave-type rows within a group) — `GET/POST /api/setup/leave-policies` (list by `groupId`, create with a duplicate-leave-type-in-group guard), `PUT/DELETE /api/setup/leave-policies/[id]` (soft delete), `GET /api/setup/leave-policies/available-types` (excludes leave types already configured in the group, mirroring legacy's `form()` exclusion subquery, with an `excludeId` param so editing a row doesn't exclude itself). Shared field-normalization logic factored into `rizo/src/lib/leavePolicies.ts` (`normalizeLeavePolicyBody`) since Next.js route files may only export HTTP-method handlers — a plain helper export from a route file breaks the route contract.
- **`/setup/leave-policy` page** — Leave Policy Groups CRUD (via `SetupCrudPage`) on top, then a group picker + a custom policy table/modal below (too many conditionally-relevant fields — ~20 — for the generic `SetupCrudPage` modal, which has no scroll handling or field grouping; built bespoke instead, matching this project's precedent from Shifts' custom form). Sanction-By uses a plain employee `<select>` (reusing the existing `/api/reports/employee-options` lookup) rather than porting legacy's autocomplete widget — a deliberate simplification, consistent with how the Employee Hierarchy mover already chose selects over legacy's autocomplete elsewhere in this codebase.
- **Deliberate simplifications, documented not hidden**: legacy's form dynamically shows/hides ~8 field groups via JS depending on the selected Duration Type (e.g. Carry Forward hidden for "Present Days," Sanction-By/exceptions gated by plan/company) and auto-computes Start/End Date defaults per duration type (attendance-cycle dates, quarter/half-year boundaries, yearly rolling window) via `att_start_end_fn` and client JS. This port shows all fields simultaneously (values save correctly regardless of duration type) and leaves Start/End Date as plain manual date pickers rather than replicating the auto-fill JS — the underlying data model and validation (duplicate leave-type-per-group, duplicate group name, delete-blocked-while-assigned) all match legacy exactly; only the form's dynamic show/hide and date auto-suggestion behavior were traded for a simpler fixed layout.
- Level of Approval / Notified By intentionally not built (legacy-gated to `gede`/`demo`/`mbct`, none of which is GRTL).
- Curl-verified end-to-end against real GRTL data (temporary password override on `GRTLADMIN01`, restored and confirmed immediately after): fetched real Leave Policy Groups (30+ real groups) and a real group's real policies (GREATLEAP Leave Policy's 3 configured leave types — Casual/Compensatory Off/Sick Leave — with real allotments); Leave Types toggle flipped and restored a real row; created a test group (duplicate-name correctly 409s), created a policy for it (duplicate-leave-type-in-group correctly 409s, available-types correctly excluded it once created), updated it (fields round-tripped correctly), deleted it, deleted the test group; confirmed the real in-use GREATLEAP group correctly 409s on delete ("remove employees under this Policy"). Both new pages confirmed loading (200) under a real authenticated session. All test rows hard-deleted afterward, admin password restored and confirmed via `SELECT`, scratch cookie files deleted. `npx tsc --noEmit` clean.

### 2.19 Company Setup QA-report fixes (2026-07-30): delete visibility, branch code, shift name, Devices

User supplied a QA report (5 findings) comparing the Company Setup menu against legacy. Verified each against real legacy source/schema before fixing rather than trusting the report's framing at face value:

- **D-01 (Critical) — Delete appeared to silently no-op across Company Setup.** Root cause found: `DELETE` was actually working correctly everywhere (soft `status=0`, matching legacy's own `updateAll(status=0)` convention, confirmed against `BranchController::deleteBranches()`/`DepartmentController`/`DesignationController`/`GradeController`) — but the `GET` list queries for **Branches, Departments, Designations, Grades** never had a `WHERE status = 1` filter, so soft-deleted rows kept reappearing in the list. Confirmed legacy's own list queries do filter `status = 1` in all 4 controllers. Fixed by adding the missing filter to all 4 `GET` routes. (Banks, Contacts, Financial Year, Holiday Groups, Salary Structure already filtered correctly — not part of this bug. Salary Head Categories deliberately shows inactive rows too, per an earlier documented decision, with `status` as a visible column — not affected.)
- **D-02 (High) — Branches: user-typed Branch Code silently overwritten.** Found the real cause: a live `BEFORE INSERT` trigger on `branches` (`branches_bi`) unconditionally overwrites `NEW.branch_code` with a system-generated `<company_code><sequence>` value — confirmed this matches legacy's own `BranchController::savebranch()`, which *also* never lets the create form set `branch_code` (auto-generates `substr(branch_name,0,3).time()` server-side) and never touches it again on edit. Our port had added an editable "Branch Code" text field that looked functional but was always discarded — a misleading UI, not a data bug. Fixed: removed the field from the create/edit form entirely (matches legacy, which never exposes it), removed `branch_code` from the `UPDATE` statement (immutable after creation, matching legacy), left it visible as a read-only list column.
- **D-03 (High) — Shifts: blank Shift Name accepted.** The label showed `Shift Name *` but the input never had a real `required` attribute, and the API didn't validate it either — a shift could be saved with an empty name. Fixed both: added `required` to the frontend field (`ShiftForm.tsx`) and a 400 guard on blank `day_time_desc` in both `POST /api/setup/shifts` and `PUT /api/setup/shifts/[id]`.
- **D-07 (High) — Leave Policy 404.** Already fixed earlier this same session (§2.18) — this finding was stale relative to when the QA report was generated. Reverified still 200.
- **D-08 (High) — Devices 404, and a stale prior conclusion corrected.** An earlier PROGRESS.md note (Phase 3) claimed no real legacy device-config screen existed (conflating `AttendanceSetupController`, a plan-gate screen, with device management). A focused re-investigation found this was wrong: `legacy/Controller/DeviceController.php` is a real, live-routed screen (confirmed via the `hrm_menu` table) for managing biometric/attendance punch devices — and its `devices` table lives in **`mypayrol_control_db`** (the control DB), not the per-company DB, joined to `mypayrol_control_db.company_branches` for branch names. Built `/setup/devices` (`GET/POST /api/setup/devices`, `PUT /api/setup/devices/[id]`, both querying `controlPool` directly rather than a per-company pool) — Device Name/Branch/Serial Number/Location fields, `DeviceId` auto-assigned as `MAX(DeviceId)+1` scoped by `company_code` (matching legacy's `saveDev()` exactly, not user-entered). No delete button: legacy's `DeviceController` has no delete/remove action at all, so none was added here either. Caught and fixed one bug during verification: the `devices` table's `DeviceSName` column is `NOT NULL` with no default — the first INSERT attempt threw `ER_NO_DEFAULT_FOR_FIELD`; fixed by explicitly inserting `''` (legacy's own `saveDev()` never sets this column either, relying on it already existing from a prior row — a real schema wrinkle, not a legacy behavior to replicate).
- **Curl-verified all 5** against real GRTL data (temporary password override on `GRTLADMIN01`, restored and confirmed immediately after): created+deleted a test Department, confirmed it now actually disappears from the list; created a test Branch, confirmed the DB trigger generated a real `GRTL09` code regardless of what was (not) sent; confirmed blank Shift Name is rejected with 400; confirmed `/setup/leave-policy` still 200; created, listed, and edited a test Device end-to-end, confirmed `/setup/devices` loads (200). Noted (not a bug): the dev DB's `mypayrol_control_db.company_branches` mirror only has 1 of GRTL's real branches populated (a pre-existing data-sync gap in this dev environment, unrelated to this fix) — the Devices list already falls back to showing the raw branch code when the name can't be resolved, so this doesn't break the UI. All test rows deleted afterward, password restored and confirmed via `SELECT`. `npx tsc --noEmit` clean throughout.

### 2.20 Company Setup Gap Report fixes (2026-07-30): compliance, statutory heads, notice period/division/section, employee devices, expense module

User supplied a formal "Company Setup Gap Report" (9 critical, 2 moderate, 3 structural gaps). Verified every claim against real legacy source and the live dev DB before building — one claim was a false positive (already built under a different Sidebar group), one initial research pass targeted the wrong table (self-caught before writing code, see below). User chose "Everything, P0 first" via AskUserQuestion. All 6 real gaps built:

- **GAP-1.3 — Company Profile missing compliance fields.** Legacy `comp_contact_info` also carries `business_nature`/`business_type`/`logo`, never wired into the port. Extended `PUT /api/company` to save all three; added a `FileUploadField` for logo and the two business-classification fields to `/setup/company`. Built a new `compliance` table CRUD (`GET/PUT /api/setup/compliance`, single-row pattern with INSERT fallback) for CIN/PAN/TAN/Service Tax/PF/ESI/PT (company + director + employee) — a full second "Statutory & Compliance" form section on the same page.
- **GAP-1.4 — Statutory Head Mapping never built.** Initial research pass wrongly targeted `emp_statutory_components` (a per-employee, payroll-computed log table, not an admin-configurable master) — self-caught before any code was written; the real target is `tax_salary_components` (17 fixed seeded rows, mapped via `TaxsalarycomponentsController.php`) joined to `salary_head_items`. Built `GET/PUT /api/setup/statutory-heads` (bulk update via `{updates:[{pkey, salary_head_item_Fkey, upper_limit}]}`, 409 on duplicate salary-head-item mapping) and a bulk-editable `/setup/statutory-heads` table page.
- **GAP-1.6 — Employee Expense module never built.** Ports `EmployeeExpensesController` (`emp_expense` table). Admin-only, matching the established precedent for every other request/approve flow in this port (no separate employee self-service login). Built the `expense_type` master CRUD, and the full claim workflow (create → authorize → approve, or reject, or remove-while-`Applied`, blocked-remove-once-`Approved`) reusing `getAuthorizerApprover()` from `src/lib/leave.ts`. Two real schema bugs caught and fixed during curl verification (not by reading the schema up front): (1) `expense_type.expense_head_fkey` is `NOT NULL` with no default and the live `ExpenseTypesController.php`/`expense.ctp` form never submits it at all — legacy MySQL's non-strict mode silently defaults it to 0, but this port's stricter DB throws; fixed by explicitly inserting `0`. (2) `emp_expense` has 7 more `NOT NULL`-no-default columns (`authorized_date`, `remarks_auth`, `approved_date`, `remarks_approved`, `modified_by`, `modified_date`, `image`) that the initial INSERT omitted, causing a 500 on every claim creation; fixed by supplying placeholder values for all of them.
- **GAP-1.7 — Notice Period has no admin CRUD screen.** Built `GET/POST /api/setup/notice-periods` (duplicate-days and duplicate-description guards) + `PUT/DELETE [id]` + `/setup/notice-periods` (`SetupCrudPage`).
- **GAP-1.8 — Division/Section have no admin CRUD screen.** Built `GET/POST /api/setup/divisions` and `/sections` (duplicate code/name guards) + `PUT/DELETE [id]` for both + `/setup/divisions` and `/setup/sections` pages. (The two frontend pages were initially missed in the same coding pass as the APIs — caught and built during this verification session, before they ever shipped un-verified.)
- **GAP-1.9 — Employee Devices (device↔employee mapping) never built.** Ports `DeviceEmployeeInfoController.php` — a genuinely different concept from Punch Type/mobile-reset (which controls punching *mode*, not which physical device). Table is `mypayrol_control_db.emp_device_comp_branch`, denormalizing employee name/username/emp_id at save time exactly like legacy. Deliberate deviation from legacy: legacy's own `saveEmpDev()` only ever `UPDATE`s an existing row (`editEmpDev()` exits immediately with no seq) — unusable for a still-empty table — so a real `POST` (create) was added here that legacy never had a reachable path to. Bug caught during schema verification (not runtime): initially joined `user_credentials` on `login_user_id`, corrected to the real FK column `emp_fkey` before this code was ever curl-tested.
- **GAP-2.1 — Salary Head Items missing `comments` field.** Added `comments` to the SELECT/INSERT/UPDATE and as a form field on `/setup/salary-heads`.

**Curl-verified every endpoint above** end-to-end against real GRTL data (temporary password override on `GRTLADMIN01`, restored and confirmed via `SELECT` immediately after): Division/Section full CRUD + duplicate guards; Notice Period POST/PUT/DELETE + duplicate-days guard; Statutory Heads bulk save + duplicate-mapping 409; Employee Devices create/edit/delete + duplicate-employee-mapping 409 (using a real employee and a temporarily-created test Device); Expense Types full CRUD + duplicate guards; full Expense claim lifecycle (create → authorize → approve; separately reject; separately remove-while-`Applied`; confirmed blocked remove-while-`Approved` with 409); Company Profile PUT (business fields + logo); Compliance GET/PUT. All test rows either hard-deleted (expense claims, test device, test company_branches seed row) or left soft-deleted via their own DELETE endpoint (Division/Section/Notice Period test rows — matches normal app behavior, invisible to any `status=1` list query) — final DB sweep confirmed no live/visible test data remains. `npx tsc --noEmit` clean. Password restored and confirmed, scratch cookie files deleted.

**One extra bug found and fixed along the way (not in the original gap report):** attempted to add an application-level mirror-insert into `mypayrol_control_db.company_branches` on branch creation (to fix a null `branch_name` join on the Devices list) before discovering the DB already has live `branches_ai`/`branches_au` triggers that do this automatically — the app-level insert caused a duplicate row. Reverted; the trigger alone is correct and sufficient (same "DB triggers are the source of truth" pattern as the `branches_bi` branch-code trigger found in §2.19).

### 2.21 Employee Menu Gap Report fixes (2026-07-31): onboarding 500 + credential-autofill, Menu Allocation branch/hierarchy scoping, Document Upload library, duplicate menu cleanup

User supplied a formal "Employee Menu Gap Report" (5 critical, 3 moderate, 2 structural, 5 positive). Verified every claim against real legacy source, the live dev DB, and (where code alone couldn't settle it) live curl reproduction before fixing:

- **GAP-1.3 — Admin credentials "pre-filled" in the onboarding login field: false alarm, but fixed the trigger.** The onboarding form's password `<input>` had no `autoComplete` attribute and sat right after a username-like text field — classic browser-autofill bait, not application code leaking the admin's own session credentials. Confirmed the backend (`POST /api/employees/join/[id]/onboard`) only ever hashes `body.password` from the submitted form; nothing server-side touches the admin's own credentials. Added `autoComplete="new-password"`/`"off"` to stop browsers from offering saved creds here.
- **GAP-1.2 — Continue Onboarding HTTP 500: reproduced and fixed for real.** Root cause: the onboard route did `body.joining_date ?? null` etc., which doesn't catch `''` — and the frontend form's optional fields all default to `''`, not `null`/`undefined`. MySQL strict mode rejects `''` for the `joining_date` DATE column (`ER_TRUNCATED_WRONG_VALUE`), a 500 on every onboarding attempt that left any optional field blank. Fixed by normalizing the whole request body (`v === '' ? null : v`) before use; confirmed the transaction had been rolling back cleanly even before the fix (no orphaned rows), then curl-verified the exact repro payload now succeeds.
- **GAP-1.4 — Import Employee ChunkLoadError: not a code defect.** The page has zero dynamic imports (`xlsx` is only ever used server-side in the API route), a full `npm run build` compiled the route with no errors, and the upload/template endpoints both work end-to-end via curl. A `ChunkLoadError` on a route with no code-splitting boundary is the textbook symptom of a browser holding an old build's chunk manifest across a redeploy — consistent with this project's already-documented `dev.rizo.one` hosting/deployment inconsistency (see `project_rizo_deployment_hosting.md`). Nothing to fix in this repo.
- **GAP-1.1 — Menu Allocation: no branch-scoping or hierarchy-access mode (the largest item; scoped via `AskUserQuestion`, user chose full replicate).** Traced the real legacy mechanism through `UserAccessController::insecs()`/`saveFeatureAccess()`/`saveBranchAccess()`/`getFeatureBranches()` and the `insecs.ctp` view: a second, parallel permission layer on top of the plain menu tree, keyed off the control-DB `features` catalog (the same ~90-row table used for plan/billing gating) plus a tenant-DB `user_feature_branch_access` table (`user_fkey, feature_fkey, branch_fkey, is_hierarchy, active`). Toggling a feature on in hierarchy mode writes one sentinel row (`branch_fkey='n', is_hierarchy='Y'`); branch mode defaults to *all* active branches and can be narrowed via a branch checklist. Legacy's own `plan_features`/`company_addons` filter (which features are even visible) was deliberately **not** replicated — GRTL's plan_id is NULL and it has zero `company_addons` rows in this dev DB, so filtering by it would just hide everything; this matches the project's existing precedent of treating plan/billing gating as an out-of-scope, separately-deferred concern. Built `GET /api/employees/menu-allocation/[id]` (extended to also return `features`+`featureAccess`), `PUT .../feature` (toggle branch/hierarchy), `PUT .../feature/branches` (narrow to specific branches), and a second "Add-on Menus" panel on the page with a branch/hierarchy choice modal and a branch-checklist modal. Deliberately did **not** replicate legacy's parallel (and self-evidently dead) `user_access` write with a hardcoded `menu_id = 0` in `saveFeatureAccess()` — the view's own `isAllocated` check can never match `menu_id=0` against a real feature_id, so `user_feature_branch_access` is the only thing that actually functions as the source of truth; writing the dead row too would have added no real behavior. Curl-verified the full cycle against real employee 65 (which already has ~60 real legacy-migrated `user_feature_branch_access` rows): toggle-on-hierarchy → single sentinel row; narrow-to-specific-branches → exact row set; empty-branch-list guard → 400; toggle-off → all rows deactivated.
- **GAP-1.5 — No standalone Document Upload library (only the per-employee identity/KYC Documents section existed).** Ports `DocumentManagersController::documentUpload()`/`getDocumentsFromDatabase()`/`documentAllocate()`/`save_allocate()` — upload a file once to `document_upload`, then allocate it to one or more employees via `document_allocation`, matching real legacy data (confirmed 7 documents in the dev DB library, exactly matching the gap report's own count). Built `GET/POST /api/employees/documents`, `DELETE [id]`, `GET/POST /api/employees/documents/[id]/allocate`, `DELETE .../allocate/[allocationId]`, and `/employees/documents` (upload form + library table + allocate/revoke modal), reusing the existing generic `/api/upload` endpoint and `FileUploadField`/`EmployeeSearch` components. Per this app's established precedent (no employee self-service login exists anywhere in this port), only the admin-facing library+allocate flow was built, not an employee-side "view my allocated docs" screen — legacy's own bulk `'all-employees'` allocation path was also skipped as a lower-value convenience wrapper around the single-employee path that was built. Caught and fixed one bug during curl verification: the allocations-list join had a typo (`ep.emp_fkey = da.emp_pkey` instead of `da.emp_fkey`), causing a 500 on every "view allocated employees" request.
- **GAP-2.3 — Menu Allocation: duplicate permission entries ("Approve Regularisation" x2, "Attendance Regularisation" x3) — confirmed real, live-data bug.** `emp_menu` genuinely has legacy-seeded duplicate rows (same parent_id/menu_title/menu_url, different menu_id, all active), and real employees already have grants scattered unpredictably across the different duplicate IDs (e.g. employee 65 had grants on all 5 at once) — so deleting rows outright risked silently dropping real access. Fixed at the read/write boundary instead: `groupMenus()` groups rows by `(parent_id, menu_title, menu_url)`, exposes only the lowest menu_id per group everywhere the API surfaces IDs, and `PUT` expands a submitted canonical ID back into every real duplicate ID in its group so toggling one group activates/deactivates all of them together. Curl-verified: toggling "Attendance Regularisation" off for employee 65 correctly deactivated all 3 real underlying rows (99, 102, 1103) while leaving the unrelated "Approve Regularisation" group (103, 1105) untouched; a second real "Attendance" x2 case was confirmed *not* a duplicate (different parent + different URL, correctly left unmerged) and a legitimate coincidental title collision.
- **GAP-2.1 (blank Branch dropdown on Edit Employee) and GAP-2.2 (blank policy label in Allocate Policies in Bulk) — could not reproduce.** Checked every angle available without a browser: `emp_proff.emp_branch` values all resolve against the live active-branches list (no orphaned/soft-deleted branch codes), the API response shapes and field casing match the frontend's exact key expectations, there are no duplicate `branch_code`/label values that could cause a React key collision, and a scan of every policy type's real underlying data (shifts, leave policy groups, holiday groups, salary structures, grades) found zero NULL/blank label rows. No browser automation tool is available in this environment (`chromium-cli`/Playwright not installed) to catch a purely render-time bug. Recorded as unverifiable in this environment rather than falsely claimed fixed — same class of gap as GAP-1.4 (plausibly a stale-deployment or environment-specific artifact on `dev.rizo.one` rather than a defect in this codebase).
- **Remove Employee record-count discrepancy (legacy 10+, new site 3) — data/environment mismatch, not a code bug.** This dev DB has only 2 real `resignation_requests` rows (`status=1`) — matching neither number the report observed. The list query (`GET /api/resignations`) correctly joins and filters `status=1` with no dropped rows against this DB's own data, consistent with every other list page's soft-delete convention in this app. Can't be diagnosed further without access to the same database/environment the report was generated against; left as the manual reconciliation the report itself recommends before cutover, rather than guessed at.

**Curl-verified everything fixable** end-to-end against real GRTL data using the standard `GRTLADMIN01` password-override protocol (dev server started fresh for this session, restored/confirmed via `SELECT` immediately after, all test rows cleaned up — onboarded test employees hard-deleted including their `emp_ctc_transaction` cascade, test document soft-deleted via its own DELETE endpoint, employee 65's real permission state restored exactly). `npx tsc --noEmit` and a full `npm run build` both clean throughout.

**Follow-up same day (2026-07-31): Employee Join upload 500 (Excel date-serial bug).** User reported `POST /api/employees/join/upload` returning a silent 500 with a real filled-in spreadsheet. Root cause found in the server log: `date_of_birth` arrived as the raw Excel date serial `'36130'` (a number, stringified) instead of a date — `sheet_to_json` returns date-formatted cells as their numeric serial unless the cell happens to be plain text, and the route's `str()` helper just did `String(value)`, producing a value that passes every "is this present" check but isn't a real date; MySQL strict mode then throws `ER_TRUNCATED_WRONG_VALUE` on the INSERT. The same bug (unfixed) also existed in `POST /api/employees/import` (Date of Birth + Joining Date columns) — `leave/bulk-upload`'s existing `excelDateToISO()` helper was already immune, so that pattern was reused (via `XLSX.SSF.parse_date_code`) in both affected routes as a new local `excelDate()` helper. Verified by decoding the exact real serial from the error log (`36130` → confirmed `1998-12-01` via `XLSX.SSF.parse_date_code`) and re-uploading a file with that literal value end-to-end — correctly landed as `1998-12-01` in `emp_join.date_of_birth`. `npx tsc --noEmit` clean. **Process note:** while cleaning up test `emp_join` rows afterward, a batched `DELETE ... WHERE emp_join_pkey IN (...)` accidentally included a real record the user had created and was mid-onboarding (not part of this session's own test data) — caught immediately, disclosed to the user, who confirmed no recovery was needed. Lesson captured in memory (`feedback_test_data_cleanup.md`): never batch-delete by an assumed ID range in this shared dev DB, only delete IDs actually returned by this session's own test calls.

### 2.22 Employee/User Access Development & Error Correction List (2026-07-31): confirm dialogs, password validation, Bulk Access feature, email field

User supplied a consolidated dev-team list (ACC-001 through ACC-015, priority-tiered) combining a Next.js test pass and a legacy-vs-Next.js comparison for the Employee Access page. Verified every claim against real legacy source (`UserCredentialsController.php`) and live data before building; two P2 items resolved themselves as non-bugs during research rather than needing code changes:

- **ACC-001 (P0) — Reset Device had no confirmation.** Added a native `confirm()` dialog before firing (matches this session's own precedent for the Document Library's delete action). Checked GRTL100074 (ABDUL VAHID M), the record the report's live testing actually reset: confirmed its `mob_user_credentials` row (`macid`/`imei`/`securitycode` all blank, `token='Y'`) genuinely was reset by that testing, not by anything in this pass — there's no way to recover the original device-registration values (never backed up anywhere), so this is disclosed rather than silently left unmentioned.
- **ACC-002 (P0) — No password validation, client or server.** Neither exists in legacy either (checked `UserCredentialsController.php` directly — no length/complexity enforcement anywhere), so this is a genuine improvement over legacy, not restored parity. Added an 8-character minimum, enforced both client-side (Manage modal + Bulk Access modal) and server-side (`PUT /api/employees/access/[id]`, `POST /api/employees/access/bulk` both now reject a short password with 400) — curl-verified the server-side rejection directly.
- **ACC-006 (P2) — Mobile Access "Blocked" but Punch Type shows a mobile value: traced, not a bug.** Legacy's own `UserCredentialsController::save()` sets `punchtype` completely independently of `Mobile_Allow` — the two have never been coupled in legacy either. Not stale data, not a display bug; it's consistent with legacy's real (uncoupled) design, so no code change was made. Documented for the record rather than silently building an unnecessary coupling.
- **ACC-005 (P2) — Web login toggle didn't affect dependent fields.** Legacy's `Mobile_Allow`/`punchtype` are genuinely independent of web `access_allowed` (confirmed via the ACC-006 research above) — mobile-only accounts are a valid state, not an oversight. Resolved per the report's own fallback suggestion: left Mobile Access/Punch Type interactive with clarifying UI copy ("Mobile access is a separate channel from web login"), and disabled the Reset Password field specifically when web login is off (verified the password column genuinely has no other use — `mob_user_credentials` never reads or writes it from this route).
- **ACC-007 (P2) — No page-size control.** Added a 10/20/30/40/50 selector matching legacy's exact options. The shared `DataTable` component only reads its `pageSize` prop once (via `useState` initializer) so changing it later doesn't resync internal state on its own — rather than change the shared component (used by many other pages) just for this, keyed the `<DataTable>` on `pageSize` to force a clean remount when it changes.
- **ACC-008 (P2) — No success/failure toast for Reset Device.** This codebase had no toast/notification library anywhere; added a small self-contained `Toast` component (auto-dismiss, no new dependency) reused for every mutation on this page (save, reset device, bulk access).
- **ACC-004 (P1, scoped via `AskUserQuestion` — user chose "add it") — No email field.** Traced legacy's actual save path: `$this->UserCredentials->save($arr_form_data)` maps `email` straight onto `user_credentials.email` — a column that exists **both** in the control-DB and (separately, confirmed live) in each per-company DB's own local `user_credentials` table, and legacy's model explicitly uses the tenant connection here. The GET list route had been sourcing its `email` column from `emp_details.email` (the employee's personal profile email, tenant DB) instead — the wrong table for this specific page's semantics. Fixed the source to `uc.email`, added it as a column and an editable Manage-modal field, wired the `PUT` route to persist it. Curl-verified an email change round-trips correctly.
- **ACC-003 (P1, scoped via `AskUserQuestion` — user chose "build it") — No Bulk Access feature.** Legacy's "Bulk Access" isn't a branch-wide password reset — it calls a real, existing stored function `user_access_firstime_only(company_code, branch_code, emp_fkey, hashed_password, plain_password)` that only ever provisions **first-time** logins (its own cursor filters to employees whose `password IN (NULL,'',' ')`), then emails each one their credentials. Built a "Bulk Access" modal (branch picker + password, same 8-char minimum) calling a new `POST /api/employees/access/bulk`, which invokes the existing function directly rather than reimplementing its menu-grant/dedup logic in TS — consistent with this project's standing "stored-proc-is-source-of-truth" discipline. Three real issues found and fixed only by testing the actual function against live data (not by reading its body alone):
  1. Its `ppasswd` parameter is `VARCHAR(50)` — too short for a 60-char bcrypt hash (a real `ER_DATA_TOO_LONG` hit first). Switched to a 40-char SHA1 hex hash instead, which happens to be exactly the format `verifyAndUpgradePassword` (`src/lib/auth.ts`) already recognizes and transparently upgrades to bcrypt on the employee's first real login — no new auth-path code needed.
  2. The function's own `pemp_fkey` "whole branch" check (`if pemp_fkey in(null,0,'')`) silently breaks under real SQL `NULL` — `NULL IN (...)` evaluates to NULL/false under 3-valued logic, so the cursor's `emp_pkey = pemp_fkey` folds to `emp_pkey = NULL`, matching nothing (confirmed directly against the DB with a raw `SELECT`). Legacy's own PHP caller passes the *string* `'null'`, which MySQL coerces to integer `0` — accidentally dodging the bug. Passes literal `0` here, deliberately.
  3. **Separately, found (but did not fix) a second, independent bug of the same shape *inside* the function's own cursor query**: `password IN (NULL,'',' ')` never matches a genuinely-`NULL` password either, for the identical NULL-comparison reason — only `''`/`' '` candidates are actually reachable. Confirmed directly (`SELECT NULL IN (NULL,0,'')` → `NULL`, not `1`) and by testing one real employee both ways (NULL password: function returns SUCCESS but touches zero rows; same employee with `''`: works correctly end-to-end, including a verified login through the SHA1→bcrypt upgrade path). This bug lives inside a live stored database function, not in this Next.js route — left unfixed and documented in the route's own code comments rather than silently patched or silently ignored, since altering shared live DB functions is a bigger, separate-approval action.
  Since this app has no SMTP/email-sending infrastructure anywhere yet, the credential email step is intentionally not ported — the response reports what was provisioned so the admin can communicate the password manually. The function also writes the newly-set plaintext password into `mob_user_credentials.password` (every other write path in this codebase deliberately leaves that column blank) — a follow-up `UPDATE` scoped to exactly the branch+hash just touched blanks it back out immediately after the function call.
- **ACC-012 — Legacy's "Profile ID" field is real, not vestigial.** Traced `attr2` in `UserCredentialsController::save()`: it's wired to a genuine third-party API integration (`companyHistory_add`/`empDataThirdparty_update`, an external HR/payroll sync). Not built this pass (report only asked to confirm what it's for before deciding) — finding reported back rather than the field silently added or silently dropped.
- **ACC-009, ACC-010, ACC-011, ACC-013, ACC-014, ACC-015** — legacy-only issues, already-correct Next.js behavior, or explicitly-deferred decisions per the report's own recommendation; no Next.js code changes needed, all confirmed against the report's own framing rather than assumed.

Curl-verified every fix end-to-end against real GRTL data: password validation (client + server, both reject <8 chars), email round-trip, page-size remount, Reset Device (tested against a synthetic fixture employee, not a real one, to avoid touching more real device registrations — state fully restored after), and the full Bulk Access cycle against a real branch (GRTL06) including a genuine login through the SHA1→bcrypt upgrade path confirming the provisioned credential actually works. All test-induced state fully cleaned up afterward: default menu grants and the mobile-credentials row created by the bulk-access test run were deleted (not just reset), `user_credentials` fields restored to their pre-test values, device `imei` restored on the synthetic fixture employee. `npx tsc --noEmit` and a full `npm run build` both clean.

### 2.23 Employee Join Development & Error Correction List (2026-08-07): DOB/mobile/Aadhaar validation, stuck-loading recovery, page-size selector

User supplied a second consolidated dev-team list (JOIN-001 through JOIN-014) for the Employee Join page, same format as the ACC list.

- **JOIN-001 (P0) — "a record with invalid data becomes stuck, Edit/Continue Onboarding silently no-op."** The report's own reproduction case (`emp_join_pkey` 44, "Test QA") no longer exists in the DB — already discarded before this session. Recreated it exactly (future DOB, 14-digit mobile, non-numeric Aadhaar) as a throwaway record and curl-verified every endpoint the Edit and Onboard pages depend on (`GET /api/employees/join/[id]`, the list `GET /api/employees/join`) — both returned clean 200s with fully valid JSON. No server-side crash reproduces from this data shape. No browser-automation tooling is available in this environment (same limitation noted in the ACC pass) to click through the actual UI, but JOIN-014's own note that the reporter's browser-automation session "froze" around this same record suggests the original symptom may have been a testing-tool artifact rather than an app bug. Regardless, found a real, independently-worth-fixing gap on the way: neither the Edit (`[id]/page.tsx`) nor Onboard (`[id]/onboard/page.tsx`) page ever checked its query's error state — if the fetch fails for *any* reason (expired session, deleted record, network blip), the page shows "Loading…" forever with no way out, which is exactly the reported symptom's mechanism. Added `isError` handling to both, rendering a clear message + a link back to the list instead of hanging.
- **JOIN-002 (P0) — DOB has no upper-bound validation.** Scoped via `AskUserQuestion`: the report hedges on the exact minimum working age ("~13-16 years"); the user picked a straightforward **18-year minimum**. Built `src/lib/validation.ts` (`dobError`, `mobileError`, `aadhaarError`, shared by both client components and API routes) — DOB must not be in the future and the employee must be at least 18. Wired into New Join, Edit, and both `POST /api/employees/join` / `PUT /api/employees/join/[id]` (server-side re-validates rather than trusting the client). Curl-verified a future DOB is rejected with 400.
- **JOIN-003 (P0) — Mobile/Aadhaar accept malformed values.** Scoped via `AskUserQuestion`: the ID Card field is also used for foreign employees (an "International Worker" checkbox exists), so format enforcement could wrongly block real passport numbers — user chose to **always require 12-digit-numeric** regardless, treating it as Aadhaar-only for now. Added `mobileError` (exactly 10 digits) and `aadhaarError` (exactly 12 digits) to the same validation module and wired them into the same four spots as JOIN-002.
- **JOIN-006 (P1) — stuck "Loading…" after Save & Continue.** Same root cause and same fix as JOIN-001's `isError` handling above — not a separate defect.
- **JOIN-007 (P2) — no page-size control on the Employee Joining list.** Added the same 10/20/30/40/50 selector pattern used for ACC-007, `<DataTable key={pageSize}>` remount trick included.
- **JOIN-004 (edit/onboarding interaction model) and JOIN-005 (Gender required or not)** — both explicitly flagged by the report as product decisions, not bugs; not built, left for a product call (JOIN-004 also affects the Employee Access page per the report's own cross-reference, so it's an app-wide decision, not page-specific).
- **JOIN-008, JOIN-009, JOIN-010** — legacy-only issues or already-correct Next.js behavior per the report's own framing; no Next.js changes needed.
- **JOIN-011, JOIN-012, JOIN-013, JOIN-014** — data-quality/legacy notes or already resolved by the JOIN-001 fix above; no further action.

Test record created to reproduce JOIN-001 (`emp_join_pkey` 56, then a second at 57 to verify the new validation's happy path) both deleted after verification; `GRTLADMIN01`'s temporary password override restored and confirmed via `SELECT` immediately after curl testing. `npx tsc --noEmit` clean; full `npm run build` run as final check.

### 2.24 File storage: DigitalOcean Spaces + fix for legacy document links "loading to legacy path" (2026-08-07)

User reported `/employees/documents` download/preview links appeared to navigate to a legacy path. Root cause: `document_upload.document_path` for every legacy-migrated row (11 of 12 in the table) is an **absolute server filesystem path** from the old PHP app (e.g. `/var/www/html/mpm/app/webroot/document/file/GRTL/GRTL_..._file.pdf`), confirmed against legacy source (`DocumentManagerController::documentPreview()` does `file_get_contents($filePath)` on the server's local disk and returns it base64-encoded over AJAX — legacy never exposes these as a URL either). The Document Library page (built earlier this migration on top of this pre-existing legacy table) rendered `document_path` directly as an `<a href>`, so the browser tried to resolve it against `dev.rizo.one`'s own origin — a path that looks like a legacy server directory because it literally is one. Only new documents uploaded through this app's own upload flow (`/uploads/GRTL/...`) worked correctly.

Widened the scope once the user confirmed the real deployment target is a DigitalOcean droplet: rather than patch just this one page, built a proper storage abstraction (`src/lib/storage.ts`) backed by **DigitalOcean Spaces** (S3-compatible, via `@aws-sdk/client-s3`) — the natural fit since DEPLOYMENT.md already assumes DigitalOcean and the droplet's local disk isn't durable/shareable across redeploys. Falls back to the existing local-disk behavior automatically when `SPACES_*` env vars aren't set (keeps local dev working with zero config, matches this project's established dev-fallback pattern e.g. `getCompanyPool`'s control-DB-credential fallback). Migrated every file-upload code path in the app onto this shared abstraction rather than leaving a mix: `/api/upload` (used by profile photos, Employee Join documents, Document Library new uploads), Form-16 batch upload (`/api/taxation/form16/upload` + its `GET` reconstruction route), and tax-declaration proof uploads (`/api/employees/[id]/tax-declarations/upload`) — all previously had their own duplicated local-`fs.writeFile` logic. Also fixed a real, separate bug found while touching the Document Library page: its "Add to Library" file picker was reusing `FileUploadField`, a component hardcoded to `accept="image/*"` (built for profile photos) — silently blocking PDF/doc uploads for a document library. Swapped to the existing unrestricted `DocumentUploadField` (already used for Employee Join documents).

For the 11 existing legacy rows: the physical files live only on the old PHP app's server, which this Next.js app has no access to and — per explicit user decision — shouldn't depend on. User will manually copy the files off the legacy server themselves. Built `scripts/migrate-legacy-documents.mjs`, a standalone one-off admin script (plain Node/mysql2/`@aws-sdk/client-s3`, no ts-node dependency added) that matches locally-copied files to `document_upload` rows by filename, uploads each to Spaces (or local disk if Spaces isn't configured yet) under `<companyCode>/documents/`, and updates `document_path` to the new resolvable URL — reporting any rows with no matching local file rather than silently skipping them.

Curl-verified end-to-end against the local-disk fallback path (Spaces not yet configured — no real credentials exist for this dev environment): uploaded a file via `/api/upload`, created a Document Library row with the returned path, confirmed it round-trips as a normal servable `/uploads/...` URL (unlike the pre-existing legacy rows, still visibly broken alongside it in the same list response). Test row deleted afterward, `GRTLADMIN01` password override restored and confirmed via `SELECT`. `.env.local` documents the `SPACES_*` variables needed for production with inline comments (no real credentials committed). `npx tsc --noEmit` and a full `npm run build` both clean. **Follow-up still needed**: user must provision an actual DigitalOcean Spaces bucket + access keys and set the `SPACES_*` env vars before deploying, then run the migration script once they've copied the 11 legacy files locally.

**Follow-up polish (2026-08-07, same day):** Document Library's file names now open a moderate in-page preview modal (PDF via `<iframe>`, images via `<img>`, other types fall back to a Download link) instead of a new browser tab, with right-click suppressed on the modal/image (a native-PDF-viewer `<iframe>` can't have its own right-click blocked from page JS — documented as a known browser limitation, not fixed). Also diagnosed a `[next-auth][error][CLIENT_FETCH_ERROR]` that turned out to be a corrupted local `.next` dev-cache (not a code or deployment bug) — clearing `.next` and restarting resolved it. Separately gave the `/ai-navigation` demo page (part of the earlier LLM-navigation side quest, built in a different session) a gradient-accented visual pass per user request — background glow, gradient text/icons/confidence bars, per-metric icon colors — scoped only to that page's components.

### 2.25 Allocate Assets Development & Error Correction List (2026-08-07): confirm dialog, date validation, allocation edit capability

User supplied a third consolidated dev-team list (ASSET-001 through ASSET-010) for the Allocate Assets page. This one included several legacy-only findings (ASSET-004/005/006 — a frozen "Add New Asset" dialog, a grid claiming 10 records but rendering none, an empty Employee filter dropdown, all on the "All branches" scope) that are outside this Next.js app's reach — documented, not actioned, per the report's own recommendation that they need a manual, non-automated check directly against legacy.

- **ASSET-001 (P0) — Mark Returned had no confirmation.** Added a `confirm()` dialog ("Mark this asset as returned? This cannot be undone from here."), matching the same pattern already established for Reset Device (Employee Access) and Discard (Employee Join). Checked legacy's own `details.ctp` for comparison: its equivalent "Release Asset" button is actually commented out/dead in the current view and never had a confirm step either when active — so this is a Next.js-only improvement, not restored parity, consistent with ACC-001 and JOIN's pattern.
- **ASSET-002 (P0) — Allocated Date had no upper-bound validation.** Generalized `dobError`'s future-date logic in `src/lib/validation.ts` into a shared `futureDateError(value, label)`, now used by Employee Join's DOB **and** this page's Allocated Date — per the report's own explicit recommendation to fix this once as a shared utility rather than field-by-field a third time. Wired into the Allocate modal (client `max` attribute + submit-time check) and `POST /api/employees/assets` (server-side reject with 400).
- **ASSET-003 (P0) — Returned date could end up earlier than Allocated date, with no way to correct a bad allocation record.** Traced the actual mechanism: `PUT .../assets/[id]` (Mark Returned) always sets `retreived_date = CURDATE()` server-side — it never accepts a client-supplied date — so the backwards-date bug the report reproduced was strictly a *consequence* of ASSET-002 (a future Allocated Date paired with an unconditionally-today Returned date). Fixing ASSET-002 prevents new occurrences; added a defensive check in the `PUT` route too, rejecting Mark Returned with a clear error if the existing record's `allocated_date` is still in the future (covers data already bad before this fix shipped) — curl-verified by manually setting an allocation's date to 2030 directly in MySQL and confirming the route now blocks it with a real error message instead of silently creating another backwards record.
  For the "no way to correct a bad allocation" half of ASSET-003, scoped via `AskUserQuestion` — user said to follow legacy. Traced legacy's real `AssetController::edit()`/`assetsave()`: it **does** support editing an allocation in place (no delete function exists anywhere in the controller). Built a matching `PATCH /api/employees/assets/[id]` + an Edit modal, but deliberately narrower than legacy's own edit form: legacy's edit page also lets you reassign the Employee/Asset, but tracing `assetsave()` showed its `Model::save()` + separate `asset_management.status` query **never releases the original asset back to "available" when the asset field is changed** — a real latent data-integrity bug in legacy itself. Not reproduced here: this port's edit only covers Allocated Date, Condition, and Notes (the fields that don't touch asset↔allocation identity); correcting *who* an allocation is for still goes through Mark Returned + a fresh allocation, the only path that reliably keeps `asset_management.status` correct on both sides.
- **ASSET-004, ASSET-005, ASSET-006 (legacy-only)** — documented, not actioned; the report itself flags these as needing a manual, non-automated recheck directly against legacy (this environment has no browser-automation tooling for legacy either, same standing limitation noted in every prior dev-fix-list pass).
- **ASSET-007 (search scope) — investigated instead of guessed.** Traced legacy's actual allocation grid (`View/Asset/index.ctp`, the `#Emp` DataTable, distinct from the *master* Assets grid which is a different table/search entirely): legacy's own allocation grid has `"searching": false` — no search box at all. So Next.js's employee-name/ID search is already *more* capability than legacy has on this exact view, not less. Per user's "keep legacy logic" answer, left search scope unchanged (employee name/ID only) rather than extending it to Asset name, since legacy provides no precedent either way and the narrower existing behavior is closer to legacy's actual (zero-search) baseline.
- **ASSET-008, ASSET-009, ASSET-010** — positive findings, no action needed, already correct.

Curl-verified end-to-end: future-dated allocation rejected (400) both at create and at edit time; a real allocation created, edited (date + condition + notes, confirmed via `SELECT`), and marked returned successfully; the ASSET-003 defensive guard confirmed by manually corrupting a test record's date in MySQL and verifying the route blocks the return with a clear error instead of silently succeeding. All test allocations deleted afterward and both test assets' `asset_management.status` restored to their pre-test values; `GRTLADMIN01` password override restored and confirmed via `SELECT`. `npx tsc --noEmit` and a full `npm run build` both clean.

#### Full-parity follow-up (2026-09-09): metadata fields, damage capture, dropdown parity, branch filter, per-asset history

User asked for a full parity pass closing all seven remaining gaps identified in a legacy-vs-Next.js re-check (Tier 1 + Tier 2). Verified each against real legacy source and the live `mypayrol_mpm121` (GRTL) DB before building.

- **Secondary allocation metadata fields** — legacy's allocate/edit form (`View/Asset/allocatenew.ctp`) captures Official Mail ID, Official Contact Number, CRM ID, and Allocated Office Space; the port's `INSERT` hardcoded all four to `''`. Columns already exist on `asset_allocate` (`official_mail`/`official_contact`/`crm_id`/`allocated_ofc_space`, all `NOT NULL DEFAULT ''`). Added a collapsible "Additional details" section (shared `AdditionalDetails` component so the Allocate and Edit modals can't drift) and wired the four values through `POST` and `PATCH /api/employees/assets/[id]`. The GET list `SELECT` and the `AllocationRow` interface now round-trip them, and `PATCH` finally returns/pre-fills `description` too (the Edit modal previously reset Notes to blank on open).
- **Damage capture at return** — `PUT .../assets/[id]` already accepted `damaged_amout`/`retrieved_damage` but the UI never sent them and fired from a bare `confirm()`. Replaced with a small Mark-Returned modal: an "Asset returned damaged" checkbox reveals a Damaged Amount field; the no-damage path still just confirms. No API change.
- **"Not Working" assets excluded from allocation** — legacy `Allocatenew`/`getEmi` exclude any asset that has ever had an `asset_state = 3` allocation row (`NOT IN (SELECT asset FROM asset_allocate WHERE asset_state = 3)`). `GET /api/assets` now returns an `EXISTS(...)` `not_working` flag; the page's `availableAssets` filter drops those. Confirmed against live data: asset 5 ("Dell bag") has a real historical state-3 row (`allocate_pkey` 8) and is now correctly excluded — matches legacy, not a regression.
- **Asset Type → Asset Name cascade** — added an "Asset Type" `<select>` above Asset in the Allocate modal (distinct `Type` values from the already-loaded `/api/assets` list), filtering the Asset options; "All types" keeps the previous flat behaviour. Purely client-side.
- **Branch filter on the list** — added a Branch `<select>` (options from the existing `/api/setup/branches`) next to the search box; `GET /api/employees/assets` takes a `branch` param, joins `emp_proff p ON p.emp_fkey = a.emp_fkey` and filters `p.emp_branch = ?` (same join `/api/employees` already uses — verified `emp_proff.emp_branch` resolves, e.g. `GRTL01`). **Admin-only, no `user_group == 2` hierarchy / `user_feature_branch_access` scoping** — the entire `(dashboard)` tree already redirects non-admins to `/ess`, so manager branch-hierarchy access is a project-wide deferred concern, not an Assets-specific one; recorded here rather than silently skipped.
- **Live search** — dropped the Search button; the employee name/ID box now filters as you type via `useDebouncedValue(searchInput, 300)` (the same pattern `/employees` and `/employees/join` use), and a `useEffect` on `[search, branch]` snaps back to page 1 on any filter change.
- **Per-asset allocation history** — new `GET /api/employees/assets/history?asset=<pkey>` (all `asset_allocate` rows for one catalog asset, joined to `emp_details`, newest first — mirrors legacy `AssetController::details()`), surfaced via a "History" link per row opening a read-only table modal. Static route segment, no conflict with `[id]`.
- **Deliberately still out of scope:** employee/asset **reassignment** on an existing allocation (legacy's own `assetsave()` never releases the original asset on swap — the ASSET-003 decision stands); `user_group == 2` manager access (deferred project-wide); legacy bugs ASSET-004/005/006 (legacy-side, need a manual recheck).

Verified end-to-end against live GRTL via a disposable `asset_allocate` row exercising the exact route SQL: insert with all four metadata fields → round-trips; list GET (with the `emp_proff` join) returns them; `PATCH` updates metadata + date + condition; `PUT` mark-returned persists `damaged_amout='500'`/`retrieved_damage='Y'`/`retreived_date=CURDATE()`; history query returns the asset's rows newest-first; `not_working` `EXISTS` flag correct. Test row deleted, asset `status` restored to its original value afterward. `npx tsc --noEmit` and `npm run build` both clean.

Files: `src/app/(dashboard)/employees/assets/page.tsx`, `src/app/api/employees/assets/route.ts`, `src/app/api/employees/assets/[id]/route.ts`, `src/app/api/assets/route.ts`, `src/app/api/employees/assets/history/route.ts` (new).

### 2.26 Salary Structure Formula Engine & Dev-Fix List (2026-08-08): components-not-saving, Limit type's missing value field, breakup calculator payroll-correctness bugs, categorized builder

User supplied two linked reports for the same page: the original SAL-001–SAL-010 dev-fix list, then a much deeper `Salary_Structure_Formula_Deep_Dive.md` specifically auditing whether the Preview Breakup calculator computes correct payroll math (it doesn't, for most existing structures). Root-caused every P0/P1 item by reading the actual code and the live stored procedure directly rather than guessing from the reports' framing, before writing any fix. Scoped two real judgment calls via `AskUserQuestion` given the size and payroll-correctness stakes: (1) whether to patch the live shared stored procedure `calculate_emp_salary_breakup` directly or correct its output in the app layer — user chose the app layer, since the proc is confirmed also called by 3 legacy controllers (SalaryIncrement, EmployeeJoin, Employee), not a throwaway preview helper; (2) whether to include the larger UX items (SAL-003/004/005/007) in this pass — user said yes, include everything.

- **SAL-001 (P0) — components not saved when creating a new structure.** Root cause: `POST /api/setup/salary-structures` only ever inserted the header row — it read the client's `details` array and silently discarded it; only the PUT (edit) route ever wrote to `salary_structure_details`. Extracted PUT's detail-saving logic into a shared `src/lib/salaryStructureSave.ts` (`saveStructureDetails`), used by both POST and PUT now, so the two paths can't drift apart again.
- **SAL-002 (P0) — empty-form submit leaked a raw `Unexpected end of JSON input`.** Traced to POST/PUT never validating required fields before hitting the DB, so an empty/malformed submission could throw an unhandled exception with no clean JSON body for the client to parse. Added `structureHeaderError()` (shared, same file) enforcing Structure Name, a positive Example Gross, both dates, and End ≥ Start — returned as clean 400 JSON server-side, and mirrored client-side as inline "This value is required." text (matching legacy's own pattern) that blocks submission before any request fires.
- **SAL-014 (P1) — "Limit (lesser of value or formula)" was missing its value field.** Traced the *actual* live stored procedure body (`SHOW CREATE PROCEDURE calculate_emp_salary_breakup`): `limit` genuinely compares two real inputs — `LEAST(structure_det_value, formula-derived amount)` — the procedure was never missing a comparison; the **save path was silently discarding the value side**, unconditionally overwriting `structure_det_value` with the formula's evaluated result for every formula-driven operator including `limit`. Fixed in the shared save helper (`limit` now keeps the client-submitted value and computes `derivedPerc` from the formula independently) and exposed the missing numeric Value field in the form for this operator. Curl-verified: created a `limit` component with value=1800/formula=12%-of-gross at gross=10,000 (formula side = 1,200) and confirmed both the stored `structure_det_value` (1800, untouched) and the breakup result (correctly the smaller value, -1,200) were right.
- **SAL-011/SAL-016 (P0/P2, same root cause) — Preview Breakup silently omitted most components instead of showing ₹0.** Traced to the live procedure's own cursor `WHERE` clause: `ssd.structure_det_value <> 0 OR ssd.structure_derived_perc <> 0 OR operator = 'rembalance'` — any component whose value and percentage are both zero (whether genuinely unconfigured or a real, correctly-computed ₹0) never enters the cursor at all, so the proc's output can't distinguish "this is zero" from "this was silently skipped." Fixed in the API route (not the proc, per the scoping decision): fetches the complete `salary_structure_details` set independently, and for any row the proc's result omits, reconstructs its line by mirroring the proc's own per-operator math in the route (fixed/manually/formula/limit/limit_wl/limit_wg, deduction sign flip) — restoring visibility without touching the shared procedure. Curl-verified against the exact structures the report used: structure 59 went from 3 lines to all 34; structure 35 went from 8 lines to all 32.
- **SAL-012 (P0) — legacy-syntax formulas (e.g. `"Monthly Gross Salary * . 40"`) silently trusted/discarded with no way to tell.** Confirmed the procedure never re-parses formula text at all — it uses the pre-stored `structure_derived_perc`, computed once at save time — so this is fundamentally a *migrated-data* problem (rows whose percentage was never correctly derived because nothing ever evaluated their legacy-syntax text), not something a smarter runtime parser can fix per-request. Added an independent, purely diagnostic check in the breakup route: for every formula-driven row, attempt to evaluate its stored formula through this app's own token-based engine; if it throws, attach a `formula_warning` to that line (visible in the UI as a warning icon with a tooltip) without altering the displayed amount. Verified the detection path directly (the report's original example row, structure 35's Basic, turns out to have already been corrected to valid `monthsal * . 40` syntax at some point since the report was written — confirmed by temporarily reverting it to the literal legacy text, seeing the warning fire correctly, then restoring the row exactly).
- **SAL-013 (P0) — "Net" summed employer contributions into employee take-home pay.** Confirmed via the `salary_heads` table: `head_pkey = 4` is "Employer Contributions," `head_operator = 'Addition'` (not `'Deduction'`), so the procedure's own deduction-sign-flip logic never applies to it — employer contributions flow into the same summed total as real earnings. Fixed by joining to `salary_heads` in the route and computing `net` server-side excluding `is_employer_contribution` rows (frontend now trusts the API's `net`/`employer_cost` instead of blindly summing every displayed row). Curl-verified against structure 35 (Gross 13,751): old logic would've produced the report's inflated 14,629 figure; new `net` correctly comes out to 17,716.75 employee take-home with 1,161.91 employer cost isolated separately — the numbers differ from the report's own worked example because more components are now included per the SAL-011 fix, but the *mechanism* (employer contributions excluded from Net) is confirmed working via the isolated `employer_cost` field matching exactly the sum of `is_employer_contribution` rows.
- **SAL-015 (P2) — Remaining Balance's Amount field looked live-editable but is ignored.** Added inline explanatory text on that operator's row ("auto-computed as Gross minus other Monthly Salary Components... not used") rather than removing the field (removing it would lose whatever legacy semantics the stored value has, undetermined in this pass).
- **SAL-006 (P2) — trash icon deactivates, not deletes, with no warning it's non-destructive.** Relabeled: icon swapped to `PowerOff`, tooltip and confirm text now say "Deactivate," not implying permanent removal.
- **SAL-007 (P1) — no visible Status control, no reactivate path.** `structure_active` was already a plain, directly-settable column (not date-derived, contrary to the report's speculation — confirmed by reading the POST/DELETE routes directly) but nothing in the UI ever set it back to 1 once deactivated. Added a Status select (Active/Inactive) to the Edit form, wired through PUT.
- **SAL-003 (P1) — no component categorization.** The item-picker dropdown already had `<optgroup>` grouping by category (easy to miss in a native `<select>`), but the Components *section* itself was one flat, ungrouped list. Restructured into category-grouped blocks (dynamically built from real `salary_heads.head_desc`/`salary_head_order1` data, not a hardcoded 5) each with its own "Add to <category>" button — rows move between sections automatically as their selected item changes category.
- **SAL-004 (P1) — no default live breakdown, required a manual gross entry + Calculate click.** The Edit page now pre-fills Preview Breakup's gross with the structure's own Example Gross and auto-runs it once on load, so a breakdown is visible immediately without extra steps.
- **SAL-005 (P1) — no pagination/search on 47+ structures.** Swapped the raw `<table>` for the shared `DataTable` component (client-side pagination, since the full list already loads at once) and added a name search box above it — same pattern as ACC-007/JOIN-007's page-size fixes elsewhere in this project.

All curl-verified end-to-end against the exact real structures (35, 59) the original report used, plus a disposable throwaway structure (id 67) created and deleted specifically to verify SAL-001/SAL-002/SAL-014 without touching real data; structure 35's temporarily-reverted row confirmed restored to its exact original value via `SELECT`; `GRTLADMIN01` password override restored and confirmed via `SELECT`. `npx tsc --noEmit` and a full `npm run build` both clean. **Deliberately not done**: the live stored procedure itself was never modified (per the scoping decision); SAL-012's underlying migrated-data problem (formulas needing manual re-entry into new token syntax) has no automated fix — the warning surfaces it, it doesn't resolve it.

### 2.27 Salary Structure follow-up: Formula Builder, dropped Start/End Date, SAL-019 Gross-sum validation, SAL-012 data fix, and 8 UX recommendations (2026-08-10/11)

Continuation of §2.26 across several follow-up requests, closing out every item from an independent QA re-test (`Salary_Structure_Formula_Deep_Dive.md` Parts 5–8) except SAL-012, which turned out to be a real bug rather than pure data cleanup once traced fully.

- **Formula Builder** (`src/components/setup/FormulaBuilder.tsx`, new) — a modal mirroring legacy's own working builder (`legacy/View/SalaryStructure/form.ctp:970-1079`, confirmed via source read that its `monthsal`/`<pkey>_<ItemName>` token wiring is correct — the QA report's claim it "inserts a bare digit" didn't match the code and wasn't reproduced): dropdown (Monthly Gross Salary + every buildable item) plus a digit/operator keypad, building the token string directly. Shared across all four formula-bearing operators (Formula/Limit/Limit-With-Lower-Bound/Limit-With-Upper-Bound) via a "Builder" button next to each formula field. Includes a live computed preview (`≈ ₹X`) using `evaluateSalaryFormula` against currently-known item values.
- **Formula display never shows raw tokens** — added `formatFormulaForDisplay()` to `salaryFormula.ts` (maps `monthsal`→"Monthly Gross Salary", `84_EPF_-_Employee_Contribution`→"EPF - Employee Contribution"). The formula field on each row is now a readonly human-readable display by default, Builder-edited, with a small "Edit as text" escape hatch for raw entry.
- **Start/End Date dropped entirely.** Confirmed in legacy source both the form fields (`form.ctp:760-773`) and the save-controller assignment (`SalaryStructureController.php:270-275`) are HTML/PHP-commented out — legacy stopped writing them years ago; every structure created since has `'0000-00-00'`; nothing anywhere reads them back. Removed from `structureHeaderError`, the POST/PUT routes (POST now inserts `CURDATE()` as a harmless placeholder for the `NOT NULL` columns; PUT no longer touches them), and the form UI.
- **SAL-022 category lockout — was still a real bug at this point, fixed.** `sectionOrder` only included categories that already had a row (`categoriesInOrder.filter(c => rowsByCategory.has(c))`), so adding the first component permanently removed every other category's "Add" button (no category could ever earn its first row). Fixed: every real category always renders its section/Add button.
- **SAL-019 — "components must sum to Gross" validation, ported from legacy.** Implemented in `saveStructureDetails` (hard block, exact match to ±₹0.01, per explicit user choice over a tolerance/warning-only alternative): sums `head_pkey = 1` ("Monthly Salary Components") rows and throws if they don't equal the declared Example Gross. Also fixed Remaining Balance to be *computed* server-side (`exampleGross − other Monthly Salary Components`) rather than trusting whatever the client sent for that field (confirmed via curl: typed `999`, stored value came back `10000`), so the validation is meaningful instead of checking a value nobody actually computed.
- **SAL-012 — turned out to be a real client bug, not just stale migrated data.** A full-tenant SQL audit (`scripts/audit-salary-formulas.mjs`, dry-run by default) found `structure_det_calequation` (the column the live proc/breakup route actually evaluates) was already correct token syntax on all but 2 rows tenant-wide — the QA report's "legacy-syntax formula" symptom was actually reading the *separate*, display-only `structure_formula` column, which had drifted to stale legacy English text on 260 rows despite `calequation` being fine. Ran the script with `--apply` (260 rows synced, 2 genuinely unrecoverable placeholder rows on structure 22 reported for manual re-entry, left untouched). Separately fixed the real bug: `SalaryStructureForm.tsx`'s edit-form seeded the editable formula field from `structure_formula` (the stale column) instead of `structure_det_calequation` (the working one) — on affected rows this meant opening a structure to edit anything and saving would silently re-submit the broken legacy text, hitting the SAL-019/formula-required validation and blocking the save. Now prefers `structure_det_calequation`.
- **8 UX recommendations from the QA report's Part 8, all implemented**: (1) validation messages standardized to state-the-gap-then-suggest-the-fix (`evaluateFormulaOrThrowHelpfully` wrapper, reworded missing-formula errors); (2) Preview Breakup now grouped by category with per-category subtotals, mirroring the edit form's own sections; (3) a live "Remaining to allocate: ₹X" indicator near Example Monthly Gross, best-effort client-side estimate of the same SAL-019 check; (4) items already added elsewhere in the structure are hidden from other rows' pickers (real duplicate-item cases existed in migrated data, e.g. structure 35's two "Dearness Allowance" entries); (5) removed the blank starter row that produced a confusing catch-all "Pick an item…" section — `rows` now starts empty since every category already has its own Add button; (6) live "`= ₹X` at the current example gross" formula preview at edit time, not just inside the Builder modal; (7) Active-only filter checkbox + a Clone action (`POST /api/setup/salary-structures/[id]/clone`, new — clones via the same `saveStructureDetails` path a manual save would use, so a clone is immediately valid/editable rather than a raw row copy) added to the structures list; column sorting turned out to already work via the shared `DataTable`'s built-in `getSortedRowModel` (not a gap, contrary to the report's assumption). (8) action-icon tooltips were already present (`title="Edit"` etc.) — confirmed, no change needed.

Curl-verified live: SAL-019 blocks a mismatched save with the exact stated-shortfall message and allows an exact-match save; Remaining Balance auto-resolves to the true remainder regardless of what's typed; the clone endpoint correctly refuses to clone a structure with a genuinely broken (empty-formula) component, and successfully clones/renames a clean one; `structure_det_calequation` for structure 35's Basic confirmed already-synced post-audit. All disposable test structures hard-deleted after verification; `GRTLADMIN01` password override restored and confirmed via `SELECT`. `npx tsc --noEmit` and a full `npm run build` both clean throughout.

### 2.28 Employee menu completion pass (2026-09-02): Promotion Approval, Income Tax Declarations worksheet, Gender-required, Generate Employee Documents (image templates + birthday email)

A menu-wide audit of the Next.js **Employees** section against the legacy `hrm_menu` "Employee"
parent (plus the items legacy keeps under *Document Manager* / *Tax*) found it ~90% migrated. This
pass closed the remaining buildable gaps across four submenus. Two new legacy-vs-Next.js comparison
docs were written where none existed (`legacy/Promotion_Approval_*`, `legacy/Income_Tax_Declarations_*`),
plus `legacy/Generate_Employee_Documents_*`.

- **Promotion Approval.** Read `PromoController.php` in full. The approval cascade
  (`PUT /api/promotions/[id]`) was already faithful; added the `emp_config` audit rows legacy's
  `changeDesignation()`/`changeDepartment()`/`changeBranch()` write (`type` = `DESIG`/`DEPARTMENTS`/`BRANCH`,
  `policy_id` = the numeric `id` of the master row, prior rows of that `(type, emp_fkey)` deactivated
  first) — `chnageType()`'s equivalent block is commented out in legacy, so no `TYPE` row here
  either. Fixed the CTC insert to date `start_date_effective` to the 1st of the current month
  (legacy `promotionWage()` `date("Y-m-1")`), not "today". Added `PUT /api/promotions` to edit a
  still-pending request (guarded to `approved_status = 'N'`), an **Edit** row action, and a
  read-only **View** modal for processed rows (GET now also joins shift/leave-policy/structure/
  manager names). The manager-scoped pending queue (`approved_by = <manager emp_fkey>`) stays
  flattened to a single admin approver — same decision as Remove Employee / Regularisation.
- **Income Tax Declarations — full legacy worksheet.** `TaxController::setup()` is a projection
  worksheet on top of declare/lock/upload. Added `GET /api/employees/[id]/tax-declarations/worksheet`
  (read-only, per-block guarded): month-wise TDS deducted + gross paid from `emp_salary_slip`
  (processed/approved payroll only, FY-window bounded by joining date), projected/actual/taxable
  salary from `emp_tax_sal_trans`, the exempt-allowance breakdown, and the New-regime
  `income_tax_slab` table. The declarations `GET` now returns each Deductions head's statutory
  ceiling (`tax_heads.attr1`) plus `otherIncomeTotal` and `cappedDeductionTotal` (legacy's `$tottax`
  = Σ `min(attr1, declared)`); the page shows a "max ₹X" hint and over-cap highlight per line, and
  a "Projection Worksheet" section with a clear "no processed payroll yet" empty state. Declared
  values themselves stay stored uncapped, matching legacy.
- **Gender required on New Join (JOIN-005).** Product decision taken to match legacy. `required` +
  submit guard on `NewJoinForm.tsx` and `JoinDetail.tsx` (field-error styled), 400 "Gender is
  required" in `POST /api/employees/join` and `PUT /api/employees/join/[id]`. Import Employee's
  template column became `Gender *` and the import route now rejects rows with no Gender.
- **Generate Employee Documents — image templates + birthday email, "legacy method".** Both were
  previously deferred (legacy renders certificates via an external microservice and sends birthday
  mail via PHPMailer, and the `templates`/`templates_details` tables aren't in the schema dumps).
  Re-implemented without the external service: `scripts/create-image-templates.mjs` (idempotent
  `CREATE TABLE IF NOT EXISTS`, one-off per tenant), `document-templates/image` CRUD
  (+`is_default`-per-`type` toggle, `templates_details` appended per save + read latest), a drag
  editor tab (background image + draggable text/photo boxes, no new dep), and
  `POST /api/employees/[id]/generate-image-document` → client `<canvas>` render → PNG /
  `jsPDF`. Birthday email: added `nodemailer` + `src/lib/mailer.ts` (SMTP_* env vars, logged no-op
  when unset — same fallback pattern as `storage.ts`) and `POST /api/employees/[id]/birthday-wish`
  (mirrors `sendemailtemplate($emp_fkey, 'birthday')`; no scheduler — manual action only, legacy's
  cron path is external). `src/lib/imageTemplate.ts` holds the shared HTML builder
  (`resolveTemplateText` keeps legacy's `{{first_name}}`/`{{last_name}}` and also exposes the full
  `buildMergeTokens` set).

**Curl-verified end-to-end against real GRTL data** (temp `GRTLADMIN01` password override, restored
and confirmed via `SELECT` immediately after): schema script idempotent (2nd run reports "already
exists"); image-template create/edit(+new details row)/set-default/generate for employee 104
(merged "Well done Aarav Sharma"); `birthday-wish` returned `no-email` for an employee with no
address and `skipped-no-smtp` (nothing sent) for one with an address while SMTP unset; tax
worksheet for employee 104 returned month-wise gross + slab table with `hasPayroll: true`; tax
declarations `GET` surfaced `cap: 60000` on "Medical Insurance : Sec 80D"; promotion
create → edit → reject → (edit-after-processed correctly 409). All test rows hard-deleted
afterward. `npx tsc --noEmit` and a full `npm run build` both clean.

### 2.29 All Employees selection widget: 4 more per-employee actions (2026-09-02)

The `/employees` selection pill (`FloatingActionPanel`) already opened Manage Access / Menu
Allocation as employee-scoped modals. Added four more — **Allocate Assets**, **Income Tax
Declarations**, **Promotion Approval**, **Remove Employee** — each opening a large `<Modal>`
scoped to the selected row.

- Rather than build four bespoke modal components, each of the four route pages
  (`employees/{assets,tax-declarations,promotions,resignations}/page.tsx`) gained an optional
  `embeddedEmpPkey` (+ `embeddedEmpName`) prop — the same dual-mode pattern already used by
  `EmployeesPage embedded` in the Employee Join "All Employees" tab. In embedded mode a page
  skips its header-slot title, hides its own employee picker / search / status filters, scopes
  its list query to the employee, and locks the employee in its create/request form.
- `GET /api/employees/assets`, `/api/promotions`, `/api/resignations` gained an optional
  `emp_fkey` filter (tax uses its `[id]` routes already).
- `employees/page.tsx` lazy-imports the four pages via `next/dynamic` (keeps them out of the
  main bundle) and renders them in `max-w-[1100px]` modals. `FloatingActionPanel` now wraps
  instead of overflowing at narrow widths.
- Curl-verified the three `emp_fkey` filters against real GRTL data (scoped result sets, empty
  on no-match, unchanged shape without the param). `npx tsc --noEmit` + `npm run build` clean.
  Browser click-through of the four modals is the remaining manual check (no browser-automation
  in this environment).

---

### 2.30 Salary Increment — structure-change branch (`alterSalaryStructure`) ported (2026-09-04)

The `/payroll/increments` "Update Salary" modal already lets you select an employee, set a new
gross, and pick a salary structure — but if the picked structure differed from the employee's
current one (`salary_hike.structure_change='Y'`, which also covers first-time allocation for a
no-structure employee), **processing silently skipped that employee**: `processIncrement()` /
`processItemIncrement()` re-read `emp_proff.structure_id`, saw it didn't match the drafted target,
and `continue`d. Legacy's "Process" button chains `alterSalaryStructure` → `process`; only the
first half was missing. Batch (multi-employee) mode and the two Excel reports remain deferred.

- **New shared helper `src/lib/salaryStructureAllocate.ts`** — `allocateSalaryStructure(conn, {companyCode, userId, empFkey, structureId})`, the verified core of `bulk-policies/salary/route.ts`'s `assign()` (ctc_upload_type 2→1 reset → `sal_structure_distribution_fn` → remark-formula recompute via `evaluateArithmetic`, ESI ceil/round + Deduction sign → `emp_config` SALARY insert → `salary_structure_limit_prc`). `bulk-policies/salary/route.ts` `assign()` now delegates to it — one implementation, no drift. `SPECIAL_COMPANIES` / `ESI_DESCS` moved into the helper.
- **New export `alterSalaryStructure()` in `src/lib/increments.ts`** — port of `SalaryIncrementController::alterSalaryStructure()`. Per distinct `(emp_fkey, structure_id)` in the draft (`structure_id != 0`): resolve effective/payout dates (same fallbacks `processIncrement` uses, factored into a local `resolveEffectiveDates`), joining-date guard, `total = SUM(new_amount)` over Direct/head_fkey=1 rows, **statutory pre-check** (legacy's `arr_statuttory_fields` UNION query ported near-verbatim, parameterised — UAN/PF, ESI, LWF, PAN/TDS against `emp_details`), **wrong-salary gate** (`structure_eg_amt > total`), then for a no-current-structure + no-CTC employee an `emp_ctc_upload` seed insert (fires `emp_ctc_upload_ai` → creates `emp_ctc_transaction`), then `allocateSalaryStructure()`. Each employee runs in its own `getConnection()` transaction. Returns `{ perEmployee, missingFields, wrongSalary }`.
- **`POST /api/payroll/increments/[id]/process`** now, when `structure_change='Y'`, runs `alterSalaryStructure()` first and returns its result as `structureResult`. `processIncrement` / `processItemIncrement` are **unchanged** — once the employee is on the target structure their existing guard passes; blocked employees stay on the old structure and are skipped as before, matching legacy.
- **`/payroll/increments` page**: the View modal's "structure-change processing is not yet available" warning is replaced with a neutral note; the Process mutation surfaces `structureResult.missingFields` / `wrongSalary` in the result message so a blocked employee is visible.
- **Verified against live `mypayrol_mpm121` (GRTL) with transaction rollback** (`emp_salary_structure` / `emp_ctc_*` / `emp_config` / `emp_proff` all InnoDB, so a wrapping txn cleanly undoes stored-fn + trigger writes):
  1. **Happy path** emp 240 structure 16→22: `sal_structure_distribution_fn` returned 1, 9 new `emp_salary_structure` rows for structure 22, old structure-16 rows end-dated, `emp_config_bi` trigger set `emp_proff.structure_id=22`, one `emp_config` SALARY row, `processIncrement` guard now passes. Rollback restored structure_id 16.
  2. **Statutory block** emp 229→16 (blank `emp_details.esi`, structure 16 has ESI heads) → `"ESI No"`, `allocateSalaryStructure` skipped.
  3. **Wrong salary** `structure_eg_amt(21001) > total(15000)` → gate fires.
  4. **First-time CTC seed** emp 229 (no structure/CTC): `emp_ctc_upload` insert → `emp_ctc_upload_ai` created the `emp_ctc_transaction` row (0→1).
- `npx tsc --noEmit` clean; `eslint` clean on all touched files (one pre-existing `set-state-in-effect` error in `increments/page.tsx:228`, unrelated, left as-is); `next build` compiles + type-checks clean (Windows build-worker post-exit crash is pre-existing/environmental).
- **Follow-up fix (first real POST to `/api/payroll/increments/29/process`, 500):** the draft-dates lookup in `alterSalaryStructure()` was ported verbatim from legacy as `SELECT with_effect_from, next_increment_date, payout_month … GROUP BY emp_fkey`, which `ER_WRONG_FIELD_WITH_GROUP`s under this connection's `only_full_group_by`. Legacy just reads `$arr_shd[0]`; replaced with `ORDER BY salary_hike_details_pkey LIMIT 1`. Re-verified end-to-end on hike 29 (emp 271, first-time allocation onto structure 59): CTC seed → `sal_structure_distribution_fn`=1 → 4 structure rows → `emp_proff.structure_id=59` → `processIncrement` guard proceeds. Rolled back.

### 2.31 Salary Structure — last legacy-parity gaps closed (2026-09-04)

A fresh legacy-vs-Next.js re-read (after §2.26/§2.27 closed every SAL-0xx defect) found three remaining behaviours in `SalaryStructureController.php` not yet ported. All server-side; none affect the live GRTL tenant's data, but they make the port faithful for every tenant.

- **Duplicate-name check (legacy `checkstructureexists()`).** `structureNameTaken(db, name, excludeId)` added to `salaryStructureSave.ts` — `COUNT(*) FROM salary_structure WHERE structure_name = ? AND structure_active = 1 AND structure_id <> ?`. POST calls it with `excludeId = 0`, PUT with the row's own id; both return **409** with `"A salary structure with this name already exists"` (the form's existing error box already renders `.error` from the body, no client change). Previously two active structures could share a name silently.
- **`fixed_days` special-companies lock (`form.ctp:258,633`).** `FIXED_DAYS_LOCKED_COMPANIES` set added (legacy's `$specialCompanies` for this controller — `HRBL KWMT AIMA ESNP MBCT MRBS STCL VGNN ABSG VGFS VSFS DRRC DJIC AGNG AYRK SRTS SHYD GTRA`; **distinct** from `salaryStructureAllocate.ts`'s `SPECIAL_COMPANIES`, which is `addEmpToSallary`'s different list — comment on each says so). For a locked tenant, POST inserts `fixed_days = 30` (the column default) and PUT omits `fixed_days` from the `SET` clause entirely, matching legacy dropping the key from its save array. GRTL is not in the list, so live behaviour is unchanged.
- **`plan='basic'` head filter (`form()` / `view()`).** The `salary-head-items?buildable=1` query now reads `comp_contact_info.plan` and, when it's `basic`, adds `AND sh.plan = 'basic'` (restricts the structure builder to heads 1/4/5) — matching legacy's `if ($plan == 'basic')` branch. GRTL is `standard`, so it still sees all heads. The `head_occurance != 'LEAVE'` exclusion legacy's form also carries was **not** ported — the §2.27 six-section builder deliberately includes Leave Types, and real migrated structures (35, 59) contain leave items that must stay editable.
- **Not done:** hiding the Fixed Days input in the form UI for locked tenants (legacy `$this->set("fixed_days", ...)`) — cosmetic, needs company-code plumbing into the client form, and the server now enforces the data behaviour regardless of what the UI sends. Also still open from the earlier analysis: `prorate_code`/"Defined For" are free-text where legacy frames them as a structured Calendar/Working-days/Fixed-days choice with an auto-filled method explanation.
- `npx tsc --noEmit` and `eslint` clean on all four touched files (`salaryStructureSave.ts`, `salary-structures/route.ts`, `salary-structures/[id]/route.ts`, `salary-head-items/route.ts`). Not curl-verified against live DB this pass — pure additive guards over the §2.26/§2.27 save path that was verified then.

### 2.32 Salary Structure edit modal — Preview split out, sticky header/footer (2026-09-04)

UI-only follow-up (no API change). The Preview Breakup was an always-open section *inside* the edit form; the form modal also scrolled as one long page, so on a big structure the heading, the Modal's close button, and Save all scrolled off.

- **New `src/components/setup/SalaryStructurePreview.tsx`** — the category-grouped breakup table (subtotals, "Employer cost" tags, `formula_warning` markers, Net + employer-cost lines) lifted out of `SalaryStructureForm`. Read-only, **no Save button, no gross input**. The gross is always the structure's own saved `structure_eg_amt`; a `useQuery` (keyed on structureId + gross, `enabled: gross > 0`) hits the unchanged `/api/setup/salary-structures/[id]/breakup` endpoint. A structure with no saved gross (0) shows "nothing to compute — set one on the Edit form" instead of calling the endpoint (which requires a positive gross). Header line shows which gross the breakdown is at.
- **`SalaryStructurePanel`** — a `Calculator` icon button now sits beside the edit pencil on each row (`title="Preview breakup"`), opening `SalaryStructurePreview` in its own `max-w-2xl` `Modal`. Separate `previewTarget` state, independent of the edit modal's `modalTarget`.
- **`SalaryStructureForm` restructured to a sticky-header/body/footer layout** (same pattern as `JoinDetail`): outer `-m-6 flex flex-col max-h-[calc(90vh-3rem)]`, a `flex-shrink-0` header (`pr-14` to clear the Modal's close button) holding the "Edit/New Salary Structure" heading, a `flex-1 overflow-y-auto` body holding the header-fields card + component sections, and a `flex-shrink-0` footer holding the error box + Save Structure button. Heading, close button and Save no longer move when the body scrolls. All preview state/effects/`BreakupLine` type removed from the form; the `SAL-004` "auto-show breakdown on load" behaviour now lives in the preview modal (it computes automatically at the structure's saved gross).
- `npx tsc --noEmit` + `eslint` clean on `SalaryStructureForm.tsx`, `SalaryStructurePreview.tsx`, `SalaryStructurePanel.tsx`.

### 2.33 Salary Structure — stop SAL-021 blocking the save of migrated structures (2026-09-04)

`PUT /api/setup/salary-structures/59` ("basic only") returned 400: *`"Conveyance Allowance    " needs a formula (its calculation type requires one)`*. Root cause: the SAL-021 validation added in §2.26/§2.27 rejects any submitted detail row whose operator is `formula`/`limit`/`limit_wl`/`limit_wg` and has no formula — but **legacy's `savesalarystructuresetup()` has never validated components at all**, and ~200 rows tenant-wide (21 of 28 active structures) carry exactly that shape from the legacy→Next.js migration: operator `formula`/`limit`, `structure_det_calequation = ''`, value `0`. Legacy re-saves them verbatim (its only gate is `structure_det_value_$i != ""`, which `"0"` passes); they compute to 0 and the breakup proc's own `value<>0 OR perc<>0` cursor drops them, so they've always been invisible. The edit form resubmits every loaded row on Save, so opening any of these 21 structures and pressing Save hit the wall.

- **Fix (per explicit "do as per legacy" decision):** in `saveStructureDetails()` (`src/lib/salaryStructureSave.ts`), the `limit` branch and the `formula`/`limit_wl`/`limit_wg` branch now only run formula evaluation `if (d.formula)`. With no formula they persist the row the way legacy does — `structure_det_calequation = ''`, `structure_formula = ''`, `structure_det_value` = the client value (0), `structure_derived_perc` derived from that value alone (→ 0), `itemValues[token] = detValue`. Not deleted, not converted — kept verbatim, exactly as legacy leaves them.
- **Unchanged / still enforced:** a *non-empty* but unparseable formula still 400s (`evaluateFormulaOrThrowHelpfully`, now behind the `if (d.formula)` guard); SAL-019's "Monthly Salary Components must sum to gross" still blocks a real mismatch. No data migration — the dormant rows stay put.
- **Verified against live `mypayrol_mpm121` with transaction + ROLLBACK** (`salary_structure_details` is InnoDB): replayed structure 59's real 34-row payload exactly as `SalaryStructureForm` builds it → `saveStructureDetails` resolves with no error; all 8 empty-`formula` + 3 empty-`limit` rows round-trip as `caleq='' formula='' val=0 perc=0` (identical to their pre-existing state); the one real formula row (`Overtime Allowance(OT)`, `monthsal / 30 / 8`) still evaluates to 42. Separately confirmed a malformed non-empty formula still throws (`Unrecognized token`) and an under-sum still trips SAL-019. `npx tsc --noEmit` + `eslint` clean.

---

### 2.36 Remove Employee — drop the port-invented `Approved` status (2026-09-09)

The F&F `approve` route was stamping `resignation_requests.Resignation_status = 'Approved'` (+ `termination.is_approved = 'Y'`, `resignation_accept.isApproved = 1`) when the wizard moved from Step 1 (Review) to Step 2 (Settlement Summary). **This is not legacy behaviour.** Legacy `approves()` ([EmployeeResignationController.php:336‑640](../legacy/Controller/EmployeeResignationController.php)) writes *nothing* to any resignation/termination status — its only writes are the Pass A / Pass B formula re-evals on `emp_settle_slip` / `emp_salary_slip`. `removeemps()` (the later "Submit Termination") sets `emp_details.status = 2` and marks `emp_settle_slip`/`payroll_master`/`leave_encashment_master` settled, but also never touches `Resignation_status`. Across the whole legacy tree `resignation_requests.Resignation_status` is only ever set to `"Cancelled"`. The `Approved` value was a port invention that created a limbo: process Step 2, close the modal without generating the slip, and the row was stuck at `Approved` with no resume path.

Now aligned to legacy's implicit model — a request stays `Applied` / `HR Reviewed` until `finalize` (our `removeemps()` port) sets it `Completed`:

- **`approve/route.ts`** — removed the three status `UPDATE`s. The method is now pure settlement compute + read-back, like legacy `approves()`.
- **`finalize/route.ts`** — gate changed from `termination.is_approved = 'Y'` to *"settlement rows exist"* (`SELECT COUNT(*) FROM emp_settle_slip WHERE emp_fkey = ? AND status = 'Y'`), which is exactly what `approves()` produces and what legacy `removeemps()` implicitly assumes. The `Resignation_status = 'Completed'` idempotency guard stays (finalize still sets it).
- **`slip/route.ts`** — gate changed from `Resignation_status IN ('Approved','Completed')` to the same *"settlement rows exist"* check. Matches legacy `Viewslip()`, which works the moment `approves()` has populated the settlement.
- **`page.tsx`** — dropped the `Approved` entry from `STATUS_COLORS`. The grid's "Process Full & Final" button already keys off `Applied` / `HR Reviewed`, so a half-processed row (settlement computed, not finalized) keeps the button and can be re-run — matching legacy, where `approves()` is freely re-runnable until `removeemps()`.
- `resignation_accept.isApproved` / `termination.is_approved` are simply no longer written by the F&F flow (they belong to the separate resignation-authorization feature); nothing in the app reads them as a gate. The modal's Step 1/Step 2 switch is client-side state (`settlementResult`), unaffected.
- `npx tsc --noEmit` clean; `eslint` clean (the 3 pre-existing `react-hooks/set-state-in-effect` errors in `page.tsx`'s date auto-fill effects are the unchanged baseline); `npm run build` ✓.

---

### 2.35 Remove Employee — `approves()` procedure-parity gaps closed (2026-09-09)

Follow-up to §2.34: an object-by-object check of the backend procs/functions legacy fires on "Process Full & Final" (non-KWMT) against our `eligibility` / `preview` / `approve` routes. The two real `CALL`s — `final_settle_pay_prc` and `leave_encash_prc` — were already invoked with legacy's exact positional args (`presant_days`/`encash_days` = 0, month_year = `YYYY-MM` of `last_approved_working_date`, OUT read-back). `weekoff_days_count_fn` matched exactly. Three residual gaps, now filled — all in `rizo/src/lib/settlement.ts` + `approve/route.ts`.

- **Formula-from-`remarks` re-evaluation ported** (legacy `approves()` non-KWMT, lines 564-627). New `reevalSettleSlipFormulas()` (Pass A — `emp_settle_slip` rows with `status='Y' AND action='P'`: recompute the arithmetic string in `remarks`, overwrite `salary_amount`/`salary_rate`) and `reevalSalarySlipFormulas()` (Pass B — the `emp_salary_slip` rows of the `payroll_master_fkey` Pass A leaks: `ceil` for an `esi` head, `round` otherwise, sign-flip on `head_operator='Deduction'`). Wired into `approve` right after the `final_settle_pay_prc` CALL, before the settlement read-back — legacy's order. Legacy uses PHP `eval()`; we use the repo's existing `evaluateArithmetic()` (`salaryFormula.ts` — digits and `+ - * / ( )` only, recursive-descent, never `eval`/`Function`) and skip any row whose `remarks` aren't pure arithmetic. **Inert against current data** (0 `emp_settle_slip` rows with `action='P'` in `mypayrol_mpm121`, so Pass A no-ops and Pass B's gate never opens — exactly as legacy's own `if ($payroll_master_fkey > 0)` would); ported for faithfulness against future `final_settle_pay_prc` output.
- **`leave_balance_inthe_year_fn` 3rd arg → `NULL`** (decision: keep legacy logic). Legacy passes `fin_year.fin_year` — an **int** like `2026` — into a parameter declared `pleave_date date`. Under this deployment's `sql_mode` that raises `ER_TRUNCATED_WRONG_VALUE: Incorrect date value: '2026'` (verified). The function's own fallback is `IF pleave_date IS NULL THEN SET pleave_date = CURRENT_DATE`, so we pass `NULL` — the "current leave year" legacy intends, without the type error. Verified `fn(emp, head, NULL)` == `fn(emp, head, '2026-08-27')` for sample employees (68→1, 67→0, 272→12), so no behaviour change today; they would only differ for a resignation whose approved date falls in a different leave year than the processing date.
- `npx tsc --noEmit`, `eslint`, `npm run build` all clean.

**Update (same day, decision: "Follow the legacy logic") — the two "still open" items below are now ported verbatim rather than corrected:**

- **`commitLeaveEncashment()` is now a line-for-line port of legacy `leaveencash($emp, $applied, 0)`**, quirks and all. It signs `(connection, empFkey, branchCode, userId, applied)`. The `eligibleEncashHeads()` set is `foreach`ed but the body **always reads `heads[0]`** (legacy reads `$salary_head_items['0']` inside its loop and ignores the iterator). Each inserted `leave_encashment_master` row: `salary_head_item_fkey = 0` (literal), `encash_days = heads[0].leave_encash_limit`, `available_days = min(that, leave_balance_inthe_year_fn(emp, heads[0], NULL))`, `requested_days = approved_days = applied` (the screen tile total, **not** the per-head figure), `approved_by = '0'`, `is_approved = 'Y'`, `approved_date = creation_date = CURDATE()`; `fin_year` / `status` left to column defaults (`NULL` / `1`) — legacy's `$arr_data` omits them, so the previous `Fin_year_seq` fetch is gone. Every iteration first `DELETE`s **all** unpaid `remarks='terminate'` rows for the employee (not head-scoped) and then inserts, so for a >1-encashable-head employee only the last iteration's master row survives while `leave_encash_prc` still fires once per iteration → `emp_encash_slip` accrues an orphan row (pointing at a since-deleted master pkey) per extra head. `leave_encash_prc` is called with legacy's literal `'0'` (puser_id) and `'@msg'` (perr_msg) args. Single-encashable-head employees — the GRTL norm — are unaffected by the loop/orphan behaviour. Returns `applied`.
- **`previewEncashableLeaveBalance()` now mirrors `workingattendnacedays()`'s `leave` tile exactly** — new `encashTileHeads()` helper: `SELECT DISTINCT salary_head_item_fkey FROM leavepolicy WHERE is_leave_encash='Y' AND LEAVEPOLICY_GROUP_ID IN (emp's group)` (**no** `leave_encash_limit IS NOT NULL`, **no** `status=1` — looser than `eligibleEncashHeads()`), then `total = Σ round(leave_balance_inthe_year_fn(emp, head, NULL), 1)` — **uncapped**, no per-head floor at 0. This raw figure is what the Process Full & Final screen shows and what `approve` now forwards into `commitLeaveEncashment()` as `applied` (legacy: the `$leaves` URL arg of `approves()` → `leaveencash()`'s `$applied`). `approve/route.ts` recomputes it server-side rather than trusting a client value.
- `npx tsc --noEmit`, `eslint`, `npm run build` all clean.

### 2.34 Remove Employee — F&F wizard, slip-in-modal, and slip legacy-parity + PDF (2026-09-09)

A UI/parity pass over the "Process Full & Final" flow and the settlement slip, all against the GRTL/non-KWMT legacy path (KWMT/DEMO/GLET/ABSG branches remain out of scope). Files: `rizo/src/app/(dashboard)/employees/resignations/page.tsx`, `rizo/src/app/api/resignations/[id]/slip/route.ts`, `rizo/src/lib/settlement.ts`, new `rizo/src/lib/resignationSlipPdf.ts`.

- **F&F flow is now a 2-step wizard in one large modal** (like Employee Join): step 1 "Review" (Resignation Details + Notice Period Adjustments + Loans/Assets), step 2 "Settlement Summary" (ADDITIONS/DEDUCTIONS with column totals + editable OTHERS block). Step names, button labels (`Process Full and Final`, `Generate Full and Final Slip`), and the `approve` → `settlement-heads` calls are unchanged — visual redesign only, plus the "no Leave Year" warning banner in step 1 (`hasCurrentLeaveYear`, `fin_year` where `Year_status='OPEN' AND is_current_finyear='Y' AND vattr1=0 AND status=1`).
- **Grid "Finalize Removal" button removed.** Legacy has no grid-row finalize; `removeemps()` is the slip screen's "Submit Termination". Collapsed into step 2: **"Generate Full and Final Slip"** now runs the strong `confirm()` → `save_heads` (OTHERS edits) → `finalize` (removal cascade + `working_days_settled`/`payroll_days`) → opens the slip. `is_approved='Y'` (set in step 1) still gates finalize.
- **Slip is an in-page modal, not a page navigation.** Deleted `src/app/(print)/` entirely (its only child was the orphan `/employees/resignations/[id]/slip` page). Grid "View Slip" and the post-Generate step both open the modal off `GET /api/resignations/[id]/slip`.
- **Slip brought to `slip.ctp` / `download.ctp` parity** (`get_emp_full_and_final_settle_slip`): rows split 4 ways — SALARY additions (`type='SALARY' AND amt>=0 AND salary_head_item_fkey<>111`), SALARY deductions (`<0`), Others additions/deductions (`type<>'SALARY'` by sign); **Net Salary = Σ all four**, so the fkey-111 leave-encashment head is excluded from Net exactly as legacy does. Dropped the port-invented "Notice Pay (informational)" line (KWMT/DEMO only). Added `cleanHeadDesc()` (trailing `YYYY-MM`→`MM-YYYY`, strip "for the month…"). Particulars now match legacy order/fields: company `business_name` (`comp_contact_info`), Relieving Date = `act_last_working_day` (fallback approved LWD), Encashed Leaves = `encashed_days`, Resignation Period Present Days = `payroll_days`, Balance Working Days = `working_days_settled − payroll_days` (0 when `working_days_settled=0`). Reason for Relieving + Date of Resignation kept below a divider (decision: keep the two extras).
- **`getTerminationContext`** extended with `payrollDays`, `encashedDays`, `actLastWorkingDay`.
- **PDF export** — new `resignationSlipPdf.ts` (`generateResignationSlipPdf`, jsPDF + jspdf-autotable, `download.ctp` layout: letterhead + particulars grid + one bordered ADDITIONS|DEDUCTIONS table with an "Others" band and a Net Salary row). Modal's button is now **"Download PDF"**; the earlier `window.print()` + scoped `@media print` hack is removed.
- **`termination.encashed_days` (decision A — faithful):** a full `legacy/` tree grep shows **no PHP path writes it** — it's populated by `final_settle_pay_prc` inside the DB. Left as-is (reads 0 until that procedure completes, which currently bails on "Attendance not verified"), matching legacy. Documented in `settlement.ts`.
- **Verified** against `mypayrol_mpm121`: all columns exist; `comp_contact_info.business_name` = "GREATLEAP MPM"; the one real Completed slip (Res#5 / emp 68) → SALARY add 32796.00, SALARY ded −50.00, **Net 32746.00**; no fkey-111 or NULL-fkey SALARY rows in the sample (the `<>111` NULL-drop quirk doesn't bite here). `npx tsc --noEmit`, `eslint` (baseline `set-state-in-effect` only), and `npm run build` all clean.

---

### 2.37 Bulk Policy Allocation — Shift Allocation tab rebuilt to legacy parity (2026-09-10)

A screenshot comparison flagged that the Shift Allocation tab was still using the generic checkbox `PolicySection` (pick one shift → bulk-assign many employees, assign-only), where legacy `EmployeeConfig/index.ctp` **tab12** is a completely different screen: a **per-employee 3-panel multi-shift manager** (employee list → unallocated SHIFTS → Allocated SHIFTS) with a **primary / secondary** shift model. The generic route wrote every assignment as `emp_config(type='SHIFT', status=1)` and blind-INSERTed, so it could give one employee multiple "primary" rows and duplicate rows — breaking `attendance/shift-planner` (`primary_shift[0]`), attendance calc, and resignation eligibility. Full dissection + gap list in `legacy/Bulk_Policy_Shift_Allocation_Legacy_vs_NextJS_Comparison.md`; completion plan in `legacy/Bulk_Policy_Shift_Allocation_Migration_Plan.md`.

- **New route** `rizo/src/app/api/employees/bulk-policies/shifts/route.ts` (`userGroup===1`, all writes in one transaction):
  - `GET` → `{ employees }` — all `emp_details.status=1` + branch, no pagination cap (legacy `listemployeesforpolicy`).
  - `GET ?emp_fkey=` → `{ allocated, unallocated }` — ports `listshiftsinemployees` (primary = `status=1`, `GROUP BY day_time_seq` de-dupe) and `listshiftsforemployees` (active shifts minus the employee's `SHIFT`/`MSHIFT` rows; all active when the employee has none).
  - `POST { emp_fkey, day_time_seq }` → `addShiftToEmp`: 1st shift → `SHIFT/status=1` (primary), further shifts while a primary exists → `MSHIFT/status=2`; an existing `(emp,shift)` row is reactivated (`status`/`type` updated), never duplicated.
  - `DELETE { emp_fkey, day_time_seq }` → `removeShiftFromEmp`: soft-delete (`status=0`); removing the primary promotes the newest `MSHIFT` (`ORDER BY modification_date DESC`) to `SHIFT/status=1`, else clears `emp_proff.day_time_seq` to `0`.
- **DB triggers respected.** `emp_config_bi` sets `emp_proff.day_time_seq` on any `type='SHIFT'` INSERT; `emp_config_au` **nulls** it on any `type='SHIFT'` UPDATE. So every handler re-writes `emp_proff.day_time_seq` (to the reactivated / promoted shift, or `0`) *after* the `emp_config` write, exactly as the legacy controller does. `emp_proff` date column is `modified_date` (verified against schema).
- **New component** `rizo/src/components/employees/ShiftAllocator.tsx` — 3-panel, single-employee-at-a-time, one shift added/removed per click (matches legacy drag-one-row and the `SalaryStructureAllocator` precedent). Green **"Primary"** badge; light confirm before removing the primary.
- **Page** `rizo/src/app/(dashboard)/employees/bulk-policies/page.tsx`: `SHIFT` removed from the generic `SECTIONS` array; `{activeTab === 'SHIFT' && <ShiftAllocator/>}` added next to the `SALARY` special-case. Other 9 tabs unchanged.
- **Deviations from legacy (accepted):** the `emp_config` INSERT populates `company_code`/`branch_code` (legacy omits them; matches every other Next.js `emp_config` writer, triggers ignore them). Allocated panel de-dupes a shift that has both a `SHIFT` and `MSHIFT` row into one line.
- **Data reconciliation** — new `rizo/scripts/reconcile-shift-allocations.mjs` (dry-run default; `--apply`, `--extra=delete|demote`). Finds employees the old tab gave multiple primary rows / duplicate `(emp,shift)` rows, collapses each to one primary (survivor = greatest `emp_config.id`), re-syncs `emp_proff.day_time_seq`. **Dry run against `mypayrol_mpm121` found 1 multi-primary employee (emp 14) + 3 duplicate groups (emp 14/99/100) — not yet applied, pending review + the `--extra` decision (plan §7 Q1).**
- `npx tsc --noEmit` and `eslint` clean on all changed files. Live end-to-end verification of the add/remove/promote/reactivate SQL against a real tenant DB still pending (plan P1/P5).

---

## Phase 3: Attendance
**Status: Register, Overtime, Shift Planner, Regularisation, Edit Punches, Check-in Reports, and Comp-off all complete (2026-07-15).** `/setup/devices` — **built 2026-07-30** (§2.19): the original conclusion below (no legacy device-config screen exists) was wrong; `DeviceController.php` is real and live-routed.

- [x] `GET /api/attendance/period?month=YYYY-MM` — calls `att_start_end_fn`
- [x] `GET /api/attendance/register?month=YYYY-MM&branch=X&status=unverified|verified` — attendance grid data (FIELD1-32 + per-employee IN/OUT/duration + policy-leave color hints)
- [x] `POST /api/attendance/register/process` — calls `insert_update_att_reg`
- [x] `PUT /api/attendance/register/[registerId]/day/[dayIndex]` — single-cell edit, parameterized (legacy's `chnagestatus()` had a real SQL-injection surface via raw string concatenation — not ported)
- [x] `GET /api/attendance/register/[registerId]/leave-options` — per-employee leave codes + live balances for the edit modal
- [x] `POST /api/attendance/register/verify` — lock a month (blocks per-row on any miss-punched/blank date)
- [x] `POST /api/attendance/register/unverify` — unlock (blocks per-employee if payroll or OT already locked for that month)
- [x] `GET /api/attendance/overtime?month=YYYY-MM&branch=X&tab=not-approved|approved`
- [x] `POST /api/attendance/overtime/approve` — calls `calculate_ot_allowance_prc`
- [x] `src/components/attendance/AttendanceGrid.tsx` — calendar-grid (employees × days, color-coded, expandable IN/OUT/Duration rows)
- [x] `/attendance/register` page
- [x] `/attendance/overtime` page
- [x] `GET/POST /api/attendance/shift-planner` — per-day shift roster, locked once attendance verified
- [x] `GET/POST /api/attendance/regularisation`, `POST /api/attendance/regularisation/[id]/decide` — admin raise + approve/reject queue
- [x] `GET /api/attendance/comp-off` — read-only earned-vs-used report
- [x] `GET/POST /api/attendance/punches`, `POST /api/attendance/punches/sync`, `POST /api/attendance/punches/resync`, `PUT /api/attendance/punches/[id]` — device punch list/add/edit + device-log sync
- [x] `GET /api/attendance/checkin-reports` — daily/early-in/early-out/late-in/late-out reports
- [x] `/attendance/edit-punches` page
- [x] `/attendance/shift-planner` page
- [x] `/attendance/regularisation` page
- [x] `/attendance/checkin` page
- [x] `/attendance/comp-off` page
- [x] `/setup/devices` page — built 2026-07-30 (§2.19), ports the real `DeviceController.php` (`mypayrol_control_db.devices`). The original note here (no legacy device-config screen exists) was incorrect — corrected during a QA-report fix pass.

**Milestone reached:** Can view, edit, and process attendance for a month (Register), approve Overtime once attendance is verified, plan shift rosters, raise/approve/reject attendance regularisations, sync/edit raw device punches, view Check-in reports, view Comp-off balances, and manage punch device records. Phase 3 is functionally complete.

### 3.1 Attendance Register + Overtime build (2026-07-14)

Three background research agents mapped legacy Attendance in depth (controllers, `.ctp` views, live DB schema/procedures) before building, per this project's standing practice. Key finding that overturned a prior assumption baked into the Remove Employee eligibility gate's comment: `attendance_register` (2,033 rows) and `emp_detail_timeattandance` (77,721 rows) are **not empty** — real historical data already exists for GRTL (migrated from legacy production), so this phase started from zero code, not zero data.

**Live vs. dead controllers** (confirmed via `EmployeeMenuController.php`'s hardcoded menu routing): ported `AttendanceRegisterNewController.php` (register/grid) and `OtAttendanceNewController.php` (OT — newer superset of `OtAttendanceController.php`, edits dated into 2026). Explicitly did not port: `AttendanceController.php` (plain, not menu-routed — superseded), the older `Attendance/` EasyUI-datagrid module (same concept, older UI), `OtAttendanceController.php` (superseded), `EmpeditpunchesController.php` (thinner duplicate, deferred with the rest of Edit Punches).

**Verified live signatures before wiring any call** (per this project's schema-drift discipline — a research agent naming a proc isn't the same as its arguments being confirmed): `att_start_end_fn`, `insert_update_att_reg` (confirmed 5-arg shape `(pbranch_code, pstart_date, pend_date, puserid, OUT poutput)` — a second call path in legacy's own model helper passes a different, incompatible arg shape and is dead/broken code, not ported), `leave_transaction_prc`, `leave_balance_inthe_year_fn`, `ot_duration_register_date`, `calculate_ot_allowance_prc`.

**Real proc-call logic ported**:
- **Process month** (`insert_update_att_reg`) does all the real per-day computation (holidays/weekoffs/leave transactions/device-attendance-derived defaults) — called as-is via `CALL`, not reimplemented in TypeScript, same "trust the stored proc" precedent as `final_settle_pay_prc`.
- **Cell edit** ports `chnagestatus()`'s real logic: blocks (409) if a leave is already Applied/Authorized/Approved for that date/session (`isLeaveAlreadyApplied`); if the new status is a leave code, auto-creates+approves a `leaveentries`/`emp_leave_transactions` row via `leave_transaction_prc` — ported now per an explicit decision, even though Leave Management (Phase 4) has no UI yet, since this is a direct backend proc call (same precedent as calling `leave_encash_prc` for Remove Employee without a Leave module existing).
- **Verify/lock** ports `checkifregistercanverify` + `verifyAttendance()`: blocks per-row if any calendar day is blank ("miss-punched date"), otherwise flips `isdelete` `'Y'→'N'` and recomputes OT duration per day via `ot_duration_register_date`.
- **Un-verify** ports `removeAttendance()`'s real per-employee skip semantics (not all-or-nothing): blocked if `payroll_master.action IN ('processed','approved')` or `emp_ot_master.is_verified='Y'` already exists for that month.
- **Overtime** ports `getNotApprovedData`/`getApprovedData` (OT only listed once attendance is verified — a real cross-module dependency, expressed via an `EXISTS` subquery, not a hard block) and `approves()` (upserts `emp_ot_master.set_duration`, flips `is_verified='Y'`, calls `calculate_ot_allowance_prc`).
- `attendance_register.isdelete` is a real legacy overload (`'Y'` means both "unverified" and, in a different code path, "soft-deleted") — kept the raw column since writes hit the same live table, but exposed to the frontend as an unambiguous `status: 'unverified' | 'verified'` instead of propagating the overload.

**Real bug found and fixed during verification** (not in legacy — a bug introduced and caught in this pass): the cell-edit route initially passed a single `half` value to both the `fromhalf`/`tohalf` parameters of `leave_transaction_prc` for a full-day leave, causing the procedure to treat it as a half-day and silently overwrite `leaveentries.leave_days` from `1` to `0.5`. Caught by inspecting the row after a live test call; fixed by computing distinct `fromHalf`/`toHalf` (matching the values already used for the `leaveentries` insert itself) and passing those instead. Also hit and fixed a NOT NULL constraint miss: `leaveentries.ISAutherizedby` has no default — added it (and `APPROVEDBY`) to the insert, sourced from the acting admin's `empFkey` (falls back to `0` for a pure-admin session with no employee record).

**Curl-verified end-to-end**: read-path checked against real historical data (a real verified `2026-06`/`GRTL08` month, 3 real employees, correct color/day values with zero writes). Write-path used a disposable cloned test employee (`emp_pkey=256`, cloned from a real active employee's `emp_proff` config) against a virgin month (`2020-01`) chosen specifically because it had zero pre-existing `attendance_register` rows for the branch. Verified: process-month (confirms `insert_update_att_reg` populates all 31 days correctly for every eligible employee in the branch — 12 real employees got real rows since the proc operates per-branch, not per-employee, matching legacy exactly), single-day edit (P/P), full-day and half-day leave-code edits (confirmed correct `leave_days`/`FROMHALF`/`TOHALF` after the bug fix above, and a real `emp_leave_transactions` row posted), the already-applied blocking gate, verify/lock (confirmed `isdelete` flip and blocked-edit-after-lock), un-verify, and the OT approve flow (confirmed OT only visible after attendance verified, `set_duration`/`is_verified` updated correctly, `calculate_ot_allowance_prc` executes cleanly). **All 12 real `attendance_register` rows created by the branch-wide process call, plus all test employee data (7 tables: `emp_details`, `emp_proff`, `leaveentries`, `emp_leave_transactions`, `emp_ot_master`, and the `attendance_register` rows themselves) were deleted afterward** — the virgin month was deliberately chosen so this cleanup fully restores the branch to its pre-test state.

`npm run build`/`tsc --noEmit` clean.

### 3.2 Shift Planner, Regularisation, Comp-off, Edit Punches, Check-in Reports (2026-07-15)

Closed out the rest of the Attendance sidebar menu. A targeted research agent mapped Regularisation and Comp-off in depth (the only two submenus not already researched in §3.1's pass); Shift Planner, Edit Punches, and Check-in Reports were covered by that earlier research. Scope decisions (via AskUserQuestion): Regularisation is **admin-only** — both raising a request on an employee's behalf and approving/rejecting a queue — since no employee self-service/hierarchy-manager login exists in this app (same precedent as Remove Employee); and this pass covers **all five** remaining submenus rather than splitting further.

**Live vs. dead variants**: ported `RegularisationController::adminindexnew`/`listadminregularizationnew`/`bulkupdate(true)` (confirmed live via `AttendanceSetupController::getAttendanceFeatures()`'s routing — `adminindex`/`listadminregularization` only matter for a hardcoded 16-company blocklist irrelevant to GRTL); `CompoffController::index()` (the `MiscellaniousReports/compoff*.ctp` variant and `ScheduledBreakOffController`'s bulk-grant workflow are a different, more complex feature, not ported); `EditPunchesController` (fuller live version — `EmpeditpunchesController` is a thinner self-service duplicate, not ported); `AttendanceCheckInOutController`'s report suite.

**Real legacy bug fixed, not replicated**: `RegularisationController::bulkupdate($adminUpdate=true)` — the live admin approve/reject path — skipped the "still pending" guard entirely and its Reject branch never checked for already-processed rows, allowing an admin to re-approve or re-reject an already-decided request (silently re-running the punch-flip logic a second time). Fixed by always requiring `approved='P' AND status=1` before any transition in `POST /api/attendance/regularisation/[id]/decide`, regardless of caller — curl-verified: re-deciding an already-approved test request correctly returned a 409 ("This request has already been processed").

**Real data-convention bug found and fixed during verification** (not a legacy bug — one introduced and caught in this pass): initial `device_attandance` inserts (both the Regularisation-approve punch and the Edit Punches manual-add punch) wrote direction into the `DIRECTION`/`ATTDIRECTION` columns and a placeholder string into `C1`. Inspecting real production punch rows during testing showed the actual live convention is the reverse — `C1` holds the direction (`'in'`/`'out'`), `DIRECTION`/`ATTDIRECTION` are always `NULL`, and `C2` carries a short source tag (`'MOB'` for the mobile app in real data). Fixed both insert paths to use `C1` for direction and `C2` for a source tag (`'REG'`/`'MAN'`), and fixed the regularisation-approve route's old-punch lookup (was filtering on `DIRECTION`, which is never populated) to filter on `C1` instead.

**Comp-off**: a pure read-only report (legacy has no comp-off request/approval table at all) — "used" days counted from `attendance_register.FIELD1..32 = 'COFF'` within the open `fin_year` window, "earned" days from `emp_detail_timeattandance` (full credit for `present='P/P'` on a weekoff/holiday, half credit for `P/A`/`A/P`) — moved the earned-minus-used arithmetic server-side (legacy left it to the view template). Generalized from legacy's hardcoded "only your own record" scope to an employee-picker, matching every other admin screen in this app.

**Shift Planner**: per-day roster (primary `emp_config` SHIFT + secondary MSHIFT options, saved overrides in `emp_shift_planner`), blocked (409) from saving once attendance for that employee/month is verified — matches legacy's real lock exactly ("re-iterate attendance in Edit Attendance to reflect shift changes").

**Edit Punches**: list/add/edit raw `device_attandance` punches with an explicit, narrow request body per route (closes a real legacy bug — `savepunch()` forwarded the entire raw `$_POST` blob to `save()`/`insert_func()` with no field whitelist, letting a caller set arbitrary columns); Sync/Re-sync call the confirmed-live `device_logs_iteration_fn`/`device_logs_resync_fn` (keyed by `emp_id`, the varchar company id, not `emp_pkey`); moving a punch's `SHIFTDATE` recomputes duration via the confirmed-live `time_duration_check`.

**Check-in Reports**: Daily Attendance built from `emp_detail_timeattandance` directly; Early/Late In/Out read from the confirmed-live `emp_early_in`/`emp_early_out`/`emp_late_in`/`emp_late_out` tables (real, pre-populated report tables with no PK — not written to by this port). Avoided `get_branch_code_abs_fn` (confirmed dead in the live DB in an earlier phase) that legacy's branch-hierarchy filtering depended on — built without branch-hierarchy scoping, consistent with this project's "GRTL is a single flat-branch tenant" simplification used elsewhere.

**Curl-verified end-to-end** on a fresh disposable test employee (`emp_pkey=257`, cloned `emp_proff`/`emp_config` SHIFT config from a real active employee) against virgin months chosen to avoid touching real data: Shift Planner read + roster save; Regularisation raise → approve (confirmed real `device_attandance` insert with the corrected `C1`/`C2` convention) → the double-processing 409 guard → a separate raise → reject; Edit Punches manual add, list, `SHIFTDATE` move, Sync and Re-sync (both procs executed cleanly); Comp-off read against real inserted `COFF`/earned test rows (confirmed correct used/earned/balance counts); Check-in Reports (daily + late-in) read against real historical production data with zero writes. **All test data (`emp_details`, `emp_proff`, `emp_config`, `emp_shift_planner`, `employee_regularaization`, `device_attandance`, `device_attandance_hist`, `attendance_register`, `emp_detail_timeattandance` — 9 tables) deleted afterward**, confirmed zero remaining rows.

`tsc --noEmit` and Turbopack's compile step both pass clean; the full `next build`'s later static-page-generation phase hit a Windows OS-level worker-thread/memory resource exhaustion (likely contention with the concurrently running dev server), not a code error — re-run `npm run build` in isolation (dev server stopped) to get a clean full build if needed.

---

## Phase 4: Leave Management
**Status: COMPLETE (2026-07-21) — apply/authorize/approve/reject, Cancellation, Balances, Encashment, and Bulk Leave Upload all built and curl-verified. See §4.1 below for how this status was reconciled.**

Admin-only per the same precedent as Regularisation/Resignation (no ESS/hierarchy-manager login exists in this app) — one background research agent mapped the real live controller (`LeaveRequestController.php`, 6,173 lines — the only live one; `EmployeeLeavesController`/`EmployeeLeaveRequestController`/`LeaveapiController`/etc. confirmed dead via the real `emp_menu` table, not a hardcoded routing file) before building, then every stored-procedure signature and table/column name was independently re-verified live against `mypayrol_mpm121` (this project's standing schema-drift discipline) before wiring any call.

- [x] `GET /api/leave/types?employee=X` — leave types applicable to the employee's leave-policy group (`salary_head_items` × `leavepolicy`, corrected the research agent's `item_desc` guess to the real column `item`)
- [x] `GET /api/leave/authorizers?employee=X` — wraps `leave_auth_apr_person_fn('auth'|'apr')`, pre-fills the apply form's Authorizer/Approver pickers from the same `emp_proff.attr1`/`emp_config` type=LAPPR hierarchy data the Employee Hierarchy / Leave Hierarchy movers (§2.10) already maintain
- [x] `GET /api/leave/requests?employee=&status=` — list, joined to leave type name + employee name
- [x] `POST /api/leave/requests` — apply: validates the leave type is part of the employee's policy, blocks on an attendance-verified month or existing real attendance punches for the range (`checkAttendanceConflict`, ports `checkAttendancePunches()`), inserts `leaveentries` (`LEAVESTATUS='Applied'`), then calls `leave_transaction_prc`
- [x] `GET /api/leave/requests/[id]/balance` — wraps `leave_balance_inthe_year_fn`/`leave_balance_inthe_month_fn`, branching on the leave type's `ALLOW_NEGETIVE` flag (per decision below, informational only — does not block)
- [x] `POST /api/leave/requests/[id]/authorize` — ports the real legacy shortcut (per explicit decision, see below): if the same employee is both `ISAutherizedby` and `APPROVEDBY`, one Authorize call auto-flips approval too and jumps straight to `LEAVESTATUS='Approved'`
- [x] `POST /api/leave/requests/[id]/approve` — only from `Authorized`
- [x] `POST /api/leave/requests/[id]/reject` — from `Applied` or `Authorized`
- [x] `/leave/requests` page — single admin page (employee picker + status filter + apply modal + inline authorize/approve/reject actions), matching Regularisation's combined-page convention rather than legacy's separate My-Requests/Team-Requests split (no ESS actor to separate them for)
- [x] `/leave/balances` page + `GET /api/leave/balances?employee=X` — per-employee grid of every policy leave type with today's balance
- [x] `POST /api/leave/requests/[id]/cancel`, `.../cancellation/approve`, `.../cancellation/reject` — Cancellation sub-workflow
- [x] `GET/POST /api/leave/encashment`, `POST /api/leave/encashment/[id]/approve` (calls `leave_encash_prc`) — Encashment
- [x] `GET /api/leave/bulk-upload/template`, `POST /api/leave/bulk-upload` — Bulk Leave Upload

**Two explicit scope decisions made before building (via AskUserQuestion):**
1. **Balance-insufficiency check**: legacy's app-layer check (`if ($yearly_balance - $leave_days < 0) ...` in `grandLeave()`) is commented out/dead in the live source — decided to leave it disabled, matching live legacy exactly, rather than add a new blocking check nobody asked for. The `/balance` endpoint still surfaces the number for the admin to see.
2. **Authorizer/approver shortcut**: decided to port it (see above) rather than always requiring two clicks, since it's real, intentional legacy behavior.

**Real discovery during curl-verification** (not a legacy bug, a finding about how the stored proc behaves): `leave_transaction_prc` does not just record a transaction — it can **silently overwrite `leaveentries.LEAVESTATUS` itself**, independent of whatever status the app just wrote, when its own internal validation fails (observed live: applying leave outside a leave type's configured cycle window returned `LEAVESTATUS='Can not Apply'` and `@err='You dont have leave balance in the leave cycle with this leave type'`, even though the app had just set it to `'Applied'`). Fixed `runLeaveTransaction()` in `src/lib/leave.ts` to always re-read `LEAVESTATUS` and `@err` after the call and return the real result — every route now returns the proc's actual final status, not the status the app assumed would land. This is exactly the kind of DB-layer behavior this project's schema-drift discipline exists to catch.

**Curl-verified end-to-end** on a disposable cloned test employee (`emp_pkey=258`, cloned `emp_details`+`emp_proff` including `LEAVEPOLICY_GROUP_ID=9`/`attr1=67` from a real active employee): leave-types lookup returned the real 3 policy-linked types (Casual/Sick/Compensatory Off); authorizers lookup correctly resolved a real manager via `leave_auth_apr_person_fn`; apply outside the policy's cycle window correctly hit the proc's own real rejection (`Can not Apply`) — confirming the fix above; apply within the real cycle window succeeded (`Applied`, correct day-count); authorize → approve completed with `emp_leave_transactions` rows correctly posted and status transitions correct at each step; re-approving an already-approved request correctly 409'd (double-processing guard); reject from `Applied` completed correctly; the authorizer=approver auto-approve shortcut was confirmed live (single Authorize call jumped straight to `Approved`, both `ISAutherized`/`ISAPPROVED` flipped). All test rows across 4 tables (`emp_details`, `emp_proff`, `leaveentries`, `emp_leave_transactions`) deleted afterward, confirmed zero remaining.

To obtain an admin session for curl-verification, `mypayrol_control_db.user_credentials.password` for `GRTLADMIN01` was temporarily overridden to a known bcrypt hash and restored to its original hash immediately after establishing the session (same precedent as §2.13's live-record repair) — the original hash was captured before overwriting, so no property of the real account was lost.

`npx tsc --noEmit` and `npm run build` both clean.

**Milestone:** Core admin-driven leave workflow functional (apply → authorize → approve/reject with real balance visibility).

### 4.1 Phase completion + Bulk Leave Upload (2026-07-21)

Continuing per the reconciled `reports/migration-plan.md` sequencing (Phase 2: finish Leave Management before Payroll). On starting this pass, found that Cancellation, `/leave/balances`, and Encashment — all three items this file had listed as "deferred" — **already existed on disk**, fully implemented (`src/app/api/leave/requests/[id]/cancel`, `.../cancellation/approve`, `.../cancellation/reject`, `src/app/api/leave/balances`, `src/app/api/leave/encashment/**`, all wired into the sidebar and the `/leave/requests` page's UI). This is uncommitted work from an earlier session that was never logged here — confirmed via `git status` inside the `rizo/` submodule, which shows the entire `src/app/api/`, `src/app/(dashboard)/`, `src/lib/` tree as untracked (`??`); nothing beyond the initial `create-next-app` scaffold has ever been committed in this submodule. Worth flagging to the user: this project's work is not currently version-controlled beyond one initial backup commit.

Rather than trust stale documentation or re-build already-working code, re-verified all of it fresh against the real dev DB before marking anything complete, per this project's standing discipline:
- **Cancellation** — curl-verified the full state machine on a disposable cloned employee (`emp_pkey=260`): cancel from `Applied` → immediate `Cancelled`; cancel from `Approved` → `CancellationOfApproved` (review required); `cancellation/reject` correctly reverts to `Approved`; `cancellation/approve` correctly finalizes to `Cancelled`; double-processing guard correctly 409s a second `cancellation/approve` call on an already-`Cancelled` row.
- **Encashment** — submitted, listed as pending, approved (confirmed `leave_encash_prc` executes cleanly and `approved_days`/`approved_by`/`approved_date` populate correctly); double-approve correctly 409s.
- **Balances** — confirmed the grid returns correct per-type balances reflecting the cancellation/encashment activity above.
- All test data (`leaveentries`, `emp_leave_transactions`, `leave_encashment_master`, `emp_proff`, `emp_details`) deleted afterward.

**Built fresh this pass: Bulk Leave Upload** (`EmployeeLeaveUploadController.php` — confirmed live via `emp_menu` menu_id 39 "Leave Upload"; the sibling `EmpleaveuploadController.php` is unrouted/dead, not ported). `src/app/api/leave/bulk-upload/route.ts` + `template/route.ts`, `/leave/bulk-upload` page. Key legacy behavior deliberately carried forward: **a bulk-uploaded row is auto-approved in the same request** (`LEAVESTATUS` goes straight to `'Approved'` with `REMARKS='Leave approved from Leave Upload excel'`) — unlike the single Apply-leave form, which starts at `'Applied'` and needs a separate authorize/approve. This isn't a shortcut invented for this port; it's legacy's own explicit final step, confirmed by reading `uploadandsaveempctc()` in full.

Two real things found and *not* ported, both confirmed against source/live DB rather than assumed:
- Legacy's monthly-balance gate (`getLeaveBalanceForAuthOrApproval`) is dead code for every company except a hardcoded 11-tenant list that does not include GRTL — for everyone outside that list, `monthly_balance` is hardcoded to `0` and the balance it compares against is read from a dict key (`l_day`) the function never actually returns (the real key is `l_days`), so the check `0 - 0 < 0` can never fire. Same "matched, not invented" precedent as the already-disabled balance-insufficiency check on the single-apply path.
- `leaveentries.emp_leave_upload_fkey`, which legacy's own source writes, **does not exist** in the live schema (confirmed via `DESCRIBE`) — CakePHP silently drops unknown fields on save, so this was already a no-op in production. Only the real, working half of the link (`emp_leave_upload.leaveentry_id`) is used.

**Real bug caught and fixed during curl-verification** (introduced in this pass, not a legacy bug): the first implementation called `leave_transaction_prc` directly with status `'Approved'` to avoid what looked like a redundant second call — but live-testing showed the proc **only inserts an `emp_leave_transactions` row on an `'Applied'` call**; calling it directly with `'Approved'` silently creates no transaction row and returns no error. This is exactly why legacy's own `uploadandsaveempctc()` does the two-step (call the proc with `'Applied'` first, *then* separately flip both `leaveentries` and the just-created `emp_leave_transactions` row to `'Approved'`) rather than a single direct call — not legacy redundancy, a real proc constraint. Fixed to match legacy's two-step exactly, re-verified: transaction row now correctly lands as `Approved`.

**Curl-verified end-to-end** on a disposable cloned test employee with a real login (`emp_pkey=261`, `user_credentials.user_id='TESTBULKLVUSER'`, cloned leave-policy config from `emp_pkey=2`): uploaded a 3-row test file (1 valid, 1 unknown-user, 1 unrecognized-leave-type-code) — confirmed 1 imported / 2 correctly reported per-row errors without failing the batch; confirmed the imported row auto-approved with the correct `leaveentries`/`emp_leave_transactions` state after the fix above; confirmed the overlap-detection guard correctly rejects a re-upload of the same row on a second pass (`imported: 0`, "already exists overlapping" error). All test data across 5 tables (`emp_details`, `emp_proff`, `user_credentials`, `leaveentries`, `emp_leave_upload`, `emp_leave_transactions`) deleted afterward.

`npx tsc --noEmit` clean throughout.

---

## Phase 5: Payroll Engine
**Status: Phase 5 COMPLETE except Arrears, deferred by explicit decision (2026-07-22). 5.1 Salary Configuration, 5.2 Payroll Processing, 5.3 Taxation, Increments, Year-End, and PDF payslip export are all done.**

Background research agent mapped `SalaryHeadsController.php`/`SalaryStructureController.php` and every referenced table/proc against the live dev DB before building (this project's standing discipline) — see full findings in the session transcript; key corrections made after independently re-verifying the agent's own report (never trust research output without a live spot-check, same discipline applied to every prior phase):
- The agent claimed `salary_structure.startdate_effective`/`enddate_effective` are dead/unpopulated columns. **Wrong** — spot-checked directly: both are `NOT NULL` with real, distinct, meaningful dates on every existing row. Built as required fields, not omitted.
- Everything else in the report (table names, the real `salary_heads`/`salary_head_items` two-flag `status`/`value` gotcha, the `salary_structure` vs `salary_structure_details` schema, the real live-confirmed `structure_det_operator` values, `calculate_emp_salary_breakup`/`sal_structure_distribution_fn` signatures) was independently confirmed via `DESCRIBE`/`SHOW CREATE` before use.

### 5.1 Salary Heads (categories + items)
- [x] `GET/POST /api/setup/salary-heads`, `PUT/DELETE /api/setup/salary-heads/[id]` — category CRUD (`salary_heads`). Confirmed live: 10 rows exist, only `head_pkey` 1/4/5/9/10 are `status=1` (2/3/7/8 are dead legacy cruft, 6 is Leave Management's own category) — all 10 still listed so an admin can see/reactivate, matching legacy's own index.
- [x] `GET/POST /api/setup/salary-head-items` (scoped by `?headId=`), `PUT/DELETE /api/setup/salary-head-items/[id]` — item CRUD (`salary_head_items`). Surfaces the real, confirmed-live two-flag gotcha on the form itself: `status` (soft-delete) and `value` (Y/N — separate "included in the active salary-head set" toggle that gates Structure-builder visibility) are independent columns; a new item is useless until both are set, so the form labels `value` "Available in Structure Builder" rather than leaving it as an unexplained second on/off flag.
- [x] `GET /api/setup/salary-head-items?buildable=1` — every item eligible for the Structure builder across all live heads (`status=1 AND value='Y'` on the item, parent head active), grouped by category for the picker.
- [x] `/setup/salary-heads` page — category CRUD (`SetupCrudPage`) + a category-scoped item CRUD panel below it (same group→drill-down pattern as Holiday Groups→Holidays).

### 5.1 Salary Structure setup
- [x] `src/lib/salaryFormula.ts` — a real recursive-descent arithmetic parser/evaluator (numbers, `+ - * / ( )`, decimals, unary minus) that replaces legacy's client-side `eval()` on a hand-built string (`legacy/View/SalaryStructure/form.ctp:572`) — but **preserves legacy's exact token grammar** (space-delimited tokens; symbols; the `monthsal` keyword; `<salary_head_item_pkey>_<ItemName>` identifiers with spaces/parens/`&` replaced by `_`) since live structures already have real `structure_det_calequation` data stored in that exact format — inventing a different mechanism would have made existing formulas unreadable. No `eval()`/`Function()` anywhere; the evaluator only ever sees a string built from resolved numeric literals and the symbol set.
- [x] `GET /api/setup/salary-structures?full=1` (extended the pre-existing lightweight lookup used by 5 other screens — its original `[{structure_id,structure_name}]` shape for `structure_active=1` rows is preserved exactly, confirmed unbroken via curl), `POST /api/setup/salary-structures` — structure header CRUD (`salary_structure`).
- [x] `GET/PUT/DELETE /api/setup/salary-structures/[id]` — full detail (header + `salary_structure_details` joined to item/head names), and save. **Full replace-on-save for details** (delete-all + re-insert) — matches legacy's own `savesalarystructuresetup()` exactly (confirmed: it does the same), not a simplification introduced here; `structure_det_id` is therefore not stable across edits, by design. Formula-type rows (`formula`/`limit`/`limit_wl`/`limit_wg`) are evaluated server-side at the structure's example gross using the new safe evaluator to derive both `structure_det_value` and `structure_derived_perc` (matching legacy's `det_value*100/structure_eg_amt` derivation), wrapped in a transaction that rolls back cleanly on a formula error (curl-verified: a formula referencing an unincluded item 400s with no partial write).
- [x] `DELETE` blocks (409) if any employee currently has the structure assigned (`emp_proff.structure_id`) — matches legacy's real delete guard, curl-verified against a disposable test employee.
- [x] `GET /api/setup/salary-structures/[id]/breakup?gross=X` — wraps the real, confirmed-live `calculate_emp_salary_breakup` stored procedure (read-only preview, writes only to its own session-scoped `TEMPORARY` table internally). `Pemp_fkey` is a declared-but-unused parameter in the live proc body (confirmed via `SHOW CREATE PROCEDURE`), so `0` is passed for a structure-level preview.
- [x] `/setup/salary-structure` list page, `/setup/salary-structure/new` + `/setup/salary-structure/[id]` bespoke form (header fields + a per-component builder: item picker grouped by category, operator select, fixed-amount or formula-textarea input depending on operator, a live token cheat-sheet under each row so the admin doesn't have to hand-compute `pkey_ItemName` identifiers, and a post-save breakup-preview panel).

**Curl-verified end-to-end** against real live data: category list returns all 10 real rows with correct status; item list under head 1 returns real production items (`Basic`, `HRA`, etc.); item create/edit/soft-delete confirmed; a test structure built with a `fixed` Basic (₹6000) and a `formula` HRA (`1_Basic * . 2`, i.e. 20% of Basic) correctly persisted `structure_det_value=1200`/`structure_derived_perc=8`; the real `calculate_emp_salary_breakup` proc correctly recomputed HRA as 8% of a *different* gross on a second preview call (₹15000→₹1200, ₹30000→₹2400) while Basic stayed fixed at ₹6000 — confirming the derived-percentage mechanism round-trips correctly through the real stored procedure, not just the local evaluator; delete-guard 409 confirmed via a disposable employee assignment; formula-error rollback confirmed (bad item reference → 400, zero partial writes). The pre-existing lightweight `salary-structures` lookup (5 existing consumers: bulk-policies, employee join onboarding, new/edit employee, promotions) confirmed still returns its original shape unchanged. All test data (`salary_head_items`, `salary_structure`, `salary_structure_details`, `emp_proff`, `emp_details`) deleted afterward.

`npx tsc --noEmit` clean throughout.

### 5.2 Payroll Processing — core run/approve flow COMPLETE (2026-07-21)

Ported `PayrollController.php` (the real, live-nav'd controller — confirmed `PayrollProcessController.php` is a dead/orphaned 6-stage pipeline with no nav entry, not touched). GRTL is not in legacy's hardcoded `$specialCompanies` list, so only the main `calculate_salary_main_prc` path was ported; the parallel `salary_process_prc`/`tax_salary_process_prc` path used by ~18 special tenants is explicitly out of scope for this build. Every proc signature (`payroll_master_insert`, `calculate_salary_main_prc`, `payroll_master_approve`) and every table involved (`payroll_master`, `emp_salary_slip`, `emp_variables_upload`, `salary_processing_audit`) confirmed live via `SHOW CREATE`/`DESCRIBE` before wiring, and the exact legacy controller logic (`processpayroll()`, `approvepayroll()`, `removePayrollEntry()`, `listpayroll()`, `showsalaryslip()`) read directly from source rather than inferred, after an initial research pass's summary of `removePayrollEntry()` turned out to be subtly wrong (see note below).

- [x] `src/lib/payroll.ts` — `seedPayrollDraft`/`processPayrollEmployee`/`approvePayrollEmployee` wrap the three procs via the `CALL proc(...); SELECT @err` OUT-param pattern (same as `leave.ts`'s `runLeaveTransaction`). `monthYearToEvuFormat()` handles a real live gotcha: `payroll_master.month_year` is `'YYYY-MM'` but `emp_variables_upload.month_year` is `'MM-YYYY'` — legacy reformats between the two with a `date_format` call, replicated exactly.
- [x] `GET /api/payroll?branch=&month=&status=pending|hold|processed|approved` — mirrors `listpayroll()`/`listapprovepayroll()`/`listapprovedpayroll()`: same `emp_details`/`termination` joins for the "resigned" warning flag, same `action` state-clause branching. The pending tab additionally excludes employees whose attendance was reversed (`attendance_register.isdelete='Y'`) for that branch/month, matching legacy — confirmed this exclusion is pending-tab-only, not applied to the approve-stage listings.
- [x] `POST /api/payroll` (seed) — calls `payroll_master_insert`, which itself deletes any stale `action IS NULL` rows for the branch/month before reseeding from `attendance_register` (the real reseed/reprocess-from-scratch mechanism, confirmed live).
- [x] `POST /api/payroll/process` — toggles `tax_include` then calls `calculate_salary_main_prc` per row (resolves `emp_fkey`/`branch_code`/`month_year` server-side from the `payroll_master` row rather than trusting client-supplied parallel arrays like legacy does — safer, same net effect); backfills the `desig` snapshot column exactly as legacy does via a separate designation lookup.
- [x] `POST /api/payroll/hold` — sets `action='Hold'`.
- [x] `POST /api/payroll/approve` — calls `payroll_master_approve` per row (settles pending `emp_advance`/`emp_loan_info` EMI rows tied to the payslip), then sets `action='Approved'` in the route — matches legacy's real split (the proc itself does not set `action`; the controller does, right after).
- [x] `POST /api/payroll/reprocess` — mirrors `removePayrollEntry()`: resets `action=NULL`, closes out open `emp_salary_slip` rows (`end_date_effective=CURDATE()`), and (companies outside the special list) resets matching `emp_variables_upload` fixed-component rows to `status=0`. **Ported with no state guard, matching legacy exactly per explicit product decision**: this can reset an already-`Approved` row back to draft without undoing the advance/loan settlement `payroll_master_approve` already committed — confirmed as a real legacy behavior (not a deliberate reversal feature) and deliberately not fixed in the port, so a future "reverse approved payroll" UI action should not be assumed safe without a follow-up decision.
- [x] `GET /api/payroll/slip/[id]` — mirrors `showsalaryslip()`: computed on-the-fly from `emp_salary_slip`'s currently-active rows (`end_date_effective IS NULL`), grouped by `salary_heads.head_desc`, split into direct (visible payslip) and indirect (employer contributions) buckets by `item_part`. No dedicated payslip table exists in the live schema — confirmed this on-the-fly approach is legacy's own real behavior, not a simplification.
- [x] `/payroll/process` page — branch + month pickers (branch dropdown reuses the existing `/api/setup/branches` endpoint), Pending/On Hold tabs, checkbox multi-select, Seed/Process (with a TDS-include toggle)/Hold/Send-back-to-Pending actions, payslip link per row.
- [x] `/payroll/approve` page — Ready-to-Approve (`Processed`/`Verified`)/Approved tabs, Approve/Send-back-to-Pending actions, payslip link per row.
- [x] `/payroll/slip/[id]` page — payslip view (header + grouped direct earnings/deductions with subtotals + net salary + a separate employer-contributions panel).

**Correction found mid-build**: a first-pass background research agent summarized `removePayrollEntry()`'s `emp_variables_upload` reset as "sets status back to **1**." Re-read the live legacy source directly before wiring the reprocess route and found the real code sets `status = 0` (not 1), scoped to `head_type='fixed'` rows only, with the `MM-YYYY` month-year reformat noted above — another instance of this project's standing discipline (never trust a research agent's summary without a direct source/DB check) catching a real discrepancy before it shipped.

**Curl-verified end-to-end** against real live data (branch `GRTL01`, a clean test month `2025-07` with zero pre-existing `payroll_master` rows, real employee `emp_fkey=8`): seed → pending list populated from real `attendance_register` data → process → `action` flips to `Processed` with real computed `gross_salary`/`net_salary`/`total_deduction` (this employee had 0 working days that month, correctly zeroing the structure and matching the "resigned" flag) → payslip endpoint correctly grouped a real `WWF Employee/Employer` pair into direct/indirect buckets → reprocess correctly closed out `emp_salary_slip` rows (`end_date_effective` set) and reset `action=NULL` → re-processed → approved (`action='Approved'`, real `payroll_master_approve` call confirmed no pending advance/loan rows existed to settle) → reprocess again confirmed working even on the `Approved` row (the ported legacy gap, exactly as decided) → hold confirmed. All test `payroll_master`/`emp_salary_slip`/`salary_processing_audit` rows deleted afterward; admin session obtained via the standing temporary-password-override-on-`GRTLADMIN01` technique, hash restored exactly afterward.

`npx tsc --noEmit` clean throughout.

- [ ] `/payroll/increments` page — not started
- [ ] `/payroll/arrears` page — not started
- [ ] `/payroll/year-end` page — not started
- [ ] Salary slip PDF generator (jspdf-autotable) — not started (payslip currently HTML-only)

### 5.3 Taxation — COMPLETE (2026-07-21)

Ported the Taxation module (`TaxController.php` + `TaxHeadsController.php` + the Form-16/TDS-report pieces of `TaxReportController.php`), on top of the declaration/lock/upload workflow already built in an earlier session. Key research finding that shaped this build: the actual TDS math — old and new regime slabs, HRA exemption, surcharge/cess/rebate, marginal relief, and the monthly-TDS redistribution formula — is **already fully implemented in two live stored functions** (`tax_salary_distribution_fn`, `tax_salary_distribution_new_fn`), confirmed via `SHOW CREATE FUNCTION`. This meant the build is orchestration, not tax-law reimplementation — consistent with this project's standing rule to call stored procs rather than reimplement business logic in TypeScript. One correctly-named-but-misleading proc was ruled out: `tax_salary_process_prc` is **not** part of the TDS chain despite its name — it's the resignation-month payroll reprocessor (called only from `EmployeeResignationController`), confirmed by reading its full body (zero references to any tax function).

- [x] `src/lib/taxation.ts` — `getOpenFinYear` (reused fin-year resolution logic already used by tax-declarations), `runTaxDistribution` (calls both `tax_salary_distribution_fn`/`_new_fn` unconditionally via `SELECT fn(...) AS result`, matching legacy's `TaxController::setup()`/`Proccess()` which always computes both regimes for the side-by-side comparison regardless of the employee's actual choice), `getTaxSummary` (reads back the current open row from `emp_tax_sal_trans_sum`/`_sum_new`), `chooseTaxRegime`/`getCurrentRegime` (`Choosetax()` port: end-dates the prior open `emp_tax_regime` row, inserts the new choice).
- [x] `POST /api/employees/[id]/tax-compute` — resolves the employee's branch's open tax fin-year, runs both distribution functions, returns the comparison. `GET/POST /api/employees/[id]/tax-regime` — read/choose regime.
- [x] Extended the existing `/employees/tax-declarations` page (rather than a separate page) with a "Regime Comparison" panel: a Compute button, side-by-side old/new summary cards (taxable income, HRA exemption for old regime, standard deduction, surcharge, cess, rebate, yearly tax, monthly TDS), and a Choose button per regime.
- [x] **Explicit product decision made before building**: legacy has two inconsistent default-regime code paths when an employee has never chosen one (`EmployeeTaxController` defaults 'O', `TaxController` defaults 'N'; `emp_tax_regime` is essentially unpopulated live — 1 row total in this dev DB). Decided to default unset employees to **New regime** (matches current government default policy), not copying either legacy default blindly.
- [x] `GET/POST /api/setup/tax-types` + `[id]` — `tax_type` CRUD (4 fixed categories: Income/Deductions/Bills/Miscellaneous), soft-delete via `tax_status=0`.
- [x] `GET/POST /api/setup/tax-heads` (scoped `?taxTypeFkey=`) + `[id]` — `tax_heads` CRUD (the 22 real declaration line items — 80C, HRA, Sec-24, etc. — each with a yearly limit in `attr1`), soft-delete via `tax_active='N'`. Replicates legacy's denormalization of the parent category name onto `tax_heads.tax_type` at save time.
- [x] `GET/POST /api/setup/tax-heads-details` (scoped `?headId=`) + `[id]` — `tax_heads_details` CRUD (sub-line-items, e.g. HRA's 4 quarters, each with a per-line cap in `tax_heads_details2`), soft-delete via `active=0`.
- [x] `/setup/tax-heads` page — three-level drill-down (category → head → sub-item), same group→drill-down `SetupCrudPage` pattern as Salary Heads, extended one level deeper.
- [x] `POST /api/taxation/form16/upload` — admin batch-upload of Form-16 PDFs; each parsed for the employee's PAN via the same regex legacy uses, matched against `emp_details.pan_no`, stored in `tax_form_documents`. Uses `pdf-parse` (new dependency) in place of legacy's `Smalot\PdfParser`. **Real bug found and fixed during verification**: `pdf-parse` (via `pdfjs-dist`) dynamically imports its worker module, and Turbopack's dev bundle doesn't carry that file into its output chunk directory, causing every upload to silently fail with "Could not parse PDF" — traced by temporarily surfacing the real error (`Setting up fake worker failed: Cannot find module '...pdf.worker.mjs'`), fixed by pointing `PDFParse.setWorker()` directly at the real file on disk via an absolute `file://` path instead of the default bundled-worker resolution.
- [x] `GET /api/employees/[id]/form16` — Form-16 lookup by employee (via PAN, not a direct FK — same indirection legacy uses).
- [x] `/taxation/form16` page — admin batch upload + employee lookup/download.
- [x] `GET /api/reports/tds` + `GET /api/reports/tds/export` (Excel) — TDS report sourced from the tax summary tables, filtered by the month a summary row started being effective, optional branch filter. **Deliberately more complete than legacy here**: legacy's `TaxReportController::generatesummaryreport()` only reads `emp_tax_sal_trans_sum` (old regime), so employees on the new regime never appear in its TDS report — a real gap. This port reads both summary tables and tags each row with its regime; this is report-only (no payroll/tax computation logic changed) so the deviation from legacy was judged low-risk and not asked as a product decision. `/reports/tds` page with branch/month filter and Excel export button.
- [x] Sidebar nav: added "Form-16 Documents" and "TDS Report" under the Payroll group ("Tax Heads" and "Income Tax Declarations" nav entries already existed from earlier work, pointing at the right new routes with no changes needed).

**Bug found and fixed during verification (second one)**: the TDS report's month-range SQL used `DATE_FORMAT(?, '%Y-%m-01')`/`LAST_DAY(?)` directly on a `'YYYY-MM'` string, which MySQL can't parse as a date (`DATE_FORMAT('2026-07', ...)` silently returns `NULL`, filtering out all rows with no error). Fixed to `CONCAT(?, '-01')` / `LAST_DAY(CONCAT(?, '-01'))`. Caught because the report returned an empty list against a real employee with known live tax summary data, not because of a type error.

**Curl-verified end-to-end** against real live data (branch `GRTL01`, employee `emp_fkey=113`, real PAN `APLPK2345H`): tax-types/tax-heads/tax-heads-details full CRUD chain including the `tax_type` denormalization following a category change, and soft-delete confirmed at all three levels; `tax-compute` against a real employee produced sensible old-regime numbers (taxable income ₹11,47,500, yearly tax ₹1,56,750 with cess) confirming the live function's slab/cess math runs correctly through the new wiring; regime selection confirmed end-dating the prior open row and defaulting to New for a never-chosen employee; Form-16 upload confirmed accepting a real PDF with an embedded PAN string and correctly rejecting a non-PDF file; TDS report and Excel export confirmed returning the real computed summary rows for both regimes after the date-range fix. All test rows (`emp_tax_sal_trans_sum`/`_new`, `emp_tax_sal_trans`/`_new`, `emp_tax_regime`, `tax_form_documents`, uploaded test PDF, test `tax_types`/`tax_heads`/`tax_heads_details` rows) deleted afterward; admin session obtained via the standing temporary-password-override technique on `GRTLADMIN01`, hash restored exactly afterward.

`npx tsc --noEmit` clean throughout.

**Not ported / explicitly deferred**: `EmployeeTaxController.php` (the older, old-regime-only, superseded-in-practice controller — `TaxController.php` is the active path and covers the same ground for both regimes); `TaxsalarycomponentsController.php`'s salary-head-to-tax-component mapping setup screen (used to configure which salary components feed the tax engine — rarely touched, existing live mappings work as-is); the generic `ReportCriterias`-driven report-filter framework underlying legacy's `TaxReportController` (replaced with a purpose-built branch/month filter instead of porting the generic criteria engine, judged unnecessary complexity for a reporting-only feature).

Note from live research: EPF (12%, ₹15,000 ceiling) and PT (real Kerala slab table) are confirmed straightforward, but **ESI rate is not uniform across GRTL's own salary structures** — some are still on the pre-2019 4.75%/1.75% rates, others on 3.25%/0.75% — the payroll engine correctly reads whichever rate is on the employee's assigned structure rather than assuming one, but this is flagged as a real data-quality issue worth a product decision (should old structures be bulk-updated to current ESI rates?).

### 5.4 Increments, Year-End, PDF Payslip — COMPLETE (2026-07-22); Arrears deferred

- [x] **Salary Increments** (`SalaryIncrementController.php` port) — **scoped to the single-employee, gross-level flow** (`item='N'`, `is_multiple='N'`); legacy's per-component (`item='Y'`) increment editor and multi-employee batch mode are not ported (would require reproducing a very large dynamic per-component form for comparatively little added value). Key finding from research: increments don't regenerate the salary structure in application code — a plain `INSERT INTO emp_ctc_upload` fires a live `AFTER INSERT` trigger (`emp_ctc_upload_ai`) that itself end-dates the prior open `emp_ctc_transaction` row and calls `sal_structure_distribution_fn` — so `src/lib/increments.ts` relies on that trigger firing rather than reimplementing distribution logic in TypeScript, consistent with this project's stored-proc-is-source-of-truth discipline. `POST /api/payroll/increments` (draft, mirrors `saveIncrement()`), `POST /api/payroll/increments/[id]/process` (apply, mirrors `process()`: validates `structure_eg_amt <= gross_salary`, calls `copy_salary_structure_to_new`, inserts `emp_ctc_upload`), `GET /api/payroll/increments?status=pending|processed`, `/payroll/increments` page (draft form + pending/processed tabs).
  - **Two real schema-drift bugs found and fixed during verification** (both in `INSERT INTO emp_ctc_upload`, both caught by a live 500 rather than assumed): (1) legacy passes its own `'0000-00-00'` placeholder for an empty `payout_month`, which strict SQL mode rejects via a parameterized insert (`Incorrect date value`) — fixed by defaulting to the increment's effective month instead of replicating the placeholder. (2) `emp_ctc_upload.approved_by` is `int` live, but legacy writes the string `login_user_id` into it — MySQL's non-strict mode silently coerced this in production; our strict-mode connection rejected it (`Incorrect integer value`). Fixed by passing `NULL` rather than fabricating a numeric value for a column legacy itself never populated correctly.
  - **Curl-verified end-to-end** against a real employee (`emp_fkey=2`, real structure `19`): draft created, processed, confirmed the trigger cascade fired for real — `emp_ctc_transaction` row inserted with a live-computed `emp_derived_anualctc`, the prior open transaction row correctly end-dated, and 5 fresh `emp_new_salary_structure` rows regenerated by `copy_salary_structure_to_new`. All test rows deleted afterward, including restoring the `emp_ctc_transaction` row our test had end-dated.
- [x] **Year-End Processing** (`YearEndController.php` port) — small, fully real. **Important finding**: this tenant tracks two parallel `fin_year` cycles per branch (a calendar-year one, `vattr1=0`, and an Apr-Mar tax one, `vattr1=1` — the one Taxation's fin-year resolution already uses); `year_ending_fn` only ever processes `vattr1=0` rows, confirmed by reading its body, so the port filters on that explicitly rather than picking either cycle arbitrarily. `GET /api/payroll/year-end?branch=` (pending-leave checklist per leave policy group, mirrors `loadprocess()`), `POST /api/payroll/year-end/approve` (bulk auto-approve all pending leave in the fin-year window, mirrors `approve()`), `POST /api/payroll/year-end/process` (calls the real `year_ending_fn`, which carries forward capped leave balances into `emp_leave_balance_year` and rolls the branch's calendar-year `fin_year` row over — confirmed this rollover only happens as a side effect of the function's per-employee loop, i.e. a branch with zero employees processed would never roll over, a real detail of the live function's behavior, not a port bug). `/payroll/year-end` page with a two-step confirm on the (real, hard-to-reverse) process action.
  - **One real bug found and fixed, not replicated**: legacy's `approve()` bulk-UPDATE has no branch/employee filter at all — only a date-range condition — meaning it would auto-approve pending leave for every branch in the whole company, not just the one being year-ended. Read as an unintentional missing `WHERE` clause (not a deliberate cross-branch feature), so the port adds the branch scope legacy is clearly missing, unlike the payroll-reprocess gap which was kept exactly per an explicit user decision.
  - **Verified carefully given the destructive, real-tenant-affecting nature of `process()`**: the checklist GET was verified against real `GRTL01` data (all real leave-policy groups and real 0-pending counts returned correctly). The `process()` action itself was **not** run against `GRTL01` or any real branch — it was verified instead against a disposable throwaway branch code (`ZZTEST`) with a manually-inserted, disposable `fin_year` row and zero employees, confirming the function call succeeds without error; the fin_year row and all trace of the disposable branch were deleted afterward. Full per-employee cascade (leave-balance carry-forward, fin_year rollover) was verified by reading the function's source directly rather than exercised end-to-end against real data, since doing so would have required cloning a real employee with real leave-balance history — judged disproportionate risk for this verification step.
- [x] **PDF Payslip export** — `src/lib/payslipPdf.ts` (jsPDF + `jspdf-autotable`, replacing legacy's HTML2PDF pipeline) ports the real payslip sections found in `SalarySlipReportsController.php`'s template: company letterhead (from the already-existing `GET /api/company`), employee info grid (name incl. "(Resigned)" suffix, branch, designation, department, present/leave/LOP days, bank details), earnings/deductions table with totals, net pay, and a separate employer-contributions table — not a pixel-perfect match, but the same real data sections. Extended `GET /api/payroll/slip/[id]`'s header select to include the already-existing `payroll_master` snapshot columns (`bank_details`, `desig`, `departments`) needed for the grid — no new tables/queries required. Wired as a "Download PDF" button on the existing `/payroll/slip/[id]` page.
  - **Not visually verified** — no browser-automation tool is available in this environment. The data path was confirmed correct via the underlying API (`GET /api/payroll/slip/[id]` and `GET /api/company` both curl-verified against real approved payroll data), `npx tsc --noEmit` is clean, and the `jspdf-autotable` v5 API usage (`import { autoTable } from 'jspdf-autotable'; autoTable(doc, {...})`) matches its documented signature — but the actual rendered PDF has not been visually inspected. Flagging this explicitly rather than claiming a full end-to-end check.
- **Arrears — deferred, not built.** Research found `payroll_arrear_master`/`emp_arrear_salary_slip`/`payroll_arrear_comp_master` and three stored procedures (`arrear_salary_process_prc`, `payroll_arrear_approve`, `taxArrearProcessPrc`) that `ArrearController.php` depends on **do not exist on GRTL's live database at all** — the legacy PHP is code-complete but was apparently never deployed for this tenant (`head_type='Arrear'` does not currently appear anywhere in live `emp_salary_slip` either). Porting it for real would mean designing and deploying new DB schema/procs, not just app code — a materially different and riskier scope. Presented to the user as an explicit decision point rather than assumed; deferred by their choice. The originally-planned Sidebar `/payroll/arrears` link has been removed (was pointing at a 404) and replaced with the real Year-End link.

**Curl-verified/typechecked throughout**; `npx tsc --noEmit` clean.

**Milestone:** Phase 5 (Payroll Engine) is functionally complete for GRTL except Arrears: can seed/process/hold/approve/reverse payroll, view and PDF-export payslips, compare and choose an employee's tax regime, manage tax-heads setup, generate TDS/Form-16 documentation, run single-employee salary increments end-to-end (including the real DB-trigger cascade), and run year-end leave-balance carry-forward/fin_year rollover.

---

## Fix-First Audit & Forward Plan Reconciliation (2026-07-20)

An independent audit (`reports/current-state-report.md`, `backend-report.md`, `code-logic-report.md` + a synthesized `reports/migration-plan.md`) reviewed the built portion of the app against real legacy behavior and produced a 25-item Fix-First list, a resequenced remaining-phase plan, and 30 open product questions. `reports/migration-plan.md` is the canonical detail document going forward — this section tracks what's been resolved from it and what's still open, so future sessions don't need to re-read the full report to know current status.

### Resolved this pass

- **F1 — password-reset permanent lockout (FIXED).** Previously: an admin-initiated password reset (`/employees/access`) set `reset_login_flag='Y'` but nothing ever cleared it, so the employee could never log in again. Ported legacy's real login-time behavior (`LoginManagementComponent.php:70-105,178-216` — not the separate email-based "forgot password" flow, which legacy also has but which isn't what F1 was about): on successful password verification, if `reset_login_flag='Y'`, generate a token, store it in `user_credentials.attr1`, and redirect the user straight to a forced password-change screen instead of a dead-ending `null`. Built: `src/lib/auth.ts` (`tryAdminLogin`/`tryEmployeeLogin` throw `RESET_REQUIRED::{scope}::{companyCode}::{token}` after password verification succeeds), `src/app/api/auth/reset-password/route.ts` (validates token + current password, sets new bcrypt hash, clears `reset_login_flag`/`attr1`, single-use), `src/app/(auth)/reset-password/page.tsx` (old/new/confirm form), login page updated to catch the `RESET_REQUIRED` error and redirect. Curl-verified end-to-end against the real `GRTLADMIN01` control-DB row (temporarily overridden to a known password, restored exactly afterward, same precedent as prior sessions): wrong-old-password rejected, mismatched-confirm rejected, correct flow succeeds and clears the flag, token reuse blocked (single-use), subsequent normal login with the new password succeeds.
- **Employee status 3-value state machine (RESTORED).** Previously: the employee list's Activate/Deactivate toggle only reached `{1,2}`, making `status=0` (true legacy "inactive") unreachable and risking collision with the Resignations cluster's own writes to `status=2`. Fixed: the manual toggle now only ever writes `1` (Active) or `0` (Inactive) — `status=2` (Resigned) is no longer settable through this endpoint at all, matching legacy (which never exposed a direct toggle to that value either; only the Resignation workflow writes it). `src/app/api/employees/[id]/route.ts` PATCH now validates `{0,1}` only and returns a 400 explaining `2` is Resignation-owned. `src/app/(dashboard)/employees/page.tsx` gained a 3rd filter tab ("Inactive") and hides the toggle button entirely for resigned (`status=2`) rows. **Bug found and fixed during verification**: `src/app/api/employees/route.ts`'s list-filter used `Number(status) || 1`, which silently coerced `status=0` back to `1` (falsy-zero bug) — the new Inactive filter would have shown nothing. Fixed to `Number.isNaN(Number(status)) ? 1 : Number(status)`. Curl-verified against a disposable cloned test employee (`emp_pkey=259`, deleted afterward): toggle 1→0→(2 rejected with 400)→1, and the Inactive list filter correctly isolated the status=0 test row post-fix.
- Both changes confirmed clean via `npx tsc --noEmit`.

### Still open (tracked in `reports/migration-plan.md`, not yet actioned)

- 24 of the 25 Fix-First items beyond F1/status remain unaddressed (see report §1) — notably the login-lockout daily-reset semantics (#2), `user_access`/`emp_menu` cascade/server-enforcement (#3), weak new-employee form validation (#5), Grade's dropped delete-guard (#7), and the birthday/anniversary year-boundary date bug (#18, dormant until ~Dec 2026).
- All 30 open product questions in report §6 are unanswered except the two resolved above (status-toggle scope, F1 approach).
- The resequenced remaining-phase order (Leave completion → Payroll → Advances/Loans → Statutory → Reports-threaded-throughout → Site/Field) has not yet been adopted as the active plan — Phase 5 below still reflects the original `MIGRATION_PLAN.md` ordering. Before starting Payroll, revisit report §2's explicit gate: Payroll's exclusion logic needs the employee-status distinction now restored above, but the deeper question of whether Resignations' `status=2` write collides with anything is still unverified.
- Data Migration Plan (report §5) — cutover/rollback strategy for the eventual dual-app coexistence window — not yet needed while both apps aren't live against the same tenant simultaneously, but should be revisited before any real cutover planning starts.

### Phase 1 — Close-out dependencies (2026-07-20)

Per report §2 Phase 1 ("close out partially-built clusters' dependencies... because Phase 2+ needs them"). Two of four items done this pass; two deferred by explicit decision (AskUserQuestion).

- [x] **Bank master CRUD** — `src/app/api/setup/banks/route.ts` + `[id]/route.ts`, `/setup/banks` page (`SetupCrudPage` pattern). Confirmed the real `bank` table (`bank_name`/`bank_branch`/`ifsc_code`/`acct_no`/`status`) matches the schema dump exactly before building. Curl-verified full CRUD lifecycle against the real dev DB (one real pre-existing `HDFC Bank` row untouched); test row created/edited/soft-deleted/hard-deleted afterward.
- [x] **Contacts master CRUD** — `src/app/api/setup/contacts/route.ts` + `[id]/route.ts`, `/setup/contacts` page. `beneficiary` table **confirmed absent** from the live dev DB (`ERROR 1146: doesn't exist`) — matches the report's own flag that it might be dead; correctly skipped rather than built against a guess. `contacts` table confirmed real and already has ~30 live rows in this dev DB (some other feature already writes bare `first_name`-only rows to it — left untouched). Legacy's own `organization_id` write in `ContactsController::save()` was dropped since the live `contacts` table has no such column (another live schema-drift catch, not just a report claim). **Bug found and fixed during verification**: legacy's own add/edit form has no "Last Name" field, but the live `contacts.last_name` column is `NOT NULL` with no default — omitting it 500'd on insert (`Field 'last_name' doesn't have a default value`); added it as a required field on the new form (same precedent as the earlier Branches `NOT NULL`-column fix). Curl-verified full CRUD + search + relationship-filter against real data; test row cleaned up.
- [x] **Subscription/plan-feature gating plumbing** (no payment integration, per decision) — **found and fixed two real, previously-unnoticed bugs** while investigating the live schema (not just the report's "4 fragmented columns" framing): (1) `src/lib/features.ts` queried a `plan_feature` (singular) table that **does not exist** in the live control DB — the real join table is `plan_features` (plural); every call to `isPlanFeatureEnabled`/`getPlanFeatures` was silently unreachable (there were zero callers anywhere in the app, confirmed via grep — this plumbing was built but never wired, exactly matching the report's framing). (2) The gating key was `feature_key`, which is a **shared display category** (e.g. 9 different features all share `feature_key='Payroll'`), not a unique feature identifier — gating by it would match any feature in that category. Fixed to gate by `feature_path` (e.g. `EmployeeLoan/ctcupload`), which is real, unique, and matches legacy's own controller/action routing. Added `getPlanFeatureCatalog()` for future tile/landing-page UIs that need name/icon/description, not just a boolean. Verified the corrected SQL directly against the one real tenant with live `plan_features` data (`DEMO`, `plan_id=6`, 94 enabled feature rows) — confirmed a known-enabled path returns enabled, a nonexistent path returns not-enabled.
  - **Not wired to any consumer yet, and deliberately not forced**: `company-config.ts`'s 5 hardcoded allowlist flags (`showHierarchyDashboard`, `siteManagementEnabled`, etc.) do **not** correspond to any real row in the `features` catalog — they're tenant-behavior toggles, not menu/tile features — so migrating them to `plan_features` would mean inventing catalog rows that don't exist in production, not "wiring" real data. Left `company-config.ts` exactly as-is (already documented in its own comment as a stopgap). The other two report-named consumers (`EmployeeManageController` tile visibility, `AttendanceSetupController` tile visibility) have no corresponding UI built in this app yet — nothing to wire until those land.
  - Central finding for future sessions: of the report's "4 unreconciled plan columns," only `user_credentials.plan_id` (control DB, already populated into `session.user.planId` by `auth.ts`) is currently live and correctly linked through to real `plan_features` data — confirmed via `demoadmin`/`DEMO` (`plan_id=6`). `central_control.plan` is a disconnected free-text field (only value across all tenants: the literal typo `"standerd"`). `plan_history` (the table that should represent "client's current plan over time") is **completely empty** — no tenant has a plan assignment through it. `features.plan_id` (a second, direct link on the feature row itself, separate from the `plan_features` join table) appears vestigial/unused by anything. **GRTL (this project's primary working tenant) has no plan assigned through any of the four columns** — so any future plan-gated UI will need an explicit decision on what GRTL's effective plan should be, or gating will need a sensible "no plan = full access" fallback for ungated dev tenants.
- [ ] **`user_access`/`emp_menu` server-side enforcement** — deferred by explicit decision; left as UI-only, matching legacy's own identical gap (not a new regression). Revisit if/when Reports (§2 Phase 2+) starts shipping many new endpoints, per the report's own timing argument.
- All changes confirmed clean via `npx tsc --noEmit`.

---

## Phase 6: Reports
**Status: IN PROGRESS (2026-07-23).** Generic dynamic-criteria engine + Employee Report (5 subtypes) + Payroll Report (5 subtypes) + LOP Report complete. Remaining categories (Attendance, Statutory/ESI-EPF, Statutory Registers, Loans/Advances/Increments as their own screens, Company/Leave/Others categories) are follow-up passes using the same engine.

### Gap-closure pass (2026-07-23): live-instrumented legacy walkthrough vs. first-pass build
A user-supplied document instrumenting the actual live legacy app's JS (`in.mypayrollmaster.online`, overriding `viewReport()`/`downloadReport()`/XHR to observe real request/response behavior, not just source reading) surfaced 3 real gaps in the 2026-07-22 first pass, closed in this session:
1. **`EmployeeDetails` criteria is a multi-select checklist in legacy** (Select all/Deselect all, search box, Include Resigned toggle, one checkbox per employee "Name - EmpID") — was built as a single-employee picker. Fixed: new `EmployeeChecklist` component (`src/components/reports/EmployeeChecklist.tsx`) + `GET /api/reports/employee-options` (`getEmployeeOptions` in `src/lib/reports.ts`), wired into `CriteriaFilterPanel`. `src/lib/reports.ts`'s `CriteriaSelections` type changed from named per-field params to a generic `Record<string, string[]>` keyed by `reportcriteria` name (matching legacy's own model) so any report generator can accept any combination of the now-9 recognized criteria types (`Units`, `Departments`, `SalaryStructures`, `Designation`, `DayTimeProcedures`, `LeavePolicyGroup`, `HolidayGroup`, `Gender`, `EmployeeDetails`) without a new typed interface per report.
2. **Employee Report was missing 3 of its 5 real subtypes.** Added `shiftpolicy` (joins `working_day_time_procedures` via `emp_proff.day_time_seq`), `leavepolicy` (joins `leavepolicy_group` via `LEAVEPOLICY_GROUP_ID`), `holiday` (joins `holiday_group` via `HOLIDAY_GROUP_ID`) — all confirmed live via `SHOW CREATE`/data dumps and curl-verified (real employee 2, "Babu", resolves to "Strict Office - 0930AM - 0530PM" / "GREATLEAP Leave Policy" / "Holiday List Kerala"). The 37-field dual-list column picker for the main `employee` subtype (choose which of First/Last Name, PAN, Bank IFSC, etc. to include) is **not** built — fixed column set only, flagged as a further gap for a future pass if needed.
3. **Payroll Report had 1 of ~15 real report-type variants.** Added 4 more, the highest-value ones: `salary` (CTC Summary, from the employee's open `emp_ctc_transaction` row), `Grosssalary` (Gross Salary Detailed, with Designation/Gender/Department criteria per legacy's real `reportcriterias` data — required joining `emp_proff`/`emp_details` since `payroll_master`'s own department/designation columns are denormalized *names*, not codes, so filtering by code against them would have silently matched nothing), `BankTranfer` (Bank Transfer, from `payroll_master.bank_details`), `Salaryslip` (Salary Slip, same underlying `payroll_master` data with slip-oriented columns — not a per-row PDF, since legacy's own "Preview" for every report type in this module renders an inline HTML table, confirmed via the live walkthrough, not a document). The remaining ~10 Payroll variants (Bank Transfer_New, Monthly CTC, Gross Salary Period-wise/Summary/Detail-New, Salary Combined, Comparison, Payroll CTC, Account) are deferred.
4. **Criteria selection made mandatory**, matching legacy's real behavior (`alert("Please Choose Criteria items First")` if none selected) — per explicit decision, replicated the trigger condition but not the blocking native alert: `requireCriteria()` in `src/lib/reports.ts` throws `CriteriaRequiredError`, surfaced as a normal 400 with an inline error message on all 3 report pages instead.
- One legacy data-quality issue found and deliberately **not** modeled: the live `reportcriterias` row for `BankTranfer`'s "Banks" criteria has `reportcriteria_field='emp_pkey'` — identical to `EmployeeDetails`, not an actual bank reference. Left unmapped (ignored, per the established "unrecognized criteria are skipped" pattern) rather than building a filter against a non-existent bank linkage.
- Curl-verified end-to-end against real GRTL data (temporary password override on `GRTLADMIN01`, restored immediately after): mandatory-criteria 400 on all 3 endpoints when `criteria: {}`; employee checklist search returning real matches; all 3 new Employee Report subtypes against real employee 2 data; all 4 new Payroll Report subtypes against real 2026-03 payroll data (CTC Summary, Gross Salary with branch filter returning 20 of 23 rows, Bank Transfer, Salary Slip); LOP report unchanged behavior confirmed still correct under the new criteria shape. `npx tsc --noEmit` clean throughout.

### Second gap-closure pass (2026-07-23): live browser-instrumented migration audit
A user-supplied audit (fetch/XHR interception against the actual running Next.js app, not source reading) largely confirmed already-tracked gaps (missing Payroll subtypes, missing Employee Report field-picker, `/reports/salary-slips`/`/reports/attendance`/`/reports/esi-epf` unbuilt, Assets/Company Profile Report have no report view — only adjacent CRUD pages) but surfaced one real, previously-unnoticed bug: **all 3 report pages showed the same "Choose at least one criteria value and click Generate" placeholder text whether the report had never been run or had just been run and genuinely returned zero rows** — a real UX regression vs. legacy's explicit no-data message. Fixed on all 3 pages (`/reports/employee`, `/reports/payroll`, `/reports/lop`) by distinguishing `generate.isSuccess` (real empty result → "No records found for the selected criteria.") from the pre-generate state, and calling `generate.reset()` on subtype change so a stale success state from a previous subtype doesn't leak into a freshly-switched one. `npx tsc --noEmit` clean.

### Fourth pass (2026-07-23): 6 more Payroll Report subtypes (11 of 15 total)
Added `MonthlyCTCReport`, `PayrollCTC`, `GrosssalaryNew`, `Comparison`, `GrosssalarySummary`, `GrossPeriod` to `src/lib/reports.ts`/`/reports/payroll` — researched via a background agent reading `SalaryReportsController.php`'s real `generate<X>report()` methods (confirmed GRTL's non-special-tenant code path for each). Deliberately **not** ported: `BankTranferNew` (confirmed a cosmetic grouping variant of the already-built `BankTranfer`, same underlying data), `salarystructure`/"CTC Detail" and `SalaryCombined` (real but lower-value config-listing reports, pull from `emp_salary_structure`/`emp_variable_pay_upload`), `Account`/"Salary Account" (legacy's own Financial-Year mode is dead/commented-out code in the live source — confirmed Monthly-only regardless of the `report_type` toggle — and its Monthly mode substantially overlaps the new `PayrollCTC`).
- **Real data-quality bug found and worked around, not replicated**: `emp_salary_slip` (the row-per-salary-head-item table backing 3 of these subtypes) is versioned exactly like `emp_ctc_transaction` — every payroll reprocess/re-approve leaves the prior batch's rows in place (`end_date_effective` set) and inserts a fresh batch, rather than replacing. A naive join by `payroll_master_fkey`/`month_year` alone triples or worse counts on any reprocessed row — caught via curl-testing (`MonthlyCTCReport` initially returned 27 rows for one employee instead of the real 9). Fixed by filtering `end_date_effective IS NULL` on every `emp_salary_slip` query; re-verified correct (`PayrollCTC`'s `standard_total`/`employer_total` cross-checked by hand against the real head-by-head breakdown and confirmed to add up exactly).
- **Second real data-quality issue found and fixed**: `GrosssalarySummary`'s branch-level aggregation initially split one real branch (GRTL08) into two summary rows because `payroll_master.branch_name` (a denormalized copy) is inconsistently `NULL` on some real rows. Fixed by grouping on `branch_code` only with `MAX(branch_name)` to pick up the populated value — collapsed correctly to one row (20 employees) matching the per-employee `Grosssalary` row count for the same branch/month.
- Curl-verified all 6 against real 2026-03 GRTL payroll data end-to-end (including the two above fixes, re-tested after correction). `npx tsc --noEmit` clean.

### Fifth pass (2026-07-23): Employee Report 37-field column picker
Closed the last known Employee Report gap from the live walkthrough doc — the "Employee Information" subtype's dual-list field picker (choose which of 37 fields to include, rather than a fixed column set). `EMPLOYEE_FIELD_MAP` in `src/lib/reports.ts` maps each field to a verified-live, developer-authored SQL expression (never raw user input, so safe to interpolate — same discipline as `buildCriteriaConditions`); `generateEmployeeReport`'s `employeelist` branch builds a dynamic `SELECT` from whichever fields are requested, falling back to the original fixed column set if none are picked. New `GET /api/reports/employee-fields` (field catalog) + `src/components/reports/FieldPicker.tsx` (dual-list add/remove UI) wired into `/reports/employee`, which now also drives its result-table columns off the picker selection.
- Several fields are legacy misnomers on the DB side (`pf` holds UAN not provident fund, `emp_details.branch_name`/`branch_address` are the employee's *bank* branch not the company branch — real name collision, disambiguated in code via table alias — `classification` holds gender, `maritual_status`/`guradian` are on-disk typos) — kept as-is on the DB side per this project's standing discipline, given clean corrected labels in the UI/API layer only.
- "Area" (one of the 37 legacy fields) has no backing column anywhere in the live schema — confirmed via research, omitted from the picker rather than shipping a broken field.
- Nominee Name/Relation use correlated subqueries against `emp_family` (`is_nominee='Y'`) rather than a JOIN, to avoid row-multiplication if an employee somehow had more than one nominee row.
- Curl-verified: field catalog returns 37 fields; a picked-field report (mobile, PAN, department, vertical, bank name, nominee name/relation, UAN) returned correct real values for employee 104, including a correct `null` for Vertical (confirmed via direct DB query that no tenant data has any vertical assigned at all — real absence, not a bug); nominee fields separately verified against employee 2's real nominee row ("Jolfy K G" / "Spouse") to confirm the subquery joins actually work, not just coincidentally null. `npx tsc --noEmit` clean.

This closes out Employee Report to full functional parity (all 5 subtypes + the field picker). Remaining Phase 6 scope: Statutory Reports (ESI/EPF, Statutory Registers), Company/Leave/Others report categories, and the 4 deferred Payroll variants.

### Sixth pass (2026-07-23): Attendance Reports — all 6 real GRTL report types
`/reports/attendance`, `src/lib/attendanceReports.ts`, `POST /api/reports/attendance` (single endpoint, `type` param, mirrors the Payroll Report route's subtype-in-one-endpoint pattern). Ports `AttendanceReportsNewController.php` — **confirmed via source read to be the actual controller GRTL routes to**, not the old `AttendanceReportsController.php` (which has 3 abandoned backup files and is only reachable for a 16-company blocklist GRTL isn't in, per `ReportController.php`'s `$pathMap`/`$not_allowed_companies`). GRTL's real report-type menu (`hrreportsNew()`'s non-special-tenant branch) is 6 types, all confirmed live against `reportcriterias` and built:
- **`VerifiedAttendance`** (Attendance Register) — reads the already-populated `attendance_register` (populated by the existing Process Register action's `insert_update_att_reg` call; deliberately not re-triggered here, matching the project's established boundary that reports read rather than process).
- **`DetailedAttendance`** (Detailed Attendance Reports) — raw per-punch dump from `device_attandance` (joined to `emp_details` via the varchar `emp_id`, not `emp_pkey`).
- **`OvertimeReport`** (Overtime Reports) vs **`Overtime`** (Approved Over Time) — confirmed via source read to be genuinely different report shapes despite near-identical legacy labels: the former is day-level OT duration detail from `emp_ot_timeattandance`, the latter is the approved monthly summary from `emp_ot_master` (the same table the existing OT approval flow in `src/app/api/attendance/overtime/approve` already writes to).
- **`Dashboard`** (Employee Check-in/out logs) — the one report of this batch that's a real date range rather than single-month, confirmed via source.
- **`regularisation`** (Attendance Regularisation) — reads `employee_regularaization` (real on-disk misspelling), the same table the existing Attendance > Regularisation admin flow already uses.
- Deliberately **not** ported: the special-tenant-only types (`Attendance`/"Attendance Register_New" default, `movements`, `LOP`, `nonpunched`) gated to a 7-company list GRTL isn't in; and dead code flagged by research (`TimeAttendance`, `AttendanceRep`, `MobilelocationRep`, `employee` types are fully implemented in the controller but absent from GRTL's — or any tenant's — actual menu array).
- Exported `requireCriteria`/`buildCriteriaConditions` from `src/lib/reports.ts` (were module-private) so this new module reuses the same parameterized-filter engine rather than duplicating it.
- Curl-verified all 6 against real GRTL data: Attendance Register (25 present/1 leave/5 week-offs for a known employee/month), Detailed Attendance (real in/out punch pairs), Overtime Reports (23 real day-level OT rows for Nov 2025), Approved Overtime (1 real approved June 2026 row, 195 min), Check-in/out logs (real punch pairs over a date range), Regularisation (19 real pending/approved requests for June 2026). `npx tsc --noEmit` clean.

### Seventh pass (2026-07-23): Statutory Reports — EPF, ESI, Professional Tax, Wage Sheet
`/reports/statutory` (replaces the previously-dead `/reports/statutory/esi-epf` Sidebar link), `src/lib/statutoryReports.ts`, `POST /api/reports/statutory`. Confirmed via research that `StatutoryReportController.php` (857KB) is the real, live-routed controller — `statutoryReportsController.php` (lowercase-plural, 49KB) is dead/unrouted code, same "near-identical name, one is stale" pattern this project has hit repeatedly elsewhere in the Reports module.
- **Deliberate, real simplification vs. legacy for EPF/ESI**: legacy's `EsiEpfReportController` computes a "PF-eligible base" by `eval()`-ing a pure-arithmetic string (e.g. `"6500*.12"`) stored in `emp_salary_slip.remarks` at report time — confirmed via source read that payroll's own write path already gates this string with a numeric-only regex before storing it, so it's never a symbolic formula, just pre-computed arithmetic. Rather than port a string-eval step (an unnecessary mini-interpreter for something with a direct SQL answer, and exactly the kind of thing this project avoids per its no-`eval` decision), EPF/ESI reports read the real contribution amounts directly from `emp_salary_slip`, matched by `salary_head_items.item` text (4 real, confirmed rows: EPF/ESI × Employee/Employer Contribution, head_fkey 5=Employee/4=Employer) — cleaner and more precise than legacy's own fragile `LIKE '%pf%'` matching against a noisier set of head descriptions.
- **Professional Tax** (`ProfTax`) — legacy presents this as a "1st/2nd half of financial year" toggle computed via PHP date math off the open `fin_year` row; simplified to a plain from/to month range (same UX pattern as the already-built Gross Salary Period-Wise report) rather than replicating the half-year toggle UI. Reads the real `Professional Tax` salary head (already computed by payroll processing, not re-derived).
- **Wage Sheet** (`wage`, from `StatutoryRegistersController::Generatewage()`) — Direct-part-only salary head breakdown per employee, matching legacy's `item_part='Direct'` filter (confirmed real values: `Direct`/`Indirect`/`Admin Only`).
- **Deliberately not ported** (per research, all GRTL-irrelevant): `EsiEpfReportController`'s e-filing "Statutory Upload" screen (generates government EPF/ESI e-filing files — a different feature shape than a tabular report, out of scope for this pass) and its `wps_template` (Gulf/Middle-East payroll compliance, irrelevant for an Indian company); ~2000 lines of SYNTHIET-tenant-branded EPF/ESI variants in `StatutoryReportController`; the KWMT/GLET/DEMO-only Professional Tax variant (GRTL uses the plain path already built); `Musterroll`/`ServiceRecord` registers and `12BB`.
- **Real data-quality bug found and fixed in the shared engine, not just this report**: the live `reportcriterias` table has a duplicate `EmployeeDetails` row for reporttype `'ESI'` — would have shown "belonging to a Employee" twice in the Add Criteria dropdown. Fixed with `SELECT DISTINCT` in `getActiveCriteria` (`src/lib/reports.ts`), benefiting every report screen, not just this one.
- Curl-verified all 4 against real GRTL data: EPF (750/750 employee/employer, matching the exact figures independently confirmed via the Payroll CTC report earlier), ESI (79/488), Professional Tax (a real 2019 historical deduction, ₹313), Wage Sheet (correctly Direct-only: Basic/HRA/Conveyance additions + EPF/ESI/WWF employee deductions, employer contributions correctly excluded as Indirect). `npx tsc --noEmit` clean.

### Eighth pass (2026-07-23): Muster Roll — 3 of 4 real Statutory Registers types now built
Added `Musterroll` to `src/lib/statutoryReports.ts`/`/reports/statutory` — a labour-law compliance register: per-employee day-by-day attendance (`attendance_register`'s `FIELD1..FIELD31` columns) plus its precomputed summary totals and Guardian/Termination-date compliance fields. Deliberately does **not** replicate legacy's side-effecting `insert_update_att_reg` call at report-generation time — reads the already-populated register, matching the same read-don't-process boundary already established for the Attendance Register report. `ServiceRecord` (the 4th type) remains deferred: research confirmed it's a genuinely different UI shape — a Form-BB-style single-employee printable form (key/value fields, not a results table), built from raw SQL against a previously-unseen `tax_salary_components` table not yet verified live — lower value relative to effort than the tabular reports built so far.
- Curl-verified against real data (employee 104, March 2026): summary totals match the exact figures independently confirmed earlier via the Attendance Register report (25 present, 1 leave, 5 week-offs), day-by-day detail (`WO`, `CL/CL`, `P/P`, ...) confirmed real. `npx tsc --noEmit` clean.

### Ninth pass (2026-07-23): Employee Loan Report + Employee Advance Report
`/reports/loans-advances`, `src/lib/loanAdvanceReports.ts`, `POST /api/reports/loans-advances`. Ports `EmployeeLoanReportsController::generateemployeeeloan()` and `EmployeeAdvanceReportsController::generateemployeeadvance()` — both confirmed via research to be single-report-type screens (not the usual multi-subtype dispatcher shape). Reused Phase 7's already-established `emp_loan`/`emp_loan_info`/`emp_advance` schema knowledge directly, no re-derivation needed there.
- **Loan Report**: filters by `emp_loan.created_date` (loan *creation* date, not the EMI schedule) over a from/to month range; `balance_amount` = `loan_amount − SUM(amount_paid)` — legacy computes the identical figure but via an overloaded/misleadingly-named PHP array key (`remarks`, which despite the name actually holds the paid-sum, not the real `emp_loan.remarks` text) — given a clearly-named column here instead. Toggles: include resigned, include completed loans (legacy defaults to active-only).
- **Advance Report**: single-month filter on `affected_month` (legacy offers a "to" field in the UI that's read but never actually applied to the query — confirmed via source, not replicated here since it'd be dead UI); always excludes already-credited advances, matching legacy's hardcoded (non-toggleable) behavior.
- **`get_branch_code_abs_fn`/`get_directors_branch_code`**: legacy's criteria-dropdown population (not the report queries themselves) calls these for branch-hierarchy scoping, but confirmed via source to be gated to company codes `GLET`/`ABSG`/`GAAR`/`HRBL` only — GRTL is none of these, so the standard plain `branch_code = ?` filter (this project's existing Units-criteria mapping) is a confirmed-safe substitute for GRTL, not a guessed approximation.
- Curl-verified against real historical data (2019-2020 test employees, since that's where real loan/advance rows exist): Loan report's EMI-paid/balance math cross-checked by hand (e.g. ₹5,000 loan − ₹2,370 paid = ₹2,630 balance, correct), `includeCompleted` correctly surfacing a closed loan alongside active ones; Advance report returning real January-2020 advances with correct employee/amount data. `npx tsc --noEmit` clean.

### Follow-up (2026-07-27): audited all remaining Payroll Report subtypes vs. legacy, found the same class of gap
After fixing Salary Slip's format mismatch, user asked to check the other 10 Payroll Report subtypes against legacy on the same three axes (view/Excel/PDF). Delegated a background research pass reading the real GRTL-path functions in `SalaryReportsController.php` (`GenerateSummaryPayrolreport`, `generatesalaryreport`, `GenerateSalaryGrossNonExemted`, `GenerateSalaryGrossNewNotExempted`, `GenerateSalaryGrossSummary`, `GenerateSalaryGrossPeriod`, `generateBanktransferreport`, `generateEmpMonthlyCTCReport`, `GenerateSalaryComparison`, `generatePayrollCTC` — confirmed these are the actual GRTL-routed functions, not the PSQUARE/SYNTHITE/HEDGE/KWMT tenant-specific duplicates that also exist in this 45,000-line controller).
- **Finding**: all 10 are branch-grouped in legacy across all three modes — a separate `<table>`/PHPExcel block per branch, each with its own header and a "Total"/"Total Deduction" row, not one flat table. `BankTranfer` specifically groups by **bank name** (parsed off the bank details), not branch. `GrosssalarySummary` is the one exception — it's already a per-branch aggregate report (one row = one branch's totals), so no further grouping applies. Several also have deeper structure not addressed in this pass (multi-level merged super-headers like "Standard Salary"/"Actual Salary" spanning several sub-columns, e.g. `Grosssalary`/`GrosssalaryNew`/`Comparison`) — flagged, not built.
- **Scope decision (explicit, per user)**: fix the on-screen HTML view first, across all 10 subtypes; Excel/PDF export for these 10 still produce a flat sheet/table for now — a known, tracked gap, not silently left unaddressed.
- **Fixed**: `/reports/payroll` now renders each of these 10 subtypes as a series of per-group blocks (branch, or bank for `BankTranfer`) — group header bar, that group's rows in their own table, a Total row summing the subtype's currency columns — instead of one flat grid. Implemented client-side (`groupRows()`/`sumColumn()` in the page component; each subtype's `SUBTYPE_META` entry gained a `groupBy: (row) => string` function) since no backend query shape changed — grouping is a display concern over the same already-correct row data.
- Curl-verified real data still resolves the expected groups: `SummaryPayroll` filtered to GRTL02+GRTL08 returns exactly `{Banglore, Head Office}`; `BankTranfer` sample data confirmed the `bank_details` snapshot's first comma-separated field really is the bank name (`"HDFC,Chennai Branch,..."`) including a real empty-string edge case (falls back to "Unknown Bank" group, not a crash). `npx tsc --noEmit` clean; `/reports/payroll` confirmed loading (200) under a real authenticated session.

### Follow-up (2026-07-27): extended Excel/PDF export to match the branch-grouped view
User said "Continue" — extended the same grouping to Excel/PDF export for the 10 subtypes, closing the gap left open in the previous pass.
- Added `exportGroupedReportToExcel`/`exportGroupedReportToPdf` to `reportExport.ts` — generic, reused across all 10 subtypes rather than one function per subtype: a bold, shaded group-header row (Excel) / group-name line (PDF) per branch/bank, that group's rows, and a bold Total row summing whichever columns are in the existing `CURRENCY_KEYS` set. Excel via `exceljs` (already added for Salary Slip's export — needed here too for the bold/shaded header styling, which the `xlsx`/SheetJS package still can't do). PDF chains `autoTable` calls per group, tracking `lastAutoTable.finalY` to stack sections down the page, with a `didParseCell` hook to bold the Total row (the last body row of each group's table).
- `/reports/payroll`'s Excel/PDF buttons now call the grouped exporters whenever `meta.groupBy` is set (all 10 subtypes), passing the same `groupRows()` output the on-screen view already renders — falls through to the original flat exporters for `GrosssalarySummary` (already per-branch aggregate) and `Salaryslip` (its own dedicated payslip exporters).
- **Still not replicated** (documented, not silently dropped): legacy's deeper per-subtype structure beyond branch/bank grouping — multi-level merged super-headers like "Standard Salary"/"Actual Salary" spanning several sub-columns on `Grosssalary`/`GrosssalaryNew`/`Comparison`. The grouping-with-totals structure (the main gap) is now matched in all 3 modes; the finer per-subtype header nesting remains a lower-value follow-up.
- Verified: `npx tsc --noEmit` clean; both new export functions' actual generation logic (ExcelJS cell/merge/fill styling, jsPDF `autoTable` chaining across multiple grouped tables with the bold-total-row hook) smoke-tested directly in Node against representative grouped data, since this environment can't drive a real browser download.

### Follow-up (2026-07-27): PDF restricted to legacy's real subset + full column-level audit and fix pass
User named the exact 6 Payroll Report subtypes legacy offers PDF for (Salary Account, Salary Bank Transfer, Salary Bank Transfer_New, CTC Detail, Salary Slip, Gross Salary Summary) and separately said "I am finding many missing columns" comparing against real legacy reports.
- **PDF restricted**: added `pdfAllowed` to `SUBTYPE_META`, true only for the 3 of those 6 that are built (`BankTranfer`, `Salaryslip`, `GrosssalarySummary`) — `Account`/`BankTranferNew`/`salarystructure` ("CTC Detail") remain deferred/unbuilt, flag is ready to flip on when they are. Every other subtype now only offers Excel, matching legacy's real UI.
- **Column audit**: delegated a background pass reading each of the 10 subtypes' real `.ctp` view `<th>` headers against our current column lists. Found a consistent cross-cutting pattern — **Employee ID** (`emp_company_id`, the real human-readable ID, distinct from name) missing from nearly every report, plus systematically dropped **lifecycle dates** (Joining Date, Termination Date) and **attendance columns** (Present Days, LOP Days, Leave Days, Week Off, Holiday, Overtime) on the detail reports, and Gender missing on `GrossPeriod`/`PayrollCTC`. Also caught two double-checked-against-our-own-code corrections to the audit's own claims before acting on them (it flagged Department/Designation as missing from `GrosssalaryNew` when they were already present) — a reminder to verify an agent's findings against ground truth already in context, not apply every claim blindly.
- **Two structural findings, not just missing columns** (both actioned per explicit user decision):
  1. **`GrosssalarySummary` was the wrong shape entirely.** Legacy's real report is an employee-level detail list grouped by branch — same row granularity as `Grosssalary` — not the branch-aggregate totals report we'd built. Rebuilt the query and columns from scratch to match (was previously `GROUP BY branch_code` with `COUNT`/`SUM`; now one row per employee, `groupBy` added to the frontend meta so it renders exactly like the other grouped reports).
  2. **`Comparison` was missing ~9 of ~10 legacy comparison metric groups** — legacy compares CTC, Gross Salary (Standard/Actual), per-salary-head amounts, Pay Days, Variable Additions/Deductions counts, and Bank Account, each as prev/current/diff triplets; ours only had Net Salary. Rebuilt to add CTC, Gross Salary, Total Deduction, and Pay Days as full prev/current/diff triplets (5 metric groups total, up from 1) plus identity columns (Employee ID, Department, Designation, Joining/Termination Date, Status). **Honest scope note, not silently under-delivered**: the per-salary-head dynamic comparison columns (a pivoted column per distinct salary head company-wide) and the Variable Additions/Deductions Count / Bank Account comparisons were NOT built — those need a materially different dynamic-column/pivot architecture (comparable to the Employee Report's 37-field picker, but transposed), not a same-shape column addition. Documented as a real, larger follow-up.
- **Fixed across the other 7 subtypes** (`SummaryPayroll`, `salary`/CTC Summary, `Grosssalary`, `BankTranfer`, `GrosssalaryNew`, `GrossPeriod`, `MonthlyCTCReport`, `PayrollCTC`): added Employee ID everywhere; Joining Date/Termination Date everywhere identity columns already existed; attendance columns (Present/Leave/LOP/Week Off/Holiday, plus Overtime via `emp_ot_master` where the audit specifically flagged it) on the per-employee detail reports; Gender on `GrossPeriod`/`PayrollCTC`. `BankTranfer`'s single glob `bank_details` string was also split into 4 real structured columns (Bank Name/Bank Branch/IFSC/Account Number, same fallback-to-`emp_details` logic already used by `generateSalarySlips`) — legacy shows these as separate columns, not one concatenated field. Settlement Amount (flagged missing on 6 of the 10 reports) was deliberately NOT added — it needs `emp_settle_slip` wired into this whole report module, a separate lift not part of the approved column set for this pass.
- Curl-verified all 9 modified subtypes against real May 2026 GRTL data: Employee ID/joining dates/attendance columns all populated correctly; `BankTranfer`'s split fields matched the known raw `bank_details` string exactly (`"HDFC,Chennai Branch,HDFC000123,8349201834"` → 4 correct fields); `GrosssalarySummary` confirmed now returning employee-level rows instead of branch aggregates; `Comparison`'s null `previous_*` fields for one employee cross-checked against `payroll_master` directly and confirmed genuinely correct (no April payroll row exists for that employee, not a bug). `npx tsc --noEmit` clean throughout.

### Follow-up (2026-07-27): item-wise (per-salary-head) pivot columns
User flagged that legacy pivots each employee's real salary-head items (Basic/HRA/Conveyance/etc — varies per company, not a fixed set) into their own columns on several reports; the previous pass only had fixed aggregate totals (Standard/Variable/Employer/Other). Scoped to all 4 flagged reports per explicit user decision (`Grosssalary`, `GrosssalaryNew`, `PayrollCTC` — plain per-item pivot; `Comparison` — the harder two-month prev/current/diff-per-item version).
- **Backend** (`reports.ts`): new shared `getItemWiseAdditions(pool, payrollMasterPkeys)` fetches the real Addition-side `emp_salary_slip` line items (same source the existing aggregate totals already draw from via `head_pkey` category, just at per-item grain instead of summed) and attaches an `items: {label,amount,rate}[]` array to each row for the 3 plain-pivot subtypes. `Comparison` fetches items for both the current and previous month's `payroll_master_pkey`, then merges by label into `items: {label,current,previous,change}[]` — the union of whichever heads either month had, not just the intersection (an employee who gained/lost a salary head between months still shows correctly, with 0 on the missing side).
- **Deliberate simplification, documented not hidden**: only the "Actual Salary" (this month's real processed amounts) pivot is built — legacy's HTML also shows a parallel "Standard Salary" (structure-value) pivot from a different source (`emp_ctc_transaction`/`emp_salary_structure`), which would double the column count for a lower-value planned-vs-actual distinction. Not built.
- **Frontend** (`/reports/payroll`): since the real salary-head set isn't known ahead of time (varies per company/employee, unlike every other column which has a fixed key), added `flattenItemColumns()` — flattens each row's dynamic `items` array into synthetic per-label keys (`item__<label>` for the plain pivot, `item__<label>__previous/current/change` for Comparison) computed via `useMemo`, so the existing grouping/Total-row/Excel/PDF-export machinery treats them like any other column with zero special-casing elsewhere. `CURRENCY_KEYS` extended per-render with the dynamic item keys so they format as currency too.
- Curl-verified against real data: `Grosssalary`/`PayrollCTC` for employee 93 both correctly returned the real Basic/HRA/Conveyance/DA item breakdown; `Comparison` for an employee with genuine April+May payroll (emp 80) correctly merged both months per head — 3 unchanged items (Basic/HRA/Conveyance, `change: 0`) plus a real ₹150 deduction change reflected correctly in the aggregate columns, confirming the per-item merge-by-label logic works on real two-month data, not just the null-previous-month edge case. `npx tsc --noEmit` clean throughout.

### Follow-up (2026-07-27): Salary Slip Excel export matched to legacy's real PHPExcel output + criteria multi-select made explicit
User asked to check the Salary Slip Excel export against legacy's actual PHPExcel code, and separately asked for the criteria value-pickers (Units/Departments/Designation/etc.) to allow multi-select.
- **Excel export rebuilt to match `SalaryReportsController.php`'s `case 'excel':` block field-for-field/style-for-style**: read the real PHPExcel code (not the on-screen `.ctp` view — Excel and HTML are genuinely different layouts in legacy) — a **single worksheet** (not one sheet per employee, which the first pass used) with every employee's block stacked down the same sheet: title rows ("Salary Slip Report" / company code / "Month - <Month Year>", merged A:D, centered), then per employee a bold size-14 header, a bold key:value details grid using legacy's exact labels and pairing (e.g. "Loss off Pay :" is paired with "Week Off :", not with something logically related — replicated as-is), a Salary/Rate/Amount list (earnings, an unconditional Total row, deductions, a Total row **only if any deduction rows exist** — matching a real conditional in the legacy code), a bold Net Salary row, and a thick outline border drawn around each employee's whole block (legacy's `$BStyle`).
- **Switched Excel libraries for this one export**: the `xlsx`/SheetJS package used by every other export in this module is the free Community Edition, which has **no cell-styling API at all** — bold fonts, borders, and merged-cell alignment are a paid SheetJS Pro feature, so it's structurally incapable of reproducing PHPExcel's formatting. Added `exceljs` (free, full styling support) for `exportSalarySlipsToExcel` specifically; every other report's export is unaffected.
- **Added 3 fields to `generateSalarySlips()`** that legacy's Excel export uses but the first pass hadn't wired: `employee_id` (`emp_proff.emp_company_id`), `login_user_id` (`user_credentials.user_id` — confirmed this table is also mirrored per-company in the company DB, not only the control DB), `termination_date` (`termination.last_approved_working_date`, status=1). Also added `rate` (`emp_salary_slip.structure_det_value`) to each earnings/deductions line item — legacy's Excel shows a Salary/**Rate**/Amount table, not just Amount.
- **Deliberately not replicated**: legacy's conditional "Settlement Amount" row for resigned (status=2) employees, which reads from `emp_settle_slip` — not wired into this report (a different, not-yet-built area of the migration); fabricating a zero would be worse than omitting the row.
- Verified: ExcelJS output smoke-tested directly (workbook builds, buffer writes, `Object.assign`-based cell styling and border-application logic all run without error) since this environment can't drive a real browser download; curl-verified the 3 new fields + `rate` on real employee 93 data (`employee_id: "GRTL1000102"`, EPF rate `1800` matching `structure_det_value`). `npx tsc --noEmit` clean.
- **Criteria multi-select made explicit**: `CriteriaOptionSelect` (used for Units/Departments/SalaryStructures/Designation/DayTimeProcedures/LeavePolicyGroup/HolidayGroup/Gender) was a native `<select multiple>` — technically already multi-selectable via ctrl/cmd-click, but that affordance is easy to miss and reads as single-select. Replaced with a checkbox-dropdown (open/close button showing "N selected", Select all/Clear, one checkbox per option), matching the pattern `EmployeeChecklist` already used for the Employee criteria. No backend change — still lands in the same `Record<string,string[]>` shape. `npx tsc --noEmit` clean; `/reports/payroll` and the criteria-options endpoint both confirmed loading (200) under a real authenticated session.

### Bug fix (2026-07-27): Salary Slip rendered as a generic grid instead of a payslip document
User directly flagged (checked the live Payroll Report screen against legacy): "Salary Slip report is like all the other reports... in legacy each report has their own format." Confirmed against `View/SalaryReports/salaryslip.ctp` — legacy's Salary Slip is a formatted per-employee payslip document (name/designation/branch header, a details grid of leave/present/LOP/weekoff/holiday days + PF/ESI/UAN/bank info, then a two-column Earnings/Deductions table with Total Earnings/Total Deductions/Net Pay), not a tabular report like every other Payroll Report subtype. The first port's `Salaryslip` branch returned a flat `payroll_master` summary row (`gross_salary`/`total_deduction`/`net_salary`/`bank_details`) and rendered it through the same generic multi-column grid as `SummaryPayroll`/`Grosssalary`/etc — a real design gap, not cosmetic.
- Fixed: `generateSalarySlips()` in `src/lib/reports.ts` now returns a structured `SalarySlip[]` (per-employee header fields + `earnings`/`deductions` line-item arrays sourced from `emp_salary_slip`, item_part='Direct', ordered by `salary_head_items.salary_head_item_order1`) instead of a flat row. `/reports/payroll`'s frontend now branches on `subtype === 'Salaryslip'` and renders a dedicated `SalarySlipCard` per employee (payslip layout) instead of the shared grid; Excel/PDF export buttons are hidden for this subtype (a batch tabular export doesn't fit a per-employee document — deferred, not built).
- Bank details replicate legacy's real fallback: `payroll_master.bank_details` (a comma-separated snapshot taken at payroll-process time) is used first, falling back to the employee's *current* `emp_details` bank fields (`bank_name`/`branch_name`/`ifsc_code`/`account_no` — `branch_name` here is the employee's bank branch, the known name-collision documented elsewhere in this project, not the company branch) only if the snapshot is blank.
- Curl-verified against real 2026-05 GRTL data: employee 93's payslip (Basic/HRA/Conveyance/DA earnings totalling ₹28,000; WWF/EPF/ESI deductions totalling ₹2,039; net ₹25,961) cross-checked exactly against `payroll_master`'s own stored `gross_salary`/`total_deduction`/`net_salary` for the same employee/month. Branch filter (GRTL08 → 5 real employees), missing-criteria 400, and empty-month `[]` all correct. `npx tsc --noEmit` clean.

### Tenth pass (2026-07-27): Statutory Upload — EPF Contribution (ECR file)
`/reports/statutory-upload`, `src/lib/statutoryUpload.ts`, `POST /api/reports/statutory-upload`. The menu item literally labelled "Statutory Upload" was a genuine naming trap — it does **not** route to any of the four look-alike `Statutory*Controller.php` files (all dead/unrelated for this menu entry, confirmed via the live `hrm_menu` table); it routes to `EsiEpfReportController::hrreports()`/`generate_report()`. Confirmed this is a pure export tool, no real government-portal API call — "Upload" means "generate the file ready for upload," matching this project's existing understanding of the module.
- Scoped to just the **EPF Contribution** sub-report (of 6 real subtypes: EPF Member Registration, EPF Exit, EPF Contribution, ESI Monthly Contribution, WPS, EPF Upload) — this is the actual EPFO ECR upload row format (`UAN#~#Name#~#Gross#~#EPFWages#~#EPSWages#~#EDLIWages#~#EPFContribution#~#EPSContribution#~#EPFEPSDiff#~#NCPDays#~#Refund`, `#~#`-delimited, CRLF-terminated — read directly off the legacy view's `file_put_contents` call), the single highest-value/most standard piece.
- **Deliberately not ported**: ESI Monthly Contribution (legacy recovers ESI wage base via the same eval()-on-remarks trick, but GRTL's own ESI employee rate is *not* uniform across its live salary structures — some pre-2019 rows are 1.75%, current ones 0.75%, already a flagged data-quality issue — so the fixed-rate-division substitute used for EPF below would be wrong for older structures; needs a per-employee rate lookup first). EPF Member Registration/Exit (one-time KYC forms with many optional international-worker fields — passport, nationality — different shape, a form not a batch file, low volume). WPS Template (UAE/Gulf Wage Protection System compliance format, confirmed irrelevant — GRTL is an Indian tenant, `in.mypayrollmaster.online`). EPF Upload (an further variant on the same data, not yet researched).
- **Avoided eval() again, safely this time**: legacy recovers the PF wage base by eval()-ing an arithmetic string in `emp_salary_slip.remarks` then dividing by `.12`. Since the statutory EPF employee rate is a fixed, legally-mandated 12% (unlike ESI's non-uniform rate above), the wage base is exactly recoverable without eval as `abs(salary_amount) / 0.12` off the employee's own PF deduction line — mathematically identical to what the stored expression evaluates to.
- **Found two genuinely dead branches while reading the legacy view closely** (traced by hand, not guessed): a `subcat` ('pf' vs 'actual') toggle and an `emp_epf_contr <= 1800` threshold check that both evaluate to the same output on every real code path — collapsed to one code path here rather than replicating dead branching.
- Curl-verified end-to-end against real 2026-05 GRTL payroll data (temporary `GRTLADMIN01` password override, restored and confirmed immediately after): hand-checked every output row — e.g. employee 93's ₹1,800 PF deduction correctly recovers to a ₹15,000 wage base (statutory ceiling) with ₹1,800 EPF contribution and ₹0 EPS (not EPS-eligible); employee 101 (EPS-eligible) correctly splits ₹340 total into ₹236 EPS + ₹104 EPF-EPS-diff. Branch filter (GRTL02 → only the one real matching employee) and empty-month (2020-01 → `[]`) both correct. `npx tsc --noEmit` clean.

**Phase 6 status**: Employee Report, Payroll Report (11/15), LOP Report, Attendance Reports (6/6), Statutory Reports (EPF/ESI/ProfTax/Wage Sheet/Muster Roll), Employee Loan Report, Employee Advance Report, Statutory Upload (EPF Contribution) — all complete and curl-verified. Remaining, lower-value scope: `ServiceRecord` register, Company/Leave/Others report categories (Assets History, Company Profile, Employee Expense reports, Leave Report, Hierarchy, Events, Access Details, Tracking), 4 deferred Payroll variants, and the remaining 5 Statutory Upload subtypes (ESI Contribution, EPF Member Reg/Exit, WPS, EPF Upload). None curl-verified as blocking — all are additive using the same infrastructure whenever picked back up.

### Third gap-closure pass (2026-07-23): criteria picker UX — user directly flagged the mismatch
`CriteriaFilterPanel` was showing every active criteria's filter control simultaneously (all of Branch/Department/Employee/etc. at once). Legacy's real mechanic (confirmed by both the earlier live walkthrough and a direct user correction) is a single "Criteria" dropdown — the user picks ONE dimension to filter by (e.g. "belonging to a Branch"), only then does its value-picker appear, and a "+ Add Criteria" control adds another row, excluding whichever criteria are already in use. Rebuilt `src/components/reports/CriteriaFilterPanel.tsx` around an `addedNames` row list + an "Add Criteria" select offering only the not-yet-added criteria for the current report type (resets on report-type change); removing a row also clears its stored filter values. No backend change needed — selections still land in the same `Record<string, string[]>` shape the report generators already consume. `npx tsc --noEmit` clean; pages confirmed still rendering (307 redirect to login when unauthenticated, no server error) — full interactive verification of the add/remove-row UI itself needs a browser, which isn't available in this environment.

**Architecture decision (explicit, per user):** legacy wraps every report in a shared generic "dynamic criteria builder" (`*ReportsController.php`: `hrreports`/`changereporttype`/`addreportcriteria`/`loadcriteriaitems`/`generatereport`), driven by a live per-company `reportcriterias` table (`{reporttype, reportcriteria, reportcriteria_desc, reportcriteria_field, status}` — 206 real rows, confirmed live). User chose to replicate this dynamic engine rather than build fixed-filter one-off report pages. Ported as `src/lib/reports.ts` (`getActiveCriteria`, `getCriteriaOptions`) + `src/components/reports/CriteriaFilterPanel.tsx` (renders whichever filter dimensions are active for a report type, fetched live). **Deliberate security fix vs. legacy**: legacy builds each filter as a raw SQL string (`"<alias>.<reportcriteria_field> IN (...)"`, both the column name and values coming from user input) — a real SQL-injection surface. This port instead maps each recognized criteria name (`Units`, `Departments`, `SalaryStructures`, `Designation`, `DayTimeProcedures`, `LeavePolicyGroup`, `HolidayGroup`, `Gender`, `EmployeeDetails` — 9 as of the 2026-07-23 gap-closure pass, up from the initial 4) to a fixed, parameterized filter; unrecognized criteria are ignored rather than trusted.

- [x] **Employee Report** (`ReportsController::generateemployeereport`/`generateSalaryStructureReport`/`generateshiftpolicyreport`/`generateleavepolicyreport`/`generateholidaypolicyreport`) — `/reports/employee`, all 5 real subtypes (Employee List, Salary Structure, Shift Policy, Leave Policy, Holiday Group). `GET/POST /api/reports/employee`.
- [x] **Payroll Report** (`SalaryReportsController::generate<X>report`) — `/reports/payroll`, 11 of 15 real subtypes (Payroll Summary, CTC Summary, Gross Salary Detailed/Summary/Period-Wise/New, Bank Transfer, Salary Slip, Monthly CTC, Payroll CTC, Previous-Month Comparison), sourced from `payroll_master`/`emp_ctc_transaction`/`emp_salary_slip`, restricted to `action IN ('Approved','Processed')` matching legacy. Deferred: `BankTranferNew` (cosmetic duplicate), `salarystructure`/"CTC Detail", `SalaryCombined`, `Account` (see Fourth pass below for reasoning). `POST /api/reports/payroll`.
- [x] **LOP Report** (`LopReportsController::generatelopreport`, reporttype `Lop`) — `/reports/lop`, sourced from `emp_detail_timeattandance` joined to `employee_info`/`termination`, filters: date range, Branch, Employee, fixed condition `others='LOP' OR (leaves='LOP/LOP' AND weekoff IS NULL)` matching legacy exactly. `POST /api/reports/lop`.
- [x] Shared export: `src/lib/reportExport.ts` (`exportReportToExcel`/`exportReportToPdf`, client-side, reused across all report pages — SheetJS + jsPDF/jspdf-autotable, same libraries as the existing TDS report).
- Nav: Sidebar's pre-existing dead `/reports/salary` link renamed to `/reports/payroll` ("Payroll Report"), new "Employee Report" link added; `/reports/salary-slips`, `/reports/attendance`, `/reports/statutory/esi-epf` remain dead links pending follow-up passes.

**Research findings for follow-up passes**: `SalaryReportController.php` (singular) is dead/stale prototype code — real engine is `SalaryReportsController.php` (plural). `AttendanceReportsController.php` has 3 abandoned backup files plus a parallel `AttendanceReportsNewController.php` (routed per-tenant via a hardcoded pathMap in `ReportController.php` — needs a tenant-routing decision before porting). `EsiEpfReportController::generate_report()` computes PF/ESI entirely in PHP (no stored proc) including an `eval()` of a stored expression string from a `remarks` column — user decided this should be ported to TS without the `eval()` (translate the real expression logic directly), deferred to when that report is built. `statutoryReportsController.php` (lowercase) vs `StatutoryReportController.php` — both live, needs live `features`/`plan_feature` check to confirm which is actually routed before porting Statutory Registers.

Curl-verified all 3 reports end-to-end against real GRTL data (temporary password override on `GRTLADMIN01`, restored immediately after): Employee List (159 real employees, branch filter narrowing to 29 for GRTL08/Banglore confirmed correct), Salary Structure subtype (29 rows with real structure names), Payroll Report (23 rows for 2026-03, matching the real 21 Approved + 2 Processed count), LOP Report (26 rows for Jan 2026 out of 1,158 real LOP-marked attendance rows in the table). `npx tsc --noEmit` clean.

**Milestone:** Core reporting engine proven; remaining report types are additive using the same infrastructure.

---

## Phase 7: Loans, Advances, Assets
**Status: COMPLETE (2026-07-22).** Asset management was already built earlier (Phase 2.7, "Allocate Assets" — catalog + allocation). This pass added the creation side of Loans and Advances; the consumption side (`payroll_master_approve` settling pending EMI/advance rows during payroll approval) was already wired in Phase 5.2.

- [x] **Employee Loans** (`EmployeeLoanController.php` port, scoped to the primary single-loan create flow — the separate bulk-Excel-upload path, which duplicates the EMI-generation loop inline, is not ported). `src/lib/loans.ts`: `createLoan` (generates the full EMI schedule up front at creation time, matching legacy exactly — not lazily per month — including its rounding-remainder-to-last-month handling), `listLoans`, `payLoanAmount` (ad-hoc "Additional Payment" outside the schedule), `markLoanCompleted`. `GET/POST /api/loans`, `DELETE /api/loans/[id]` (soft-delete), `POST /api/loans/[id]/pay`, `POST /api/loans/[id]/complete`, `/loans` page.
  - **Investigated and resolved the "0% interest static balance" concern raised before building** (flagged by research as a possible legacy bug where opening/closing balance don't decrement month-to-month): re-reading `employeeloansave()` directly showed the *primary* single-loan creation path amortizes correctly even at 0% interest — the anomaly seen in live sample data traces to the separate, unported bulk-upload path's duplicated (and apparently buggy) copy of the loop. So despite the earlier decision to "replicate exactly," no bug actually needed replicating in the code path being ported — confirmed by curl-testing a real ₹10,000/4-month/0%-interest loan and seeing the balance correctly step 10000→7500→5000→2500→0.
  - **A second, real bug found and fixed during verification** (in the "Additional Payment" logic, discovered only once actually exercised): legacy's `amount_pay()` inserts its own payment row with `amount_paid=''` (empty) rather than the paid amount — meaning legacy's own completion check (`SUM(amount_paid)` vs `loan_amount`) can never actually see a lump-sum payoff reflected in that sum, a real gap in legacy's own bookkeeping. My first implementation had a related but different bug: the newly-inserted payment row was itself swept into the "apply payment to outstanding EMI rows" loop, corrupting the distribution (caught immediately via curl-testing — the payment landed on the wrong months). Rewrote to filter strictly on `paid_status='A'` (matching legacy's own filter, which naturally excludes the payment row's `'S'` status) for the distribution loop, and to record the real amount on the payment row itself so completion detection works correctly — a deliberate, documented improvement over legacy's own accounting gap, not a replicated bug. Curl-verified: a ₹5,000 payment against a ₹10,000/4-month loan correctly zeroed the two latest EMI months from the back, leaving the loan open at half paid; a second ₹5,000 payment correctly triggered auto-completion (`is_completed='Y'`, all schedule rows flipped to `paid_status='P'`).
- [x] **Salary Advances** (`EmployeeadvanceController.php` port — confirmed the separate `AdvanceController.php`/`advance_expense` table is an unrelated petty-cash/travel-expense feature, not touched). A single lump-sum row, no repayment schedule — repayment is consumed wholesale by the already-wired `payroll_master_approve`. `src/lib/advances.ts`: `getAdvanceLimit` (80% of one month's gross — GRTL isn't in the GLET/ABSG special-tenant list that gets a more complex attendance-prorated variant, so only the simple formula is ported), `isPayrollAlreadyProcessed` (advisory-only, matching legacy — it does not hard-block a save even when payroll's already run for that month), `createAdvance`, `listAdvances` (only shows `is_credited='N'` rows — a pending queue, defaulting to the current month, exactly matching legacy's real list behavior, not a simplification). `GET/POST /api/advances`, `DELETE /api/advances/[id]`, `GET /api/advances/limit`, `/advances` page.
- **Curl-verified end-to-end** against real live data (employee `emp_fkey=2`): loan creation, EMI schedule correctness, two-stage additional payment to full payoff and auto-completion, list correctly reflecting `is_completed='Y'`; advance creation, limit lookup (real ₹0 result reflecting this employee's actual live `emp_ctc_transaction` data, not a bug), list, and soft-delete. All test rows deleted afterward. Sidebar's pre-existing `/loans`/`/advances` links (previously dead) now resolve correctly — confirmed the paths match exactly, no Sidebar edit needed.

`npx tsc --noEmit` clean throughout.

### Completion pass (2026-09-08): parity gaps — see `legacy/Employee_Loans_Migration_Plan.md`

Scope-gap analysis + phased plan written to `legacy/Employee_Loans_Migration_Plan.md`. Phases 0–1 implemented (not yet curl-verified against a live DB):

- **Phase 0 — correctness fixes to shipped code:**
  - `markLoanCompleted` now also runs `UPDATE emp_loan_info SET amount_paid = loan_emi ... WHERE status = 1` (legacy `completed()` does this; without it a force-completed loan under-reports paid/balance/completion on the ledger + Loan Report). Added the `status = 1` filter legacy uses on both updates.
  - `payLoanAmount` gained a `userRemarks` param → written to `emp_loan_info.user_remarks` on the `'S'` payment row (legacy reads `remarks` from the POST and stores it there; the port dropped it). `POST /api/loans/[id]/pay` now accepts `remarks`.
  - `payLoanAmount` guards ported from `amount_pay()`, all raising a new `LoanValidationError` (→ HTTP 400 with legacy's exact message): (1) payroll already processed for the current month for this employee — **hard block** for loans, unlike the advisory-only advances case; (2) this month's EMI on this loan already settled (`paid_status='P'`, not a transfer) → "Loan Already Processed"; (3) payment would exceed remaining balance after this month's scheduled EMI → "Total Additional Payment exceed Balance Amount".
  - `isPayrollAlreadyProcessed` moved from `lib/advances.ts` to `lib/payroll.ts` (now shared by loans + advances); re-exported from `advances.ts` so existing callers are unchanged.
  - `/loans` list: added the employee / branch / EMI-start-month filter toolbar (mirrors `employeeloanlist()`; the API already accepted these params but nothing rendered controls).
- **Phase 1 — per-loan detail / EMI ledger view:**
  - `getLoanDetail(pool, loanPkey)` in `lib/loans.ts` — master + computed schedule (running opening/closing balance, closing floored at 0, rows with `loan_emi=0 AND paid_status IN ('A','P')` hidden and not advancing the serial number — matches `viewloan.ctp`), plus `paid` / `balance_amount` (legacy's `<= 12 → 0` quirk kept) / `completion_pct` (ceil, capped 100). Does **not** replicate `viewloan()`'s writes-on-read column normalisation (legacy cruft).
  - `GET /api/loans/[id]` → `getLoanDetail`, 404 when missing/soft-deleted.
  - `/loans/[id]/page.tsx` — header card, "amount to be paid" + progress bar, month-by-month ledger table, and the additional-payment (amount + required remarks) and "Mark completed" actions relocated here from the list (shown only while `is_completed != 'Y' && balance_amount > 0`).
  - `/loans` list rows now navigate to `/loans/[id]`; the per-row pay/complete icon cluster removed (delete kept, now with a confirm).

Not yet done (later phases in the plan): EMI month-transfer (Phase 2), per-loan PDF/Excel (Phase 3), 3-day edit window (Phase 4), Loan Report summary mode (Phase 5), `getLoanBalance` endpoint (Phase 6), bulk uploads (Phase 7, deferred).

`npx tsc --noEmit`, `eslint` on touched files, and `next build` all clean.

---

## Phase 8: Plan-Gated / Company-Specific Modules
**Status: NOT STARTED**

- [ ] Performance management (if plan permits)
- [ ] Site & field operations (VGFS, VSFS, GEDE only)
- [ ] Procurement / inventory
- [ ] Mobile tracking

---

## Phase 9: Polish & Cutover
**Status: NOT STARTED**

- [ ] Migrate Slim PHP API v1 endpoints to Next.js API routes
- [ ] End-to-end test with real data (parallel run)
- [ ] Performance test (connection pool under load)
- [ ] UI responsiveness + mobile audit
- [ ] Error boundaries and loading states
- [ ] Production environment setup

---

## Environment Setup Checklist (Manual — User Action Required)
- [ ] MySQL running locally with `mypayrol_control_db` and at least one company DB (e.g. `mypayrol_trial`)
- [ ] Fill `.env.local` with actual DB credentials after project is created
- [ ] Run `npm install` in `rizo/` directory
- [ ] Run `npx shadcn-ui@latest init` in `rizo/` directory after npm install
- [ ] Run `npm run dev` to verify the dev server starts

---

## Login Rework (2026-07-06)

The original `authorize()` in `rizo/src/lib/auth.ts` was an invented simplification (required a "Company Code" field, queried a single unified `user_credentials` table in the control DB) that didn't match the legacy app and had a live bug (`central_control.plan_id` doesn't exist — any login attempt would SQL-error).

Reworked to match legacy's real two-path flow (`legacy/Controller/SiteController.php:268-360`, `legacy/Controller/Component/LoginManagementComponent.php`):
- **Admin path**: control DB `user_credentials` (by `user_id`/`email`) → `userGroup = 1`, `empFkey = null`, `planId` sourced from that row's own `plan_id` column (fixes the bug above).
- **Employee path** (fallback): company code derived by stripping digits from the username (e.g. `GRTL100011` → `GRTL`), looked up in `central_control`, then checked against that company's own `user_credentials` table via `getCompanyPool()` → `userGroup = 2` (hardcoded, matching legacy even though the company table's own `user_group` column holds different data), `empFkey` from the row.
- Login page no longer has a Company Code field — just Username + Password, matching legacy UX.
- Ported legacy's account lockout (3 failed attempts, resets daily) for the employee path; surfaced as a distinct "Account locked" message via a thrown `LOCKED` error from `authorize()`.

**Still deferred:** `reset_login_flag = 'Y'` blocks login but doesn't build the actual password-reset UX; email-based device login (`emp_device_comp_branch`) is out of scope.

---

*Last Updated: 2026-07-06 — Login rework complete*
